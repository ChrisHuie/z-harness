name: harness-check
on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: harness_check (ci mode — skips local-machine and binary anchors)
        run: python3 hooks/harness_check.py --ci
