---
name: project-port-skills-hardening
description: "Bidirectional adapter-port skill hardening program (post-Teal #4765): doctrine + scope DECIDED, phase structure, audit-corrected rules. Multi-session, both directions."
metadata: 
  node_type: memory
  type: project
  originSessionId: aa049dfd-aa09-4a10-a1e2-61f821d8e096
---

Hardening the adapter-port skills (BOTH directions) after Teal PR #4765, so future ports don't repeat the failures and can one-shot more adapters. DECIDED 2026-06-03 (user, via 4-agent audit):

- DOCTRINE = **lean-conformance + ADR**. Skills were built "above-and-beyond" (emit fuzz/bench/doc.go; ROADMAP brags "only adapter in 267 corpus with them") — target repos REJECT that. Pivot: emit exactly what upstream merges; fuzz/bench/doc.go become dev-only aids stripped before PR. Record as ADR docs/decisions/009-*. Reverses a documented strategy → reframe methodology (canary-runbook §5 quality-bar rows ~558-565 + §10; teal-reflection PR5/PR6 ~236-248 + §2c ~158; ROADMAP ~L56).
- SCOPE = **Tier 1 + Tier 2 (full one-shot program), BOTH directions** (port-java2go + port-go2java + Go reviews + Java reviews + read skills). Multi-session.

PHASES: 0 = ADR (doctrine). 1 (Tier1 harden) = shared disposition system + "target-conformance beats source-fidelity" tie-breaker (review/skills/shared/ on BOTH sides: framework-utilities.md + framework-utilities-java.md); corrected check fixes; porting-skill references; template getBidType nuance + symmetry; B6 methodology reframe; gzip repo-wide + Java reconcile. 2 (Tier2 archetypes) = C10 custom non-OpenRTB body, C11 auth/signing, C15 privacy/native, C12 Go↔Java template symmetry invariant, C16 read-side extraction, + C13/C14/C17/C18. FINAL = re-run hardened reviews vs Teal PR as proof.

AUDIT-CORRECTED rules (my initial plan was too blunt; verified vs live prebid-server — do NOT regress):
- getBidType: error-on-miss is the MAJORITY upstream default (135/261 adapters return (BidType,error); 63 return bare BidType w/ vouched constant, e.g. adkernelAdn→BidTypeVideo). Template default = error-on-miss, BUT keep VOUCHED-constant-fallback option matching source's REAL default (template bug: blindly defaults "banner" when source defaults "video"). Review flags SILENT/unvouched/mis-typing fallbacks, NOT all constant fallbacks. Do NOT force (BidType,error) unconditionally; do NOT force impsByID map (14/261 use it → optional/INFO for nested multiplicative loops only). The Teal PR fix (error+skip+map) was correct FOR TEAL; generalizing it bluntly to the template was the error.
- Corpus = "canonical (no extra test/doc artifacts)" NOT "exhaustive" (multi-file helpers models.go/utils.go are allowed per go-artifact-shapes.md L70).
- gzip casing = 8 sites repo-wide incl. read-side + reconcile Java review's "Go is case-sensitive" claim (bidder-config-pr-review ~379); SPLIT value-casing (gzip→INFO) from field-name typo (endpoint-compression→stays FAIL). Runtime ToUppers at exchange/bidder.go ~L1133 (NOT :850/:100).
- tie-breaker must NOT auto-promote WARN-by-design cases (Site/App-ID defensive checks, forward-compat branches).
- Missed siblings to also edit: porting-guide §4 prohibited-patterns; duplicated pre-submit lists; read-side endpointCompression docs; CHANGELOG; the ADR.

9 NEW classes beyond Teal's 8 (one-shot blockers): C10 custom body + C11 auth = NO template path (can't one-shot huaweiads/rubicon/mediasquare today); C12 template asymmetry breaks both-ways (Go dropped ext-prebid-video-placement, Java still emits; by-bid-mtype real in Go, TODO-stub in Java); C13 params byte-fidelity/schema-validity (aax minLength loss = only `fail` in suite); C14 alias/registration topologies (per-alias IT classes; freewheelssp 3-YAML; cadent dual-core); C15 privacy/Regs/GPP + native-request build; C16 read-side field mis-extraction (freewheelssp ext field-count drift); C17 open-URL/first-imp endpoint derivation; C18 geoscope/GVL/compression/maintainer-email metadata gates.

Java side mirrors ALL class gaps (getBidType silent-banner WORSE — no vouching mechanism). Java review STRENGTHS to mirror BACK to Go: silent-binding-typo→FAIL, verified-absent-method blocking (grep-verified absence elevates ASK→BLOCK), constructor↔factory signature alignment, strict-form PR-type detection.

PROGRESS (multi-session, in progress): branch `harden/port-skills-lean-conformance` (off main; pushed to origin, no PR opened yet — user authorizes each push). LANDED: Phase 0 (ADR-009 lean-conformance doctrine + README index) + Phase 1.1 (shared disposition system: severity→action ladder + target-conformance-beats-fidelity tie-breaker + no-auto-promote guard + more-artifacts-≠-quality corollary, BOTH framework-utilities.md & framework-utilities-java.md) + Phase 1.2a/b/c (Go adapter-code & Java bidder-class checks wired to disposition; 3-tier bid-type calibration BOTH sides = FAIL mis-type / WARN unreachable-safety-net / OK sole-media-type-or-vouched, deliberately NOT blanket-FAILing the upstream-merged `.orElse(banner)` pattern; non-canonical artifact doc.go/fuzz/bench→FAIL w/ legit multi-file helpers preserved; perf nested-loop-only; first-valid-wins divergence; gzip value-casing corrected to INFO — verified vs `exchange/bidder.go:850` `strings.ToUpper`, field-NAME-typo stays FAIL) + **Phase 1.2d (second-pass checks)**: 3 more Go review checks — stdlib-idiom-duplication (hand-rolled helper duplicating a one-line stdlib idiom, e.g. isBlank→strings.TrimSpace; cites the tie-breaker), destructive-overwrite-of-caller-ext/JSON→document-the-clobber, distinct-error-message-per-unmarshal-failure-mode; + maintainer-email gate reframed name-pattern-DENY-list→role-token-ALLOW-list so a personal address on the bidder's OWN corporate domain no longer slips. LESSON (generalizable): hardening can be DOCTRINE-complete but CHECK-incomplete — a principle/tie-breaker with NO concrete mechanical check to fire on catches nothing (a hand-rolled isBlank proved it; strings.TrimSpace is identical incl. NBSP). A finding that is a regression from an already-correct template → prioritize the self-review gate (1.3); run-notes that celebrate the anti-pattern → prioritize methodology reframe (1.5). + **Phase 1.4-partial — multiformat⇒bid.MType invariant (2026-07-01, pushed, triggered by Teal #4765 `postindustria-code` follow-up)**: the true harness gap the Teal review left — we checked bid-type COVERAGE + FALLBACK-silence but never linked `multiformat-supported` to a mandatory per-bid `bid.MType`. An imp-only resolution (fixed banner>video>native priority) mis-types co-present formats on a multiformat imp (all bids→banner). Landed BOTH directions: Go review canonical rule (framework-utilities "Multiformat imps require per-bid disambiguation" = FAIL) + adapter-code-pr-review step-4 FAIL tier + step-9 coverage cross-link; Go port template `bidder.go.j2` new `ctx.multiformat_supported` + loud-fail guard on imp-only resolution; port-java2go SKILL rule; Java review bidder-class-pr-review mirrored FAIL tier + cross-link; Java port template `bidder.java.j2` NOW has a REAL `by-bid-mtype` branch (switch on `bid.getMtype()`, throws on miss) replacing the silent-banner TODO stub → **partially closes C12** (Java by-bid-mtype no longer a stub); +7 tests both directions (guard fires / correct shape renders / single-format unaffected), 554/554 pass. Guard mechanism: `{}.<missing>.fail` raises UndefinedError even under default (non-Strict) Jinja Undefined — verified empirically. REMAINING: 1.3 porting-skill references (canonical-corpus guard, fidelity-boundary, self-review gate, §4 prohibited-patterns), 1.4-rest template getBidType nuance not covered above (fallback-value symmetry etc.), 1.5 B6 methodology reframe (canary-runbook/teal-reflection/ROADMAP), Phase 2 (C10/C11/C13-C18 archetypes + C12-remainder: Go dropped ext-prebid-video-placement asymmetry), Final (re-run hardened reviews vs Teal #4765 as proof).

**F6 (2026-07-06) — reverse-direction validation COMPLETE + port-go2java PRODUCTION-PROMOTED 0.5.0→1.0.0.** The full Teal flow ran Go→Java on rtbstack (green-field, outside the MVP corpus): read → port → 6-reviewer gauntlet (25 findings, 0 FAIL, every finding adversarially verified) → upstream PR to prebid-server-java (merge-quality; Gates 2+3 green-field, Jacoco 100% line). BOTH port directions now at v1.0.0. Reflection landed as branch `feat/f6-port-go2java-v1.0.0`: Rules 47 (grouped-by-key batching) / 48 (param-derived endpoint macros + new taxon, taxonomy 1.1.0) / 49 (opposite-framework-default keys → effective-value emission; the gauntlet's headline catch — Go absent-key false vs Java adapter-default true on modifying-vast-xml-allowed; template arm was INVERTED, now always-explicit), rules 0.3.0, ADR-010, canary trace under docs/runs/. Durable lessons: (1) spec-vs-spec R5 is value-blind to framework back-fill — 3 live upstream pairs (adkernelAdn/adverxo/thetradedesk) diverge TODAY and pass R5; reader-side effective-value program DEFERRED, tracked in ROADMAP Phase E; (2) never pipe mvn gate runs (exit-code masking, bit 3×; canonical gate `mvn -B package --file extra/pom.xml`); (3) teammate reviewer agents must DELIVER findings via message to the orchestrator — printed output is invisible. Hardening program remainder still open: 1.3 porting-skill references, 1.5 B6 methodology reframe, Phase 2 archetypes (C10/C11/C13-C18 + C12-remainder), Final re-run vs Teal #4765.

Workflow: branch before edits (skills repo on main); user approves per PHASE-GROUP (not per-edit, given scale); never push without authorization. See [[reference-port-fidelity-vs-target-norms]], [[project-teal-pr-remediation]], [[feedback-no-pr-specifics-in-memory]].
