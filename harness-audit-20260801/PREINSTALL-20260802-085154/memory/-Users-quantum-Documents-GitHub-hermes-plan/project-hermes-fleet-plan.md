---
name: project-hermes-fleet-plan
description: "This repo is the architecture plan for a personal multi-Mac Hermes agent fleet (central platform, domains = appendix fixtures); plan at v0.14.2 on main (2026-07-20, executable source-control authority + audit_plan.py green); Phase-0 plan status: review, awaiting operator OpenSSH-signed approval"
metadata: 
  node_type: memory
  type: project
  originSessionId: a5bb67a2-2dbc-4364-849b-52df11524c93
  modified: 2026-07-20T11:49:00.974Z
---

`hermes-plan` holds the full architecture/rollout plan (README + docs/00–13 + appendix 90) for the user's **personal** agent-fleet lab: Mac Studio control plane + Mac mini runners + Windows laptop over Tailscale, running Hermes agents (Nous Research `hermes-agent` — one *profile* + OS user + isolation slot per "worker").

**v0.2 pivot (2026-07-19, current):** the plan's focus is the **central platform, NOT domains**. Key changes from v0.1: (1) **no model gateway** — capture + cost control are native per harness/provider, supporting BOTH subscription (Claude Max/ChatGPT, estimated-only) and API-key (metered) billing per worker; (2) "verticals" (betting/kalshi/stocks/cards) reframed as domain-blind **workers** on a fixed contract, with the domain research moved to `docs/90-appendix-verticals.md` as test fixtures; (3) new pillars added — `docs/12-sandboxing-isolation.md` (tiered isolation I0–I4, nested-harness containment, Tart snapshots for durable/resumable sessions) and `docs/13-fleet-intelligence.md` (reflection/medic/teacher via a governed eval-gated promotion pipeline). Emphasis on nested-harness observability (Hermes→Claude Code/Codex/OpenCode→subagents) and agents fixing/teaching agents.

**Why:** v0.1 done in one pass (5 research streams). v0.2 reframe by user direction. v0.2.1 folded in 3 of 4 second-round streams: (a) sandbox — "claw patrol"=`denoland/clawpatrol` (egress cred-firewall, NOT a sandbox; pairs w/ action broker), NVIDIA tool=`NVIDIA/OpenShell` (libkrun microVM runtime wrapping our agents); **Firecracker is KVM/Linux-only but libkrun gives Firecracker-class isolation natively on the Macs** — no Linux box needed for isolation; OpenCode has NO native sandbox (never run bare); Claude Code Bash sandbox reads ~/.ssh/~/.aws by default (must deny in provisioning). (b) obs — add ONE second pane = **Arize Phoenix** self-hosted (OpenInference-typed nested trees + evals; ELv2). (c) fleet-intel — Hermes ships most machinery (`skill_manage`, `/learn`, `write_approval`); **`NousResearch/hermes-agent-self-evolution`** = productized medic (GEPA over traces→PRs); Mem0/Supermemory for scoped shared memory; live agent-to-agent teaching deliberately absent → learn via artifacts. Docs 12/13 GROUNDED. v0.2.2: 4th stream (harness-tree) fully integrated — **all research complete, none outstanding**. Key late findings: OpenCode=`anomalyco/opencode`, no OTel, Anthropic-API-key-only; **Claude Code `X-Claude-Code-*-Agent-Id` headers DON'T exist** (corrected); subscription workers = org-seat/estimate/T0 only (personal Max has no admin API, multi-account=ToS violation) → API-key mode is the fan-out path; **read cost from each CLI's native store, NOT Hermes's terminal scrape** (returns nothing billable for Codex); **override bundled Codex skill's `--yolo`/danger-full-access default**; Codex OTel defaults to Statsig (force otlp/none); OpenAI has no per-project hard $ cap via API (watchdog is the only stop); OpenRouter = best per-key control. Doc 04 has the 8-item capture-blind-spot register + reconstruction scheme + provider cap table.

**v0.3 (2026-07-19):** added `docs/14-cloud-and-always-on.md` (cloud/always-on tier) per operator request, verified vs live Cloudflare/Tailscale/Fly docs. Decisive: **Cloudflare = watchdog + ingress, NOT worker host** — Workers can't run Hermes (no FS/subprocess); Cloudflare Containers have ephemeral disk (resets Hermes SQLite), can't join the tailnet (egress 80/443/DNS, rootless, no UDP → Tailscale won't work), ~3–6× cost. Always-on **Hermes worker + medic → Fly.io** (µVM, native Tailscale, persistent volume, ~$6–11/mo) or Hetzner/DO VPS; joins tailnet → observability identical to a mini AND recovers the L2 exec-ledger via auditd/eBPF (Linux root). Watchdog = **dumb tripwire (Cloudflare Worker+DO+Cron dead-man) + smart medic (Hermes on Fly/VPS)**; instances push heartbeats to a public HTTPS ingress (`cloudflared`/Funnel on Studio, mTLS/Access). Cloudflare native OTLP export makes L3/L4 port cleanly; the one CF-Container loss is the kernel exec-ledger (compensate app-level; no world-actor workers there). New decisions D37–D40, open items O22–O24.

**v0.4 (2026-07-19):** operator added **command center + lossless-capture + cloud data plane** direction. **"multica" = `multica-ai/multica`** (~41k★, v0.4.4) — open-source managed-agents platform / command center that orchestrates coding-agent runtimes **including Hermes + Claude Code/Codex/OpenCode/Kimi/OpenClaw** as "teammates"; Go+Next.js+Postgres17/pgvector+local daemon; concepts: Autopilots(cron), Squads, Skills, Unified Runtimes(local+cloud), Multi-Workspace, operator-vs-worker separation. **Multica = command-center layer ON TOP of Hermes+CLIs; our capture/control/cloud substrate goes UNDERNEATH** (doc 15). Operator's plan: fork/self-host Multica, first agent work = improving it (dogfood). New paramount **P13 (lossless capture — never lose execution/prompts/output/cost; cloud-portable S3-compatible system-of-record switchable across AWS/Azure/GCP; hot path may be lossy, cold never)** + **P14 (command center, separation of concerns, operator above both)**. Review dimensions (work/token efficiency, models, output style, tool usage, data passed, subagents, skills) now first-class in WorkRecord + reflector. New docs **15 (Multica)** + **16 (durable cloud data plane)** — both v0.1 with ⏳ pending 2 in-flight research streams (`research-multica`: license/data-model/lossless-ness/tap-surface/does-it-replace-Plane; `research-cloud-dataplane`: primary-cloud/multi-cloud-posture/lossless-write-path). Decisions D41–D44, open O25–O29. Docs 00/06/07/11/13/README reconciled. Now 17 docs+appendix (00–16, 90).

**v0.4.1:** `research-cloud-dataplane` landed → **doc 16 finalized**. Key verdict (pushes back on operator's "AWS/Azure/GCP maybe all 3"): **buy PORTABILITY, not multi-cloud — tri-cloud active-active is theater.** Egress is the ONLY real cost lever ($0 on R2, free-to-3× on B2, $0.08-0.12/GB hyperscalers) and it punishes switching. **Primary durable archive = Cloudflare R2 (or Backblaze B2): zero egress = never actually locked in**, native S3 API, Object Lock+versioning (WORM). If a named hyperscaler is mandated: GCS(S3-interop)/S3 primary + rclone async mirror to R2/B2. Everything writes S3 API (Azure is the odd one out, needs shim). Lossless write path = OTel Collector `file_storage` persistent queue + `awss3exporter` + `retry_on_failure`, **scrub-before-WORM** (can't delete leaked secrets once Object-Locked). **Langfuse v3 is blob-first → its bucket IS a replayable system-of-record** (ClickHouse rebuildable from it). Keep hot stack on Studio; only cold archive → cloud. Stay vendor-neutral (OTLP+object storage), not CloudWatch/Azure Monitor. D45/D46; O26-O28 resolved. Doc 08 reconciled (C1 system-of-record continuous-immutable-to-cloud supersedes restic-as-primary; C2 relational still restic). **R2 converges with the Cloudflare watchdog (doc 14 Logpush→R2).** **v0.4.2: `research-multica` landed → doc 15 finalized; ALL research closed (12 streams).** Verified from source: Multica license = **modified-Apache-2.0 source-available** (internal fork clean; keep frontend branding or run headless; no third-party SaaS). Go+Chi+sqlc+Postgres17/pgvector+Redis+Prometheus (NO OTel/Langfuse/Loki). Stores `task_message` (activity, **secret-redacted** via pkg/redact, not byte-lossless) + `task_usage` (tokens only, **NO cost_usd**) — **cost is DERIVED from a hardcoded `metrics/pricing.go` table, discarding the CLI's authoritative total_cost_usd** (headline gap → keep native CLI cost capture). **No OTel/webhooks/external events API → capture is PULL-ONLY** (Postgres CDC/read-replica on task_message/task_usage/agent_task_queue + PAT REST + realtime WS + /metrics scrape). **run_id injects via agent custom-env (migration 040) + custom-args (041).** Hermes-under-Multica runs an **isolated per-task HERMES_HOME overlay** (state.db+memories task-local, NOT ~/.hermes) — our Hermes capture works but must discover the overlay path. **Multica REPLACES Plane** (+ our cron via Autopilots, + agent registry); ADDS Squads/profiles/templates/multi-workspace. cloudruntime = client to Multica's OWN managed cloud (not BYO) → for Fly/VPS tier, run the Multica DAEMON there as a local runtime. No plugin system → extension = fork+code (approachable). **VERDICT: command-center-on-top, our lossless substrate underneath as source of truth (D44), Multica tapped read-only.** Top-3 first-work tasks = (1) Postgres CDC→substrate, (2) native CLI cost+raw-session capture underneath, (3) add OTLP/MinIO export shim to our fork + extend pricing.go. D11 superseded (Plane dropped), O9 moot, O25 resolved. Plane→Multica reconciled across all docs. **17 docs+appendix (00–16, 90); plan at v0.4.2.**

**v0.4.3 (operator refinements):** (1) **Observability FIRST confirmed** — doc 10 Phase 1 reordered: ingestion core (OTel Collector fan-out → Prometheus[metrics]/Langfuse+Phoenix[traces]/Loki[logs] + R2 archive) + native-OTel wired per tool, PROVEN via a real Claude Code/Codex run, BEFORE Multica layers on top. Added a concrete **"Native OTel ingestion" section to doc 03** (per-tool matrix + Collector config sketch; Prometheus = metrics via push/`prometheusremotewrite`, not per-process scrape). (2) **Multica REFRAMED as benchmark/starting-point/fork-base, NOT permanent** — operator is building their OWN command center; the SUBSTRATE is the invariant, command center is a swappable surface (P9/D44); reframed doc 15 (new "starting point not destination" section), doc 00 mission+glossary, D41. (3) One research stream in flight: `research-native-otel` — verifying whether the **Claude DESKTOP APP** emits exportable OTel (likely NOT — fallback = Anthropic Usage/Cost + Claude Code Analytics APIs) + confirming CC/Codex→Prometheus specifics. Doc 03 Claude-app row marked ⏳ pending it. Everything else (CC rich OTel, Codex statsig-gotcha, OpenCode no-OTel-SSE-bridge, Gemini OTel) already verified.

**v0.4.3 native-OTel research LANDED (13th & final stream, all closed):** **Claude desktop/web chat app = CONFIRMED no-exportable-OTel blind spot** (internal-only telemetry; `/usage` even excludes other devices). Fallback = **Enterprise Analytics API** (`/v1/organizations/analytics/*`, covers claude.ai Chat surface + USD cost, but **org-tier only** — personal Max exposes no such API, ties D33) OR route work through Claude Code. **Claude Code CLI AND IDE-extension** both emit rich OTel (shared ~/.claude/settings.json) incl. `claude_code.cost.usage` (USD approx) + `.token.usage`. **Codex `[otel]` confirmed** (statsig default trap) but emits **lifecycle metrics only — token export UNVERIFIED, no USD** → Codex cost = rollout TokenCount×rate. **Gemini CLI** OTel confirmed (`gemini_cli.token.usage`, no USD). Ingestion (D47): one OTel Collector → **Prometheus** (metrics, via `prometheusremotewrite`, Prometheus needs `--web.enable-remote-write-receiver`; push not scrape) + **Langfuse** (traces, `otlphttp` `/api/public/otel`, HTTP-only no gRPC) + **Phoenix** (`:6006`) + **Loki** (logs, `otlphttp` `/otlp` — NOT deprecated `loki` exporter). Doc 03 has the full matrix + Collector config sketch. D47/D48/D49 added. Docs 03/04/10/11/00/15 updated. **Plan at v0.4.3; 13 research streams all closed.**

**v0.4.4:** operator raised the cross-surface capture gap (chat app + multiple harnesses/apps/surfaces/modes — "app to cli to interactive to printable session"; "since hermes is at a higher level we can achieve this"). → new **doc 17 (Centralized Analytics & Cross-Surface Capture)** + principle **P15: capture at the highest vantage point that sees the work.** Vantage hierarchy: orchestrator (Hermes/Multica — they SPAWN the leaf tools so they capture I/O even when the tool exports nothing) > native tool telemetry > session-file harvest > provider analytics API. Build a **thin unification layer + 3 feeders** (orchestration capture shim, session-file harvester, provider-analytics pollers) reconciling per-field into the one system-of-record; **integrate** the components (Langfuse/Phoenix/Prometheus/Grafana + provider analytics APIs — Anthropic Enterprise Analytics covers claude.ai Chat but org-tier only). No product does app+CLI+orchestrator unification → build it (thin), not buy. Reconcile-per-field: cost authoritative=native/session-file/provider-API (never Multica derived); raw prompts=session-file (not orchestrator redacted). Coverage report now asserts surface coverage (every surface×mode has a named path). D50, P15, O30/O31. Done from existing verified knowledge (no new research — budget). **19 docs+appendix (00–17, 90); plan v0.4.4.**

**v0.4.5:** operator gap-finding thought exercise on the **scheduled-job lifecycle** ("monitor, catalog, add in Multica, assign the right agents, learn for future runs, designed outputs"; "surface suits the job — Claude Code for coding"). → new **doc 18 (Job Orchestration)**. Finding: plan had the SUBSTRATE but no **job-lifecycle layer**. Gaps filled: G1 git-versioned **job specs** (schedule+routing+output-contract+budget); G2 **job→surface/model routing** via a tunable affinity table (Multica Squads execute; the "Claude Code for coding" insight as a learned policy); G3 **durable scheduling** (retry/catch-up/overlap/missed-run — O32: Multica Autopilots vs add Temporal/Inngest/Hatchet/Windmill); G4 **`job_id` catalog** (view over ledger + Jobs board); G5 **output contracts + validation** (designed outputs = a GATE, JSON Schema/LLM-judge rubric, invalid=failed run); G6 **schedule-SLA monitoring** (fired on-time+produced-output, distinct from dead-man liveness); G7 **job-level learning** (reflection keyed by job_id → spec improvements via governed promotion pipeline; tunes the surface affinity by evidence). Thin — mostly assembly over substrate; only possible new component = durable-execution engine (O32). D51, O32-O34. Reconciled docs 00/11/13/15/README. ALSO spawned **`research-industry-stack`** (in flight): what top AI-native companies use in July 2026 (orchestration/obs/scheduling/memory/gateway/coding-agent/sandbox) — benchmark our stack. **20 docs+appendix (00–18, 90); plan v0.4.5.**

**v0.4.6:** operator added 3 interests: (A) testing/evals/**comparing models AND harnesses on tasks**, (B) **task queuing** (not just scheduling), (C) **profile management** (store + switch/schedule/hot-swap centrally). → new **doc 19 (Evaluation & Comparison)**: 3 modes (regression/comparison/continuous-shadow); **BUILD the cross-harness runner over the capture spine** (no tool runs one task across arbitrary harnesses; cost/latency/tokens captured free per cell), **ADOPT** eval-platform experiments (Langfuse; ⏳+promptfoo/Inspect) + external benchmarks (⏳ SWE-bench/Terminal-Bench). **KEY: the comparison scoreboard IS the routing affinity table (doc 18 G2/G7) — routing tuned by evidence** (D52). (B) **Queuing** added to doc 18: **Multica already has a Postgres claim-based queue** (`ClaimTasksByRuntime`, lease/ack) — we add priority/fairness/concurrency/backpressure/retry/dead-letter/budget-admission policy; scheduling+queuing = two triggers, one router (part of D51). (C) **Profile library + dynamic activation** added to doc 02: profiles=git-versioned library; **switching = activation/placement NOT identity-mutation (P7 intact)** — schedule or hot-swap which profiles active on which slots centrally; deactivated profiles **suspend** (state persists on disk/snapshot) + resume; within-profile model/mode = config variant; activation = operator/T2 audited action (D53). D52/D53, O35-O38. Research in flight: `research-industry-stack` (2026 top-AI-company stack benchmark) + `research-eval-comparison` (agent benchmarks + eval platforms + queue patterns). Reconciled docs 00/11/13/15/README. **21 docs+appendix (00–19, 90); plan v0.4.6.**

**v0.4.7: `research-industry-stack` landed → new doc 20 (Industry Benchmark, July 2026).** Verdict vs top AI-native cos: **ALIGNED** (OTel/Langfuse+Phoenix standard; Hermes+Multica = frontier "custom harness + control plane" pattern; GEPA/DSPy+promptfoo = literally what Nous Research uses; Mem0; Claude Code/Codex/OpenCode mainstream). **AHEAD** of typical solo op (running GEPA at all, self-hosted OTel obs, a real command center). **BEHIND — 3 real gaps:** (1) **durable execution** — cron+watchdog below the Temporal/Inngest/Trigger.dev/Hatchet/Cloudflare-Workflows bar → **RECOMMENDED: Cloudflare Workflows/Agents** (lowest friction, already on Workers+R2; resolves O32; the biggest gap); (2) **no ephemeral code-exec sandbox** (E2B/Modal/Daytona fast-start+snapshots; **Fly Sprites** in-family) → O40; (3) **no online-eval/guardrail + distilled judge** on live traffic (Galileo Luna-style) → O39. Minor: no-gateway misses spend/fallback (defensible solo; lightweight LiteLLM = escape hatch for resilience only — nuanced D6). Table-stakes to track: OTel GenAI semconv still experimental (pin OpenInference); MCP as universal tool interface; prompt caching supports no-gateway; turbopuffer pairs w/ R2. Applied gaps to docs 12/13/14/18/19; D54; O39/O40; O32 resolved (Cloudflare Workflows). `research-eval-comparison` STILL in flight (agent benchmarks + eval platforms + queue patterns). **22 docs+appendix (00–20, 90); plan v0.4.7.**

**v0.4.8: `research-eval-comparison` landed — FINAL stream, ALL 15 research streams now closed.** Corrected doc 19: a cross-harness runner DOES exist — **ADOPT Terminal-Bench + Harbor** (ships adapters for Claude Code/Codex/OpenCode/Aider/OpenHands/Goose/Gemini/Qwen/Grok/Cursor CLIs — `tb run --agent <adapter> --model <m>`) + **SWE-bench Verified** (coding anchor) + **τ-bench** (tool use) + **Inspect (`inspect_ai`, UK AISI)** as the sandboxed agent-eval engine (agent-bridge wraps external harnesses) + **Langfuse experiments** = scoreboard; **NO Braintrust** (proprietary; Langfuse covers). BUILD only the **Hermes-native adapter** + champion-challenger/shadow-eval loop. **Methodology**: separate model/harness axes; k-trials/task; report mean±CI + **pass@k (capability) AND pass^k (reliability)**; cost as **$/task AND $/success** ($/success ranks configs); speed incl. step/turn count; **McNemar** (paired binary) + bootstrap CIs at small N; **Pareto frontier (cost×quality) = the routing affinity table**; **use DSPy/GEPA to tune each scaffold PER MODEL before comparing** (else unfair). Champion-challenger = replay captured runs through challenger + diff (cheap since we capture all) + CI regression gate. **QUEUE CONFIRMED**: Postgres claim-queue = correct 2026 default (River/pgmq/Graphile/Oban pattern; Multica's is right); durable-workflow engine (Cloudflare Workflows / **Hatchet** — Postgres-co-located w/ Multica) goes ON TOP for O32, NOT Redis underneath. O35/O36/O37 resolved. Research confab flags: promptfoo NOT acquired by OpenAI; tau-bench has no voice/τ³ variant. **22 docs+appendix (00–20, 90); plan v0.4.8; ALL research closed.**

**v0.4.9:** operator raised **DBOS** for durability → verified live (dbos.dev). **DBOS Transact = embedded, Postgres-ONLY durable-execution LIBRARY** (not a service/cluster/broker); first-party SDKs **Python/TS/Go/Java** (Go matches Multica, Python matches our glue); provides durable workflows + durable queues + cron + **human-in-the-loop `recv`/`send`**; **workflow state commits in the SAME Postgres txn as app data** (atomic w/ ledger); step-level checkpoint→resume-after-crash. Users: Notion/Walmart/BMS/Supabase/Yutori. → **Adopted as the O32 durable-execution answer (D55)**, beating Cloudflare Workflows (now scoped to EDGE/watchdog only — Workers can't run DBOS) / Hatchet / Temporal because it's a **library over the Postgres we already run = lightest ops**. Added **"execution durability" as a distinct dimension to doc 08** (in-flight work resumes after crash) vs data durability (R2 archive+restic). DBOS wraps OUR orchestration code (job lifecycle steps, pollers, reflection/promotion jobs); agent runs stay captured+retryable; Multica claim-queue stays task queue; DBOS HITL maps onto T2 action-broker approvals. ⏳ verify DBOS Go-SDK maturity + license at build. Updated docs 08/11/14/18/20. **Plan v0.4.9.**

**v0.5.0:** operator asked the language question → new **doc 21 (Languages & Implementation Stack)**, D56. Component language map (verified): Hermes=**Python** (81.7%)+TS; Multica=**Go** backend (Chi/sqlc/pgx)+Next.js/TS frontend; Claude Code/OpenCode=**TS**, Codex=**Rust**; DBOS SDKs=Py/TS/Go/Java; DSPy/GEPA/Inspect/Mem0/Phoenix/Terminal-Bench=**Python**; promptfoo=TS; Cloudflare Workers=**TS** first-class; OTel Collector/tsnet=Go. **STRATEGY: Python PRIMARY** (all our built code — plugins, ledger, capture shims/harvesters/pollers, eval/comparison runner, reflection/medic, DBOS glue), **TypeScript at the EDGES** (Cloudflare watchdog/Workflows, Multica frontend, browser/Playwright, any UI), **Go where MULTICA forces it** (fork its backend + DBOS-Go option + tsnet), **Rust CONSUMED not authored** (Codex + fast CLI tools — no home-lab hot path justifies writing it). Rationale: bottleneck is LLM latency/orchestration-correctness NOT language perf → optimize ecosystem-first-classness + fewest-languages-a-solo-op-maintains; new language only enters w/ a forcing function. Provider SDKs + agent/eval ecosystem are Python/TS-first (Go/Java official-but-trailing, rest=bindings) — native-no-gateway means we use those first-class SDKs directly. Language is part of a job's SHAPE → feeds routing (a Go task vs Python task may route to different model×harness). **v0.5.1: O41 RESOLVED — DBOS all-Python** (its flagship/most-full-featured SDK; operator judged Go SDK less complete). Our **Python** job-orchestration layer owns durable execution; **Multica (Go) triggers/dispatches into it**; **NO new Go written** — Go confined to editing Multica's EXISTING backend (CDC tap, OTLP shim, pricing.go, custom runtime). This further minimizes the Go surface. Updated D55/O41/O32 + docs 08/18/21. **23 docs+appendix (00–21, 90); plan v0.5.1; all research closed; ready to build — observability spine first, Python-primary (TS at edges = Cloudflare/frontend/browser; Go only to edit Multica; Rust consumed not authored).**

**v0.6.0 (2026-07-19):** operator changed the durable-execution optimization target — from "lightest ops" to **"fully TEST durability/scalability/distribution + the implementation"** → **adopt Temporal (self-hosted) as the distributed durable-execution fabric + PROVING GROUND** (new **D57**, new **doc 22 `22-durable-execution-fabric.md`**). Rationale: Temporal's separated server↔poller↔persistence parts make failures *rehearsable* (kill/partition/drain drills); best test tooling (replay-CI catches nondeterminism, time-skipping test env, omes ⏳); the field's scale-grade engine (closes doc-20 gap 1 in kind). **D55 AMENDED not deleted: DBOS narrows to ledger-atomic in-process glue** (its unmatched property: workflow state + WorkRecord in ONE Postgres txn) + bake-off comparator; **new O42 consolidation gate at Phase-3 exit** (Temporal-everywhere / DBOS-everywhere / split). Key doc-22 specifics: server+UI+own `temporal-pg` on Studio compose (SQL visibility, NO Elasticsearch; **`numHistoryShards=512` — IMMUTABLE, set once**); Python SDK only (server = consumed Go containers, O41 extended); Temporal-worker processes are called **"pollers"** (avoid collision w/ fleet "worker"); **Temporal's run_id ≠ our run_id** (ours = `PlatformRunId` search attribute; theirs recorded as secondary IDs on WorkRecord); **payloads by reference ONLY** (~2MB/50MB limits; history = coordination state, NEVER the SoR — D44 pattern); cancellation = kill path (heartbeating activities); queue map `q.control`/`q.exec.<host>`/`q.exec.macos|linux`/`q.canary`; Schedules replace cron for platform jobs (overlap/catch-up = G3 semantics as config); `canary-heartbeat` Schedule feeds the Cloudflare dead-man. **Observability additions (new D58): Grafana Tempo joins the sinks for execution/infra traces** (LLM/agent traces STAY Langfuse+Phoenix — D5's one-second-LLM-pane rule explicitly untouched; collector gets a `traces/infra` pipeline routed by service.name); Temporal server metrics = **Alloy SCRAPE** (deliberate exception to D47 push-first — long-lived service) + SDK/poller metrics OTLP-push; official `temporalio/dashboards` Grafana dashboards; alert catalog (missed-Schedule=page, schedule-to-start p95 + slot saturation + **persistence latency = the home-lab scalability canary** = digest). Proving ground: Phase 2 = fabric stand-up + durability drills (kill -9 poller / restart server / kill temporal-pg → zero loss, measured MTTR); Phase 3 = distribution (multi-node queue, drain, tailnet-ACL partition) + scale (1k fan-out, ramp to the **measured Postgres-ceiling number = the fleet's scalability budget**); standing suites += replay-CI, fabric chaos (quarterly + monthly auto mini-chaos on q.canary only), fabric canary. Not adopted v1 (w/ triggers): Temporal Cloud, XDC, ES visibility, Nexus, Build-ID versioning (use `patched()`). Reconciled docs 03/05/08/10/14/18/20/21 + README (also fixed doc-10's stale duplicate gateway-era Phase-2 exit-test block + its stale `loki` exporter mention). **v0.6.1: operator set the O42 PRIOR — run BOTH engines, but expected endgame = DBOS-everywhere**: Temporal = the QUICK WIN (day-one Schedules/retries/HITL/UI, zero code) + the TESTABILITY scaffold while infra scales out = **the Multica pattern applied to execution** (starting point + benchmark, NOT presumed permanent; "Temporal is scaffolding we're glad to have needed; DBOS is the house we expect to live in"). Consequences in doc 22: **engine-portability rule** (business steps = engine-neutral Python; Temporal/DBOS = THIN BINDINGS; engine-specific features only w/ documented exit mapping, Schedules↔DBOS-cron, signals↔recv/send → O42 swap = bindings change not rewrite) + **paired DBOS drills at MEASURED scale** (crash-resume, HITL-survives-restart, fan-out on DBOS queues — the bar DBOS must clear); DBOS runs in production from day one (ledger-atomic paths) so the gate compares real evidence. D55/D57/O42 amended; docs 08/18/20/22 + README reconciled. **24 docs+appendix (00–22, 90); plan v0.6.1 (later committed in c1897d1 together with v0.7/v0.8).**

**v0.7.0 (2026-07-19):** operator thought exercise — *"prompting engine: improve prompting per instance (nuanced/specialized) while learning centralized things about prompting AND looping; verify layers separated, instances separate"* → gap-finding found the plan had the learning MACHINERY (GEPA/reflection/promotion pipeline, docs 13/19) + storage discipline (D4/P11) but **no knowledge architecture** (only `skills/shared/` vs `<worker>/` — no home for model-idiom or shape-level lessons; no promotion SCOPE semantics; looping not an artifact; no measured composed-prompt identity; SOUL.md/CLAUDE.md/AGENTS.md hand-maintained separately). → new **doc 23 (`23-prompting-engine.md`)** + **P16** + **D59**: (1) **layered prompt stack** L0 technique → L1 model idioms → L2 harness render templates → L3 shape playbooks → L4 worker specialization (`profiles/<worker>/soul.frag.md`) → L5 mode/job overlay, in `prompt-layers/` in the distro; composer in `fleet-sync` renders per (worker,harness,mode) — **SOUL.md/CLAUDE.md/AGENTS.md are RENDER TARGETS from one library**; (2) **measured `prompt_stack_hash`** in WorkRecord (render manifest, not testimony) → scoreboard ranks prompt versions like models; incident blast-radius = one ledger query; (3) **loop policies** first-class `<shape>@<model>.yaml` (iterations/reflect-cadence/fan-out/retry/compaction/stop/budget-reflex) rendered into config AND prompt; (4) **learning ladder**: lessons classify to NARROWEST evidence-supported scope (worker→shape→model→fleet), widening eval bars + canary breadth; **extraction rule: L4 never copied upward — librarian extracts domain-stripped technique w/ run_id provenance**; (5) separation invariants: no mutable shared prompt state, artifacts-only across minds (D31 extended), optimizer runs centrally on exhaust, renders node-local from pinned releases. New O43 (layer format: presumed markdown+YAML+our composer vs DSPy/POML ⏳) + O44 (loop-policy schema + scope ratification). Reconciled 00 (P16+glossary+doc-map, also added missing doc-22 map row)/02/05/06/10/13/18/19/22/README. **25 docs+appendix (00–23, 90); plan v0.7.0; still uncommitted.**

**v0.8.0 (2026-07-19):** operator thought exercise — *"planning agents + general planning for implementation/design/architecture: centralized? per-profile? in skills? what did we miss?"* → **planning = FOUR capabilities**, plan covered only #1 implicitly: (1) **in-run planning** → [23] loop policies/playbooks/harness plan modes — no ceremony; (2) **plans as governed git artifacts** (`plans/<plan_id>/`: plan.md + per-plan D-rows + tasks.yaml + retro.md; mandatory **`context_read` provenance** ("only plan over context explicitly read"); lifecycle draft→review→approved→executing→done/abandoned→retro'd; **consequence-tiered review — a consequential plan is a BATCHED INTENT**, campaign-sibling of the broker gate, never bypasses per-action broker gates); (3) **`planner`/`architect` = the PROSPECTIVE 4th fleet-intelligence archetype** (completes reflector/medic/librarian retrospective trio; read-only context; **propose-not-execute** mirroring medic's propose-not-patch; **planner ≠ executor by default** for consequential plans — fresh eyes + routing win: plan on strong models, execute per-task); (4) **platform meta-planning: the hermes-plan repo format IS the plan-work output contract** (docs + D-rows + revisit triggers + research streams + changelog); Phase-4 `platform-planner` proposes plan diffs through review, operator stays approver (P14). **Boundary rule**: plan outlives the run or crosses workers → artifact under governance; within-run scratch → loop policy. **Honesty loop = P2 extended to intent**: execution measured vs declared plan; replans = recorded events; **silent divergence alerts like an unregistered action**; retros grade plan-vs-outcome → ladder + estimate-calibration scoreboard. `plan_ref` in WorkRecord → ledger plan catalog; `plan-run` Temporal workflows (child per phase, HITL gates between phases). New **doc 24 (`24-planning-layer.md`)** + **D60** + O45 (plan home/format) + O46 (separation thresholds). Reconciled 00/06/13/18/22/23/README. **26 docs+appendix (00–24, 90); plan v0.8.0; committed+pushed as `c1897d1` (one commit for v0.6.0→v0.8.0, matching repo precedent of bundled version commits).**

**v0.9.0 (2026-07-19):** operator flagged **OpenWiki + Google OKF** → live-verified (WebSearch/WebFetch: spec + Google Cloud blog + LangChain post). **OKF v0.1 = Google Cloud's Open Knowledge Format, published 2026-06-12** (`GoogleCloudPlatform/knowledge-catalog`, okf/SPEC.md): vendor-neutral typed-markdown knowledge bundles — dir tree of .md w/ YAML frontmatter; ONE required field `type` (no registry, tolerate unknown); recommended title/description/resource/tags/timestamp; reserved `index.md` (progressive disclosure)+`log.md` (changelog); concept ID = path minus .md; consumers MUST tolerate broken links; **arbitrary extension keys allowed+preserved** (→ our `scope`/`provenance: run_ids`/`review`/`verified` ride there); formalizes Karpathy's LLM-wiki pattern; git history = attribution. **OpenWiki = LangChain OSS CLI** (`npm i -g openwiki`) generating/maintaining codebase wikis; 0.2 emits OKF; agents consume via AGENTS.md/CLAUDE.md pointers; typed **deterministic tag/type search beats agentic grepping** (token win). **KEY DISTINCTION: AGENTS.md-class = INSTRUCTIONS (doc 23 render targets); OKF = KNOWLEDGE the instructions point at** — "instructions point, knowledge sits behind the pointer." → new **doc 25 (`25-knowledge-plane.md`)** + **D61**: completes the knowledge taxonomy (instructions[23]/skills/memory[13]/**knowledge[25]**); `fleet-knowledge/` OKF bundle in git (class G/B), P16 scope model (`platform/` `repos/` `shapes/` `models/providers/` + **worker-private `workers/<w>/`** never cross-linked; generalization = librarian extraction via scope ladder); consumed via fleet-sync static links + **knowledge MCP server** (P9 — one path for Hermes+CC+Codex+OpenCode; deterministic-first, O48); produced by OpenWiki scheduled jobs (⏳ vet per taps policy) + librarian (facts→25, behavior→23, procedures→skills) + reflection corrections (**P2 on reference knowledge: wiki = declared world-model, reconciliation keeps it honest**) + plan retros + research; freshness (`verified`-age) monitored; retrieval logged per run_id → knowledge graded like skills. **Format yes, platform no** (Google Knowledge Catalog SaaS not adopted, D46). New O47 (bundle home; hermes-plan itself OKF? which repos first) + O48 (server design). ⏳ spec is 3 weeks old — pattern survives drift. Reconciled 00/05/08/13/23/24/README. **27 docs+appendix (00–25, 90); plan v0.9.0; committed+pushed as `72a3b42` (v0.6–v0.8 = c1897d1).**

**v0.10.0 (2026-07-19):** operator exercise — *"integrating with APIs + centralized vs profile-based memory + Hermes has built-in learning/reflection but we want to TEST our own learning/reflection systems"* → three landings. (1) **doc 26 (`26-external-integrations.md`) + D62**: P4 extended to ALL external APIs — **reads via central data services (our MCP servers), writes via broker only; WORKERS HOLD CAPABILITY, NEVER THIRD-PARTY CREDENTIALS** (creds live in owning service/broker from vault — injected worker can call a tool but can't exfiltrate a key it never had); git **Integration Registry** (one YAML/integration: owner service, auth custody+rotation, **fleet+per-worker QUOTA BUDGETS w/ admission control at owning service** — the API sibling of spend caps, cache TTL+serve-stale-on-error, OKF knowledge ref reconciled vs observed behavior, health probe, deprecation watch); model-provider APIs stay doc-04 territory; quarterly registry review. (2) **doc 13 → v0.3 + D63 memory architecture**: "**centralize the SERVICE, isolate the DATA, promote by evidence**" — one Mem0/Supermemory on Studio (C) w/ server-side per-worker namespaces; classes = working/episodic/semantic/operator/fleet × lifecycle; **consolidation ladder**: episodic→semantic per worker, extraction sorts BY KIND (facts→25/OKF, behavior→23/prompt-layers, procedures→skills; only memory-shaped remainder → small `fleet` namespace, ladder-only, read-as-untrusted); "**memory is where lessons are BORN, not where they LIVE**"; forgetting live memory = safe (P13 archive lossless) — decay is a quality lever. (3) **D64 learning bake-off**: Hermes-native (`skill_manage`+`/learn`+self-evolution) = **champion**, our reflection pipeline = **challenger**, control = off; **GATES CONSTANT, PROPOSER VARIES** (all treatments through same promotion pipeline); **ONE MIND PER TREATMENT** (auto-authoring disabled under B; cloned profiles per shape; fleet namespace frozen during windows); metric = **learning velocity** (scoreboard slope/week/$) + acceptance + post-promotion regression + time-to-first-lesson, judged by doc-19 paired stats; sharpens D30 → adopt-AS-CHAMPION; **O21 folded into new O50** (per-capability consolidation gate — plausible: Hermes wins skill authoring, ours wins prompt/loop tuning, hybrid stands). New O49/O50. Reconciled 00/05/19/25/README. **v0.10.1 — output styles** (operator asked; live-verified): **Claude Code output styles alive-but-turbulent** (deprecated 2.0.30 → REVERSED 4 days later; `/output-style` cmd removed v2.1.91 → `outputStyle` setting; md+frontmatter files, project-nested closest-wins, plugin-distributable; **CUSTOM STYLES DROP BUILT-IN SWE INSTRUCTIONS unless `keep-coding-instructions: true`** — fleet clamp D36-class; **subagents UNAFFECTED by styles** — own system prompts, forks inherit); **Codex** = coarse `personality` (none|friendly|pragmatic, supportsPersonality models) + `model_verbosity`/`model_reasoning_summary`, fine voice via AGENTS.md/model_instructions_file; **Kimi CLI** = Jinja2 system-prompt templating, no named style feature ⏳; **Claude API has NO styles feature** (corrected operator's guess — client-side prompt edits; app Styles = D48 blind spot, not a render target). → **D65** in doc 23 v0.2: style fragments live IN the existing layers, composer renders per-harness native slots; **styles don't survive delegation** → depth-critical form rules go in propagating targets or output contracts (18 G5); **style ≠ contract** (voice preference vs validity gate; divergence → measured output_style dimension); style versions ride prompt_stack_hash → scoreboard-rankable. Reconciled 06/18/README. **v0.11.0 — persona architecture** (operator asked re: personas per agent/subagent + capture + overlooked layers; live-verified): **persona ≠ specialization ≠ style** — L4 SPLIT: `persona.md` (WHO — **operator/T2-governed, identity-adjacent, NEVER ladder-writable**) + `specialization.md` (what-it-knows, ladder-learned). **Persona tree captured at every depth** (Q2 extension): render manifest enumerates EVERY mind in a worker's tree under prompt_stack_hash — main render + **subagent role defs as render targets** (`roles/` library → `.claude/agents/<role>.md` = persona body + policy frontmatter: least-priv `tools`, per-role `model` routing, `maxTurns` loop bound, `skills` preload, `memory`) + **known holes**. VERIFIED per-level facts: **Hermes SOUL.md = first-in-system-prompt; delegation runs `skip_context_files` → subagents get BUILT-IN DEFAULT IDENTITY (persona HOLE at depth ≥1**, upstream issue #476 per-mode personas ⏳); **Hermes Profile Distributions** (2026: whole agent as installable git repo — native D53 packaging ⏳ O51); **Multica injects teammate identity block ABOVE everything** ("You are {agent} (ID:…)" + persona instructions via buildMetaSkillContent meta-skill) → author from OUR fragments, mirror don't hand-diverge (D44); **CC subagents**: own system prompt (md body) + CLAUDE.md + git status + preloaded skills, NOT parent prompt/style/auto-memory/history (fork inherits all; Explore/Plan also skip CLAUDE.md); **subagents CAN nest** (Agent tool inherited); frontmatter incl. **`memory: user|project|local` (per-ROLE persistent memory — drift/poisoning surface, fleet default OFF, sub-namespaced under worker)**; hooks receive `name` as **`agent_type`** (per-depth persona capture FREE); transcripts at `subagents/agent-{id}.jsonl`; **`--append-subagent-system-prompt` (≥2.1.205) pierces ALL nesting depths** = fleet non-negotiables channel in headless. Rules: **inherit-and-cannot-exceed for minds** (P12 sibling; render-time persona LINT + frontmatter allowlists); `persona_adherence` + `agent_type`-at-depth join WorkRecord (06); one persona many surfaces (chat bot/Multica card/ledger from same fragment). **D66** in doc 23 v0.3 + new **O51** (roles v1, lint rules, memory grants, Profile-Distributions adopt-fit, Multica config render). Reconciled 02/06/13/README. **28 docs+appendix (00–26, 90); plan v0.11.0; v0.10.0→v0.11.0 uncommitted.**

**v0.12.0 (2026-07-19):** operator requested **full technical audit** → **doc 27 (`27-technical-audit.md`)**: repo-wide grep sweep + 2 parallel doc-audit subagents (01/04/09/12 + 14–17) + live toolchain verify. **Three gap classes.** (1) **GUARANTEES ASSERTED, NEVER EXERCISED** (the headline): scrub-before-WORM untested (redaction regression = IMMORTAL leak under Object-Lock Compliance) → **scrub-canary honeytoken standing test** + gitleaks; "never lose, verified" archive NEVER READ BACK → daily sampled read-back + monthly blob-replay rebuild; loss-prevention machinery unobserved (file_storage buffer depth unalerted — "no loss until buffer fills"; rclone mirror lag; **CDC slot lag can fill Multica's WAL = takes down the SOURCE**; poller staleness) → spine self-SLIs in doc 03; **clock sync = unstated precondition** of PID+timestamp correlation (zero NTP/chrony mentions repo-wide) → O53; **nothing watches the watchdog** (CF cron/account lapse = silent) → mutual watch via existing Healthchecks; rogue-action detector + recorder-down alert (enforceRecorder=false fail-open) get planted drills. (2) **BUILD SUBSTRATE UNDEFINED** (CI jobs specified in 10/19/22, runner/toolchain NOWHERE) → **D67**: GitHub Actions + self-hosted runner on canary mini IN A SLOT; **uv+ruff+ty** (⏳ beta; **Astral joined OpenAI 2026-03** — Tart-class stewardship watch) + pytest + **hypothesis** (property-test reconciliation/dedup) + **schemathesis**; pre-commit+gitleaks; **trivy**+**renovate**; **alembic** + WorkRecord schema-evolution policy; **locust**; Grafana provisioning-as-code; signing tool = O54. (3) **OPS HYGIENE** → O52 (per-store retention table + disk/growth alerts + platform-cost panel — model spend was exhaustive, PLATFORM cost unobserved), alert escalation/silencing rider, Fly-box patching+nightly ansible-check drift, Multica fork CI + schema-drift detector, ingress negative probes, Sysmon named for winbook (iff runner), **O55 starter SLOs** (coverage ≥99%/wk, ingest p95≤5min, |cost drift|≤2%/day, skew ≤250ms ⏳). **7 new standing tests in doc 10**; doc 03 gains spine SLIs + explicit NO-SAMPLING statement (P13); doc 21 gains toolchain section. Positives: drill culture ahead of most shops; pip-audit/osv already present; blind-spot register right. New D67 + O52–O55. **29 docs+appendix (00–27, 90); plan v0.12.0; committed+pushed as `40d342a` (v0.10.0→v0.12.0; prior commits: c1897d1 = v0.6–v0.8, 72a3b42 = v0.9).**

**v0.12.1 (2026-07-19) — reconciliation begins:** **O2 RESOLVED → D68: secrets manager = Infisical self-hosted** (operator confirmed NO existing 1Password — killed its only edge). Live-verified July 2026: free tier = **5 identities/3 projects; identities count humans AND machines; OIDC/SAML paywalled** (~$18/identity Pro) → **identity-frugal design**: ~4 machine identities (operator, provisioning/fleet-sync, broker-reserve, dataservice-reserve); per-node secrets RENDERED at provision (doc 05 pattern — nodes hold no vault clients); scale path = free Google/GitHub SSO → Pro when team forms → **OpenBao fallback** (MPL Vault fork, no caps, unseal-ceremony ops) if caps/paywalls bite; **BREAK-GLASS KIT mandatory** (age-encrypted offline seed: Infisical admin + provider admin keys + TS auth + restic/B2 — two locations; the vault can't be the only path to rebuilding its own host). **First plan artifact under doc-24 contract: `plans/plan_2026q3_phase0/`** (plan.md w/ front-matter+context_read+phases A–F+acceptance criteria = doc-10 exit tests + audit riders; plan-local decisions.md P0-D1..D5; tasks.yaml w/ deps+shapes) — **O45 PARTIALLY RESOLVED by doing** (platform plans live in plans/ in hermes-plan; worker/build-plan home → Phase 2). Phase-0 plan status: `review` — operator approval flips to `approved`. Prioritization given: Track 1 ride-alongs (O53 chrony, D67 hygiene-at-creation w/ planted-secret test, O47a knowledge seeding, A5 cross-watch), Track 2 Phase-1-gating design (O23 watchdog schema, O30 analytics row, O43 composer), Track 3 = phase-due cadence. Reconciled 05/11/README. **Plan v0.12.1; v0.12.1 uncommitted (v0.12.0 committed as 40d342a).**

**v0.13.0 (2026-07-19):** operator elevated their practice triad to principle — "dogfooding + improvement + compartmentalization so we can be nimble; where there is GitHub we should be able to work with jj/GitLab/etc" → **P17** (doc 00) + **D69** + **SUBSTITUTION MAP register in doc 05**: every product dependency classified **SBD** (swappable-by-design: substitute named, seam thin, exit rehearsable) or **DC** (deliberately-coupled: conscious bet + documented exit + trigger); unclassified = audit finding; **every future D-row must state its Substitution-Map row**; quarterly review walks the table. Key rows: forge=SBD (git protocol = truth, forge = mirror; NO load-bearing GitHub-exclusive features; subs = GitLab CE/Forgejo/Gitea, Radicle; **jj (Jujutsu) sanctioned as colocated client on same repos**; "lore" NOT identified as a 2026 forge — possibly kernel-style email-patch flow which git supports natively, operator to clarify ⏳); **CI=SBD via D67 AMENDMENT: pipeline logic in repo scripts, workflow YAML = thin trigger adapter** (Forgejo/Gitea Actions are Actions-compatible; self-hosted runner moves with us) — D67's original GitHub-Actions phrasing was the first violation the principle caught; Temporal/Multica/R2/sinks/Infisical/OKF/providers/runtimes/watchdog-host = SBD (existing decisions re-classed); **Tailscale/macOS/Postgres = DC** w/ exits (WireGuard+rebuilt-features / Linux-parity-table / pg_dump+SQL). Phase-0 task b2 updated (CI thin-adapter from day one). Reconciled 00/05/21/plans/README. **Plan v0.13.0; v0.12.1+v0.13.0 uncommitted.**

**v0.13.1 (2026-07-19):** **O30 RESOLVED → D70: the unified analytics schema** (doc 17 v0.2). Structural insight: **TWO GRAINS** — chat-app surface will NEVER carry run_id (provider analytics APIs = daily aggregates) → run-grain (= **WorkRecord EXTENDED**, O30's prediction held) + **usage-grain** table (date×provider×billing-identity×model→tokens/USD, deliberately not row-joinable); reconciliation = the GRAIN JOIN (Σ run-grain vs usage-grain per day×identity, O55 ≤2% tolerance). Three moves: (1) **harvester ADOPTS orphan sessions** as `trigger: human-direct` ad-hoc WorkRecords (synthetic run_id, measured from harvest) — "no silent work" covers uninstrumented work, run-grain stays ONE store (D44); (2) **per-field provenance** `value+source+is_estimate+candidates` — reconcile-per-field STORABLE, disagreement visible as data, Multica pricing.go excluded by write path; (3) **surface×mode×vantage dimensions** — P15 coverage = a GROUP BY. New metric: **DARK SPEND** (provider-seen spend joined to no run) = the measurable form of "account for all these systems", alerted. Storage thin: ledger Postgres + alembic (analytics/v0) + materialized views (v_runs, job/plan catalogs, scoreboard cells, coverage, dark-spend). **Unblocks ledger API v0 = Phase 1's first build.** Committed v0.12.1+v0.13.0 as `f3859b4` earlier this session. **Next queued: O23 (watchdog heartbeat protocol + DO registry schema), then O43 (composer format). Phase-0 plan still status: review (operator has NOT yet said "approved").** Plan v0.13.1; committed as `d7d0a44`.

**v0.13.2 (2026-07-19):** **O23 RESOLVED → D71: heartbeat protocol + watchdog registry** (doc 14), closing audit **A5** (unwatched watchdog). Payload v1 `{v, instance_id, class, seq, sent_at, status, note?}` — **seq = restart detection; sent_at = free per-node CLOCK-SKEW signal from off-fleet vantage (O53 synergy)**; per-class cadence/miss: runner+cloud 60s→late@2→dark@5min, spine 60s→dark@3min, fabric-canary 5min→dark@15min, per-job SLA rows share registry; **AGGREGATION: home-lab-dark = ONE page not N; fleet-dark = one page**; flap damping + recovery notices + `silence_until` maintenance windows (closes A12 for dead-man path); **registry = DO SQLite, DECLARATIVE registration from fleet.yaml via signed sync — unregistered/invalid beater = SECURITY page**; **push-dead+pull-alive = "egress broken"** (own runbook) vs dark; **alert channel = hosted ntfy.sh private topic primary (⏳ or Pushover) + hosted Healthchecks secondary — Email Workers BETA kept OFF critical path**; **cross-watch BOTH directions**: watchdog→Healthchecks every sweep, Kuma→watchdog /status. Auth = per-instance bearer from vault (D68), rotated. Build-time: TS Worker/DO. **Next queued: O43 (composer format), then O54 (signing).** Plan v0.13.2; committed as `518dd9a`.

**v0.13.3 (2026-07-19):** **O43 RESOLVED → D72: prompt-layer format & composer** (doc 23). **Layers = md + YAML frontmatter** (`layer` id=path OKF-style, `scope`, `targets` [system|claude-md|agents-md|output-style|agent-def:<role>|config], `order`, `optimizable`; **content layers template-free** — humans + optimizers edit prose); **Jinja2 ONLY in L2 harness templates**; **composer = hermetic `fleet-sync render`**: pure fn (git tree, worker, harness, mode) → artifacts + render-manifest.json (every layer@commit, file+sha256, D66 persona tree incl. holes) → prompt_stack_hash; NO timestamps/env inputs (same tree = same hash → one-variable experiments + incident attribution real); **LINT INSIDE THE RENDER, unskippable** (persona lint, D65 keep-coding-instructions clamp, requires/targets/scope violations — worker-layer referenced outside its worker FAILS render). Verified July 2026: no external format displaces md+frontmatter — **dotprompt rejected** (executable-per-prompt w/ model config = WRONG GRAIN; our model binding happens at render via stack), **POML rejected** (HTML-ish, zero pull), **DSPy = optimizer boundary ONLY** (`optimizable: true` → GEPA loads; proposals return as md diffs via ladder). Rationale: overdetermined — every render target/neighbor speaks md+frontmatter (SOUL/agents/styles/skills/AGENTS.md/OKF/Copilot prompt files); **review is the governance surface → hand-editability wins**. **Next queued: O54 (signing mechanics), then O33 (output-contract format).** Plan v0.13.3.

**v0.13.4 (2026-07-19) — pre-handoff review of Phases 0–1** (operator: "handed off to our review team — catch all things possible, define anywhere unclear"; doc 10 → v0.4). **CRITICAL FIX: WORM sequencing** — archive bucket starts versioned-but-NOT-locked; **Object Lock Compliance only after scrub-canary green ×3 consecutive** (locking a leaking pipeline = immortal leak; audit A1 had the test but NOT the ordering). Phase 1 build list caught up: D70 ledger schema + alembic 001, D72 composer first render (hash from first captured run), **harvester + pollers added** (doc 17 said Phase 1, doc 10 had omitted — dark-spend comes alive), D71 watchdog spec, **spine SLIs + golden-replay gate live same day as Collector**. Exit tests added: scrub-canary ×3 (gates the lock), archive read-back v0, **cross-surface coverage test** (17 defined it, 10 never listed it; chat-app leg conditional on O31), cross-watch drill in dead-man test (kill watchdog → Healthchecks pages), buffer-depth SLI observed during kill-the-spine, prompt_stack_hash+provenance assertions. NEW up-front section in doc 10: **phase gates** (each phase = plan artifact + retro; next plan authored at close), **external-prerequisites table** (Phase 0: Tailscale/forge/B2-R2 account/age keypair; Phase 1: Anthropic workspace+key, **active Claude subscription login for estimated-mode reference worker**, Cloudflare, ntfy.sh+Healthchecks; deferred: Fly/org-tier), **minimum hardware = Studio + 1 mini through Phase 1**, rough estimates (P0 ≈ 1 pt-wk, P1 ≈ 2–4 pt-wks). Phase-0 section reconciled w/ plan artifact; tasks.yaml gained missing **d0 = B2/R2 account+bucket+keys task**. Plan v0.13.4; **committed+pushed as `d0785f4` — Phases 0–1 are handoff-ready for the review team.** Recent commit chain: c1897d1 (v0.6–0.8), 72a3b42 (v0.9), 40d342a (v0.10–0.12), f3859b4 (v0.12.1–0.13.0), d7d0a44 (v0.13.1), 518dd9a (v0.13.2), d0785f4 (v0.13.3–0.13.4). **Open decision queue after this: O54 (signing mechanics), O33 (output-contract format), O29 (Multica placement), O31 (org tier), O52/O55 finalization. Phase-0 plan still `status: review` — operator approval starts execution.**

**v0.13.5 (2026-07-19):** **O54 RESOLVED → D73: signing = OpenSSH signatures, ONE mechanism fleet-wide** (doc 09 "Signing mechanics" section; closes audit item 14). Git SSH signing (commits/tags) + **`ssh-keygen -Y sign/verify` for blobs** (D71 fleet.yaml→watchdog sync payload). **Two channel-scoped roles in `allowed_signers`**: operator key signs anything; **vault-held fleet-release key signs stable/canary release TAGS ONLY** (used by release job after promotion gates) → automated-key compromise bounded to fake releases (still face canary+circuit-breaker), never arbitrary trust. **Verify-at-consume** everywhere "unsigned artifacts don't load": fleet-sync `git verify-tag` pre-install (distro/layers/knowledge/plans all = git), tap path (`hermes skills update` wrapped), watchdog Worker (embedded pubkeys), CI signature checks. **Root of trust = bootstrap script's commit-hash pin + operator pubkey fingerprint** (two physically-controlled things); allowed_signers ships via provisioning chain; rotation = operator-signed diffs; fleet-release key recovers via vault export, operator key via break-glass kit. **Rejected as named SBD substitutes (D69 rule): sigstore/cosign** (external trust infra + forge-coupled keyless OIDC = P17 friction, no v1 benefit), **minisign** (redundant vs ssh-keygen -Y); container images stay digest-pinned. **Next queued: O33 (output-contract format), then O29 (Multica placement) + O31 (org tier).** Plan v0.13.5; uncommitted.

**v0.13.6 (2026-07-19):** operator clarified: **"right now it is just us — me and then other agents; robust but single person approves."** → **D74 + APPROVAL MATRIX in doc 09 + P14 sharpened**: **exactly ONE human approver by design**; "review team" = REVIEWER AGENTS + operator; agent gates (staging/scan/eval/judges/canary) do heavy reading; human gets **one batched high-signal queue** (command center), every item carries evidence (run_ids/scores/diffs — ratification = a read not an investigation); **pending approvals WAIT or EXPIRE-DENY, never default-approve** (fail-closed). Matrix: ALWAYS-HUMAN = T2+ actions (expire-deny), plan/phase gates, shape+model+fleet promotions, persona/activation changes, canary→stable one-click, ALL trust-root changes (keys/allowed_signers/break-glass — no automation touches). AUTO-POST-GATES = canary releases (fleet-release key), dep bumps to canary, worker-local promotions from Phase 4 — **audited weekly; dial turns back on findings**. Robustness w/o four-eyes = 3 honest compensating controls: unskippable gates + reversibility (canary/circuit-breaker/git) + ledger reconciliation catching approval abuse as drift. Re-cut matrix when second human joins (D68/SSO trigger). Plan v0.13.6.

**v0.13.7 (2026-07-19):** **O33 RESOLVED → D75: output-contract format** (doc 18 G5). Live-verified: **Anthropic native structured outputs GA** — `output_config.format` (was output_format; beta headers dropped) grammar-compiles the JSON Schema so violating tokens are IMPOSSIBLE; `strict: true` tool use; **required subset = additionalProperties:false + all-props-required**. → **Structured contracts = pydantic-authored JSON Schema in exactly that strict subset** (pydantic = source of truth in distro `schemas/`, emitted JSON Schema committed beside, CI-synced): ONE contract = constrained generation where supported + **always-on post-hoc validation** (gate sovereign, never depends on provider feature; CLI-mediated runs hit same validator). **Prose = promptfoo `llm-rubric` YAML** (O35 tool; judge returns {reason, score 0-1, pass} → WorkRecord; **one file = runtime gate + doc-19 evals**). **OPERATOR SET (asked via AskUserQuestion): judge = per-contract `judge:` knob, DEFAULT FRONTIER** ("very configurable, leaning frontier except small/less-consequential") — judge strength scales w/ consequence like D74; judge calls = captured+costed runs; weekly operator spot-check calibration; O39 distilled judge = future cheap tier. **Repair = 1 bounded recorded attempt** (operator confirmed; `durable.repair: 0-2`; ~95% first-repair fix verified; `repair_attempts` → review dimensions; **first-pass vs post-repair validity SEPARATE on scoreboard** — no laundering model weakness). Failures → retries → dead-letter/digest (D74). Contract evolution: additive-minor/version-bump; plans' acceptance criteria reuse both formats. **Next queued: O29 (Multica placement) + O31 (org tier).** Plan v0.13.7; **committed+pushed as `39d5fb6`**. Session commit chain: c1897d1 (v0.6–0.8) → 72a3b42 (v0.9) → 40d342a (v0.10–0.12) → f3859b4 (v0.12.1–0.13.0) → d7d0a44 (v0.13.1) → 518dd9a (v0.13.2) → d0785f4 (v0.13.3–0.13.4) → 39d5fb6 (v0.13.5–0.13.7). **Remaining open queue: O29+O31 (placement/org-tier pair, likely wants operator input on cost), then O34 (routing static-vs-learned), O38 (activation schedule), O44 (loop-policy schema), O46 (planner thresholds), O49 (registry home), O50 (bake-off protocol), O52/O55 finalization, O47/O48/O51 halves, plus Phase-2+ bake-offs (O14/O19/O20/O22/O24/O39/O40/O42). Phase-0 plan STILL `status: review` — operator approval starts execution.**

**v0.13.8 (2026-07-19):** **O29+O31 RESOLVED → D76/D77, configurability-first** (operator directive: "focus on configurability so we can optimize and be nimble"). **D76 placement**: Multica on **Studio now as a `fleet.yaml command_center.host` knob**; addressing placement-independent (tailnet name `multica.<tailnet>`, daemons connect outbound); **migration runbook drilled Phase 3: Studio→Fly→Studio round-trip** (P17 exit-rehearsal made literal). **LOAD-BEARING INSIGHT: relocating Multica alone = availability THEATER** — the broker (the actual approval gate) is on the Studio, so approvals pause in a home outage regardless → the real Phase-3 question = the whole **control-availability unit** (Multica+broker+approval path) on its own machine (verified Fly ≈$10–15/mo dedicated; separate from the worker box per P14). **D77 chat-app**: verified **Analytics API = ENTERPRISE-tier only** (Team $25–30/seat, 5-seat min ⏳ = dashboards, NOT the API) → **no org seat** (~$1500+/yr to watch one person's personal chat = wrong trade); real work → Claude Code (D48); personal chat = **out-of-scope-by-choice + EXPLICITLY ANNOTATED on the dark-spend view** (the metric never silently lies); poller ships **org-ready but dormant** (`org_analytics: {enabled: false}`) — team-forms/tier-change/app-telemetry-export flips it with ZERO build. Docs 14/17 reconciled. **PRE-PHASE-2 DECISION QUEUE NOW CLEAR** — remaining O-rows = Phase-2+ bake-offs/finalizations (O14/O19/O20/O22/O24/O34/O38/O39/O40/O42/O44/O46–O52/O55) or await outside review. **Operator stance: NO phase approvals until outside review completes** (Phase-0 plan stays `review`). Plan v0.13.8; **committed+pushed as `eee631a`**. Full session chain ends: …39d5fb6 (v0.13.5–0.13.7) → eee631a (v0.13.8). **STATE FOR NEXT SESSION: pre-Phase-2 decision queue CLEAR (decisions through D77); Phases 0–1 handoff-ready; Phase-0 plan at `status: review` awaiting the operator's OUTSIDE review (do not push for approval); remaining O-rows are Phase-2+ bake-offs/finalizations; likely next work = outside-review feedback integration, or Phase-2+ items if operator directs.**

**v0.13.9 (2026-07-19):** **O52 RESOLVED + O55 provisional → D78: hot-store retention & lifecycle table in doc 08.** Live-verified mechanisms: **Langfuse v3 built-in nightly retention deletes across ClickHouse AND its blob bucket** (needs s3:DeleteObject) → **BOUNDARY SHARPENED in doc 16 story: Langfuse's working bucket ≠ the WORM archive** (separate bucket; Langfuse holds ZERO archive creds; with retention on, its bucket = a window; **the archive ALONE is the replayable SoR**); ⚠ **CH system-log tables (trace_log/text_log/…) ship with NO TTL** — the documented disk-filler, set TTLs explicitly; CH ≥25.7 lightweight-delete envs; **Phoenix `PHOENIX_DEFAULT_RETENTION_POLICY_DAYS`** (≥9.0, default 0=infinite); Loki compactor retention_enabled+retention_period; Tempo compactor block_retention; MinIO ILM; eslogger rotate-daily/delete-shipped+7d; Temporal namespace retention ⏳ confirm. **Starter windows (ALL rendered config knobs — configurability directive): Prometheus 90d, Loki 30d, Langfuse 90d, Phoenix 30d, Tempo 14d, MinIO cache ship+14d, NDJSON 7d local, restic 7/4/6 unchanged.** Disk alerts 75% digest/85% page + growth-rate anomaly (spine SLIs); **platform-economics panel** added to doc 03 dashboards (bills/growth/tier-triggers). **O55 starter SLOs adopted PROVISIONALLY — formal ratification at Phase-1 retro w/ a month of real data** (data-informed beats blessing 8 numbers cold). **Remaining queue = purely Phase-2+** (O14/O20/O22/O42/O50 bake-offs, O34 routing, O44/O46–O51 machinery halves) **or awaits outside review.** Plan v0.13.9; uncommitted.

**v0.13.10 (2026-07-19):** **O49+O34+O38 RESOLVED → D79/D80/D81** (three design decisions, no external research needed). **D79 (registry)**: `integrations/` dir in the distro, ONE YAML per integration, `schema: integration/v1` (doc-26 schema as-is); registry+enforcement land w/ first data service; day-one entries wait for Phase-2 worker pick. **D80 (routing)**: **static→learned is a GRADIENT not a choice** — affinity table = rendered config; `worker: auto` resolves via table day one (pinning allowed); "learning" = reflector proposes **table DIFFS through the existing ladder** (18 G7/19) once ≥N graded runs/shape (Phase 3) — no new learner system, never a migration. **D81 (activation)**: **git-scheduled (`activations/` YAML per slot: windows × profiles × overlays), fabric-executed (`profile-activation` workflow: suspend→snapshot→activate, durable HITL at D53/D74 gates), Multica-DISPLAYED** (never owns); hot-swap = same workflow on demand; Autopilots stay job triggers. Docs 02/18/26 reconciled. **Queue now: O44 (loop-policy schema), O46 (planner thresholds), O51 (roles v1) = Phase-2 machinery halves designable next; then drill-gated bake-offs (O14/O20/O22/O42/O50) + outside-review integration.** Plan v0.13.10; **committed+pushed as `ac015ba`**. **STATE: decisions through D81; the desk-designable queue is down to O44 (loop-policy schema) / O46 (planner thresholds) / O51 (roles v1 + persona lint) — everything else is drill-gated (O14/O20/O22/O42/O50), trigger-gated (O3/O5/O6/O8/O11/O15/O16/O19/O24/O39/O40/O47/O48), or awaits the operator's OUTSIDE REVIEW (Phase-0 plan stays `review`; do not push for approval).**

**v0.13.11 (2026-07-19):** **O44+O46+O51 RESOLVED → D82/D83/D84 — the LAST desk-designable items.** **D82 (loop-policy v1)**: fields SELF-DESCRIBE render targets (`render: config|prompt|both`) — composer stays generic; mechanical→config (iterations.max→HERMES_MAX_ITERATIONS/maxTurns/spawn flags; depth caps; compaction ⏳/harness), behavioral→prompt; shape×model scope, ladder-only specialization, D74 ratification. **D83 (planner thresholds)**: same-worker OK = T0 + single-worker + no broker actions; SPLIT MANDATORY (planner≠executor + reviewer-agent + operator) = any T1+ action OR >1 worker OR mind/platform-asset changes; fresh eyes STRUCTURAL (executor gets artifact, never planner's session); **plan approval NEVER delegates broker headroom**. **D84 (roles v1)**: reviewer(frontier)/researcher(cheap)/tester/harvester(cheap)/planner-probe, ALL `memory: off`; **persona-lint v1 = 5 forbidden-claim classes** (authority/credentials/cross-worker-reach/identity-mutation/gate-bypass; regex+phrase lists, FAIL-THE-RENDER per D72); **Profile Distributions RESEARCHED: NOT the install path** — installs TRACK DEFAULT BRANCH (pinning "planned not shipped"), UNSIGNED, no lockfile → violates D4/D73/P5; BUT their layout (SOUL/config/skills/cron/mcp + distribution_owned-vs-preserved-local split) = exactly our render-vs-local boundary → **fleet-sync sole installer; render emits distribution-compatible layout FREE (P17 seam: sharing a worker later = export not rework)**; third-party distros vendor-in only (clone→review→pin, never `hermes profile install` from live URLs on fleet nodes); watch upstream signing+lockfile roadmap → revisit trigger. **Multica agent configs = desired-state sync via PAT + drift-detect/revert** (git truth, Multica displays). Docs 02/23/24 reconciled. **QUEUE NOW FULLY EVIDENCE-GATED OR REVIEW-GATED — nothing desk-resolvable remains. Next movement = outside review feedback, Phase-0 execution (on operator approval), or Phase-2+ drills.** Plan v0.13.11; **committed+pushed as `5e8a35b` — THE DESK QUEUE IS EXHAUSTED.** Final session state: **decisions D1–D84, plan v0.13.11, docs 00–27 + appendix + plans/plan_2026q3_phase0 (status: review)**. Everything remaining is: (a) the operator's OUTSIDE REVIEW (integrate findings when they land), (b) Phase-0 execution on operator approval, (c) Phase-2+ drill-gated bake-offs (O14/O20/O22/O42/O50), (d) trigger-gated items. Full v0.13.x commit chain: f3859b4→d7d0a44→518dd9a→d0785f4→39d5fb6→eee631a→ac015ba→5e8a35b.

**PR #1 review (2026-07-20) — `codex/enterprise-control-hardening`, "Harden Hermes control and assurance plan" (GPT-5.6-authored, 47 files +2111/-426, plan→v0.14.0, new doc 28 + 7 schemas + policies + audit_plan.py + CI):** operator asked for thorough review + improve. Ran 4-track parallel review (principles/currency/machinery/scope). **VERDICT: high-quality PR — facts FLAWLESS (zero hallucinations: GPT-5.6 Sol/Terra/Luna, Kimi K3, ChatGPT Work, R2 Bucket-Locks, pgBackRest, tsnet all real/current), machinery REAL (7 schemas valid 2020-12, fixtures validate vs real jsonschema, audit enforces its claims, CI SHA-pinned). Control-lease (fail-closed admission w/o gateway) + per-slot workload identity = genuine sound contributions.** TWO problems, both FIXED as review commits on the branch (pushed; PR comment posted): **(1) Rev 1 `c27a77e` — never-lose inversion**: PR demoted D42 crown-jewel by putting 365d window on the SCRUBBED OPERATIONAL SPINE (no privacy justification). RESTORED D1-operational → `indefinite` (never-lose); only D2 raw content + D3 credentials bounded, D3 never archived. Added `indefinite` to data-policy schema + made it a MACHINE-ENFORCED invariant (audit fails if D1 not indefinite/D3 indefinite); extraction-before-expiry rule; reconciled orphaned "lossless-forever" prose (00/13/25/07); machinery nits (GOV-2 added to REQUIRED_CONTROLS, not_before rename, CTL-1 egress-guard→Phase-1 design + fully-mediated-network precondition). **(2) Rev 2 `82045c8` — Phase-0 over-rotation**: controls right, scope enterprise-shaped ahead of the spine (vs P1/D49 + not-HA/cattle non-goals). Added RAMP-WITH-CAPABILITY principle (doc 28 + P0-D15); DEFERRED out of Phase 0: commercial EDR→Phase3+ (detection=spine SLIs+eslogger+osquery+egress guard), cold-spare host→rehearsed restore-path (removed PR's own internal contradiction w/ doc-08 deferred warm-standby), ≥15min-UPS→graceful-shutdown, 4-zone VLAN→one boundary, all-infra-IaC→cloud+CI-drift, PITR-beyond-ledger/broker dropped (Temporal re-trigger, vault encrypted-export), pre-Phase-0 signed ritual→decisions.md row. **Phase 0: ~2-3wk+procurement/43 tasks → ~1-1.5 nimble wk, no procurement gate, every load-bearing control kept.** Audit green (33 artifacts), DAG intact. **STATE: PR #1 open on branch codex/enterprise-control-hardening with my 2 revisions pushed; NOT merged; Phase 0 still `review` awaiting operator sign-off. `main` still at 5e8a35b (v0.13.11) — the v0.14.0 hardening lives only on the PR branch until merged.** Decision framing to remember: hardening RAMPS with capability (cheap load-bearing now, heavy infra when a worker's authority/evidence earns it) — this is the counter to enterprise-ceremony-first.

**v0.14.0–v0.14.2 (2026-07-20, `30923de` — NOT yet merged: main is still `5e8a35b`/v0.13.11; the v0.14.x line lives on the `codex/enterprise-control-hardening` PR branch, with `codex/phase0-admission-kernel` stacked on it as active WIP, incl. my `a4e9edb` v0.14.3/D95 orchestration-seams commit — pushed as branch `d95-orchestration-seams`, **open as PR #2 stacked on PR #1** [base=codex/enterprise-control-hardening; merge #1 first or retarget to main after]):** PR #1 + revisions; then **v0.14.1 (`bdd7dfa` "Harden Hermes authority recovery and evidence contracts")** — D68/D73/D86/D90 amended + **D92–D94 added**: Phase 0 self-contained (cloud-only OpenTofu, imperative-adapter read-back, vault PITR before valuable secrets), later authority capability-gated (semantic ≤300s leases, unattended=T1+/headless, API workers brokered/credentialless), plan approval = routine-operator exact-hash OpenSSH attestation; and **v0.14.2 (`30923de` "Make Hermes source-control authority executable")** — D93 amended: static integration + landed-head GitHub workflows, `plan-authority` Environment, machine-readable configure/read-back tool, personal-Pro limitations stated honestly. **Repo now has an EXECUTABLE governance layer**: `scripts/audit_plan.py` (4,232-line locked semantic audit — passes: 35 md artifacts, 28 schemas, 68 fixtures), `scripts/github_controls.py` + 22 unit tests (pass), `governance/` (source-control-activation runbook + github-repository-controls.json), `policies/data/` (D0–D3 data classes), `fixtures/assurance/` (68 valid fixtures), `schemas/` (28 committed + 2 untracked WIP: phase-activation-receipt-v1, phase-task-result-v1 — referenced by plan.md approval_contract covers-list), `.github/workflows/` (plan-audit-integration + plan-audit-head). Audit deps venv: jsonschema/PyYAML/rfc3339-validator/rfc8785 (requirements-audit.txt, hash-locked). **D85–D94 ARE rows in 11-decisions.md's Decided table** (lines ~111–120: assurance rule, semantic leases, per-slot workload identity, data classes, registry taxonomy, Phase-0 self-containment, action-path classes, unattended-route defaults, plan-approval attestation, D2 fail-closed disposition); D2/D7a/D62 amended in place — but **D6 ("no model gateway/proxy") was NOT amended** despite docs 03/04/README now mandating a provider-access credential-injection gateway on API routes (D86 leases avoid inference-proxying, so spirit survives, letter misleads). Phase 0 plan status still `review`; plan.md approval_contract requires routine-operator OpenSSH signature over exact file hashes. README v0.14.2 top section authoritative; v0.13.11 paragraph kept below marked historical/non-authoritative.

**How to apply:** read `docs/11-decisions.md` first — decided-vs-open ledger (v0.2 changelog at bottom; open O13–O19 cover admin-API caps, microVM approach, memory backend, optimizer, OpenCode matrix). Next milestone: Phase 0 per `docs/10`. The user's prebid.org work is separate from this personal project. They favor comprehensive written docs, max-depth thinking, and infrastructure-first framing over domain specifics.
