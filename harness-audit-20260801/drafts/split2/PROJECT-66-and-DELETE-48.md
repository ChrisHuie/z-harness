# The 66 PROJECT files and the 48 DELETE files — Gate 0 extraction

Method: every one of the 114 files was read in full and rewritten with each proper noun
replaced by its category. **P** = what survived and is still actionable. **I** = what was
deleted. A P is only recorded if it would be true and useful in a repo never seen.

**Headline:** 64 of 66 PROJECT files carry a principle that should travel up. 37 of 48
DELETE files are unsafe to delete as-is. `~/.claude` is **not a git repo** (verified:
`/Users/quantum/.claude/.git` does not exist) — every deletion here is irreversible.

Destination shorthand: `CM` = `~/.claude/CLAUDE.md`; skill names follow PLAN.md §4
(`agent-dispatch`, `writing-for-github`, `refactor-safety`, `testing-ci`, `pr-review-method`,
`git-workflow`, `prebid-adcp`, plus a proposed `system-design` for the architecture Ps that
have no home in the 12-skill set).

---

# JOB 1 — the 66 PROJECT files

## 1A. salesagent, engineering (30 indexed + 3 unindexed)

| file | P (survives substitution) | I (deleted) | destination |
|---|---|---|---|
| `agentdb_persistent_schema_masks_fresh_db_failures.md` | A long-lived dev datastore accumulates every migration ever applied, so a test that depends on provisioning passes locally and fails on a from-scratch instance. **A green run against a persistent fixture is necessary, never sufficient** — any test moved into or added to a suite that touches schema is re-run against a fresh instance. Generalises: any stateful fixture that is never torn down converts a provisioning bug into a false green. | container name, port, table name, PR/commit | `testing-ci` |
| `feedback_a2a_skill_handlers_must_raise.md` | When the error envelope is assembled at **one** dispatch boundary, handlers must signal failure by **raising the typed error**, never by returning a hand-built error object: success is derived from the *shape* of what is returned, so a hand-built error lacking the marker field is classified as a success and ships a fake success artifact. Review rule: `return` on an error path inside a handler is a defect; grep the handler family for returned error shapes. | transport, class names, file paths | `pr-review-method` |
| `feedback_a2a_wire_tests_must_drive_on_message_send.md` | A test named *wire* / *boundary* / *end-to-end* that calls an internal handler directly verifies the handler, not the wire. Envelope and serialisation assembly sits **between** handler and transport; only driving the real entry point covers that layer. Remedy: drive the entry point and assert on the emitted artifact, **or rename the test so its name matches what it verifies**. Second P: when a refactor deletes the mechanism a rule cites, re-verify the rule — it may survive with a new reason. | method names, transport | `testing-ci` |
| `feedback_advisory_on_success_error_pattern.md` | **Enumerate allowlisted exception sites by an in-code marker grep, never from a count recorded in prose** — the recorded count drifted 4→6 and one cited site no longer qualified. Second P: the same syntactic form can be correct in one envelope and a defect in another; the discriminator is the **enclosing** envelope, not the call. | file list, marker string | `CM` (Memory §: carry the query, not the answer) + `testing-ci` |
| `feedback_changed_wire_field_dormant_grader_check.md` | **Diff-anchored review dispatch cannot see a test that *should* cover the change but lives in a file the diff never touched** — and cannot see that it is dormant (unbound / unregistered / auto-skipped). On any interface-field change, map field → every scenario that grades it across the whole corpus, then check each is actually executing. Auto-skip makes "passing" and "never ran" indistinguishable. | detector path, reviewer names, PR # | `pr-review-method` + `testing-ci` |
| `feedback_check_normalizer_before_input_surface_regression.md` | A "field is dropped / behaviour regressed" claim is a claim about the **whole request path**. **Probe the entry point, not the model** — middleware upstream may already rewrite or delete the field, making the change behaviour-neutral. A model-level probe answers a different question than the one asked. Second P: when a claim is falsified, **narrow it to the half the evidence supports** rather than withdrawing it entirely. | normaliser path, field names | `CM` Verification §; `pr-review-method` |
| `feedback_fitness_functions_pattern.md` | **Pick the enforcement channel by what the invariant depends on:** statically checkable from local files → an in-repo AST/config test; depends on external or supply-chain state, or needs a real subprocess → an off-the-shelf CI tool (a bespoke re-implementation is path-dependence); depends on admin scope or runtime measurement → a verify-script. Second P: use the **published** name for a pattern in durable artifacts so they stay legible outside the repo. | tool names, guard count | `testing-ci` (pairs with ROUTING-RULE Gate 1) |
| `feedback_mock_only_tests_dont_prove_wiring.md` | A test that patches the dispatcher verifies the mock's configuration, not the code path. Per new boundary path require **≥1 test per affected transport driving the real entry point**; mock only what is out of scope (auth), never the layer under test. Mechanisable: count new real-entry-point calls in the diff vs new error-path tests — zero on an error-path change is the gap, with an explicit carve-out for pure helper/schema tests. Third P: **cite a reference test by NAME, not line number** — line citations drift. | transport APIs, test path | `testing-ci` |
| `feedback_planning_doc_location.md` | (1) Plans default to chat; a file only when explicitly requested — the bar is "did they ask for a written plan", not "is this big enough". (2) **A notes directory can hold TRACKED content: run `git ls-files <dir>` before deleting anything in it.** `git status` shows only the untracked ones, so `ls` overstates what is safe to remove; deleting a tracked note is a real change that shows in the diff. (3) Session working notes stay local; at most one durable maintainer-facing doc per change. | dir paths, PR #s | `git-workflow` (2) + `CM` Output § (1,3) |
| `feedback_pr_base_current_main_push_origin.md` | With multiple remotes including forks, the PR target is **not inferable** from the branch — confirm once and record it. **The local default-branch ref is routinely stale**: fetch before rebasing or diffing against it, or the change silently bases on an old head. Pre-push scope check = `git diff --stat <base> HEAD` shows only intended files. | remote names, repo | `git-workflow` |
| `feedback_readiness_verdict_gate.md` | **A readiness verdict is never inherited and is stamped to (head SHA + pull time), never durable.** A *code* re-check is not a *review-state* re-check — different dimensions; re-pull review threads on the current head before any verdict. **A diff you yourself requested is still an unreviewed diff once it lands.** Plus: sibling-sweep every confirmed finding (you own the twin of your own finding); never mark a "this test grades X" claim as inferred — mutation-test it; hand-sweep new **test** code for duplication, because sub-threshold duplication is invisible to the duplication gate; a partial copy of a canonical map is its own defect class. | charter §, detector path, PR # | `pr-review-method` — highest-value single file in this silo |
| `feedback_run_e2e_locally_before_push.md` | **When CI fails twice on the same target with a different error each time, you are peeling layers of rot** — stop pushing speculative fixes and reproduce locally against the full stack; each red run on a public branch is visible. Second P: **any line-shifting edit can stale a per-line allowlist or baseline**, so run the full local gate after any production line shift even when the logic is unrelated. | commands, file paths | `testing-ci` |
| `feedback_run_full_suite_before_every_push.md` | Six travel-worthy Ps: (1) **the convenience "quality" target is a SUBSET of the suite** — enumerate exactly which suites it does not run before treating green as suite-green. (2) Whole-tree invariant checks mean a one-file edit can fail a test in an unrelated file — **per-file test selection structurally cannot catch them**. (3) **Network-gated tests silently skip offline, so a local run collects fewer tests than CI — the lower collected count is the tell.** (4) A dependency/CVE audit may run as a separate non-test gate that **no** test invocation surfaces; and a scanner can fail with a *tooling crash* rather than a finding — re-run before owning it. (5) **CI is not a superset of local** — it may skip suites the local runner runs, so a green check rollup is not evidence the suite passes. (6) Auto-skip on missing step definitions means a written scenario gates nothing. | targets, suite paths, PR #s | `testing-ci` (this file alone justifies a section) |
| `feedback_uow_detached_after_exit.md` | An object obtained inside a managed scope (transaction, session, context manager, pool checkout, temp dir) **may become invalid at scope exit — lazily**, so the failure appears at first attribute access *outside*, not at exit. Keep reads inside the scope or copy the needed scalars out. **A mock returns a plain object with no lifecycle, so the test passes green while production takes the invalid path** — lifecycle bugs need a test against the real resource. Review rule: scan for attribute access on a scope-returned object after the dedent. | ORM/UoW names, file:line | `testing-ci` + `system-design` |
| `feedback_use_full_review_skill_not_handrolled_fanout.md` | **Reach for the packaged in-house skill rather than reconstructing it from its parts.** Hand-launching the same agent *types* reproduces the fan-out but drops the selection and consolidation discipline that lives in the wrapper, and can silently skip a lane — **a harness's value is in the parts that are not agents.** | skill name, PR # | `agent-dispatch` (sharper mechanism than CM's current cost-framing) |
| `feedback_valueerror_boundary_vs_internal.md` | **Split exception migration by AUDIENCE, not syntax:** an error that can escape to an external caller becomes a typed, wire-shaped error; a programmer-contract violation stays an untyped crash, because wire-shaping it **misreports a code bug as a caller fault**. The discriminator is the call graph — can this raise reach the boundary without an intervening handler? Safety net: a catch-all translator at the boundary so misclassification degrades rather than crashes. | class names, counts | `system-design` |
| `harness_error_wire_per_transport_mechanics.md` | (1) **A shared test harness offering one uniform entry per backend may capture the REAL wire for only some of them and SYNTHESIZE it for others** — a synthesised envelope pins content but not framing, so a serialiser that drops fields stays invisible. Verify per backend what the harness actually captures before trusting a uniform "test all transports" API. (2) `from X import name` binds in the **importing** module — patching the source misses every site that imported it; **patch every bind site and count them.** (3) **Assert on the parsed structure, not a substring of the stringified error** — the echoed request body can satisfy the substring. (4) A reviewer's "just use the uniform API everywhere" can rest on a false premise about that API. | transports, module names | `testing-ci` |
| `no_concurrent_agentdb_during_full_integration_run.md` | **Failures clustered in resource-heavy tests that do not reproduce in isolation are the contention signature** of a shared single-instance test resource. Prove it with a clean serialised re-run — never conclude "flaky/infra" by assertion. Second P: **do not pipe a long run's output through `tail`** — it discards the tracebacks you need; redirect to a file and grep it. | container, counts | `testing-ci` |
| `project_review_harness_share_repo.md` | (1) **A history rewrite does not purge**: pre-rewrite commits stay fetchable by SHA until GC; true erasure needs the host's support or delete-and-recreate. (2) When publishing internal tooling, **split by class** — the behavioural/diary layer is private, the reference/catalog layer is shareable — and make the shipped artifact self-contained so removed files leave no dangling citations (redefine the citations as IDs). (3) A share pipeline must be **re-runnable and scripted** (path rewrites, scrub greps, self-tests, install dry-run), not a one-time manual scrub. | repo URL, SHAs, file counts | `git-workflow` (1) + `writing-for-github` (2,3) |
| `reference_agentdb_port_mismatch.md` | **A wall of identical connection errors across every test is infrastructure — and it is neither pass nor fail: the tests never ran.** Diagnose by comparing the process's env/config against the actual listening port; a stale exported env from a previous container generation desyncs silently. **Machine fact, verified live today: `timeout` is not installed on this Mac** (`which timeout` → not found) — it had to be hand-pasted into this task's brief, which is direct evidence it belongs at user level, not in a repo silo. | script path, ports | `CM` (the `timeout` line) + `testing-ci` |
| `reference_bdd_harness_patterns.md` | (1) **A dispatcher that routes by key must raise loudly on an unhandled key** — a silent default is how drift stays invisible. (2) **Generated files carry a DO-NOT-EDIT header naming the regeneration command and the source revision**; a hand edit without a source bump is a violation. (3) Under a parallel runner, **hardcoded resource IDs collide** — allocate unique per-test IDs. (4) When a harness converts "unimplemented" into "skip", a hard runtime failure localises to the **wiring table**, not the code under test. (5) A test client that bypasses real auth middleware needs an explicit dependency override — the bypass is silent. | 24 named patterns, file:line | `testing-ci` |
| `reference_bdd_harness_pitfalls.md` | (1) **A harness that supplies "sane defaults" defeats any test probing the absence of that field** — before adding a validator for X, grep the harness for default-backfill of X. (2) A convenience path that is not a thin proxy of the production entry point hides contract violations; **when one variant fails and a sibling passes for the same scenario, the passing sibling is the discriminator**. (3) **A structural/AST guard on test strength is necessary, not sufficient — it cannot see circularity.** Trace each asserted value to its SOURCE; three named vacuity shapes that pass the guard: re-derives production's own expression against the same source; asserts a harness-constructed invariant; echoes an input literal. (4) Before reporting a weak assertion as "passes on broken production", **confirm the test is executing** — a dead test is not weak-but-passing; run the specific case and read PASS vs SKIP, never infer from an aggregate count. (5) **There can be more than one independent silent-skip gate**; clearing one leaves the other. (6) Separate pre-existing from branch-introduced failures using **both** provenance (`git log <base>..HEAD -- <path>` shows the branch never touched it) and environment-specificity. | 9 numbered pitfalls, file:line | `testing-ci` — second-highest-value file in this silo |
| `reference_gam_creative_level_targeting_exists.md` | **A schema field that exists and validates but that no downstream consumer reads is INERT — it looks implemented and is not. When checking "is X implemented", find the CONSUMER, not the declaration.** Second P: a capability may be driven by one side's *configuration* rather than the other side's *request* — check the other party's config path before declaring it missing. | adapter, file:line | `pr-review-method` |
| `reference_gam_language_targeting_silent_drop.md` | **A field valid at the schema layer and unhandled at the backend layer is silently dropped**: the validator only rejects *unknown* fields, so a *known-but-unimplemented* field passes validation and does nothing. The gap between "known to the schema" and "handled by the backend" is exactly where quiet failures live — **enumerate the backend's handled set and diff it against the schema's field set**. Second P: when the validate path says "supported" and the build path unconditionally raises, wiring the field alone will not work. | field, adapter, file:line | `testing-ci` + `pr-review-method` |
| `reference_harness_freshness_mechanisms.md` | (1) **A review harness commits the sin it exists to catch — validating against a stale PROXY instead of the derived source of truth. Never hardcode a version in a harness artifact; derive it at runtime.** (2) **When a human catches something MECHANISABLE that a detector missed, extend the detector — don't just note it.** Division of labour: humans excel at convention/consistency drift (wrong version, tag-vs-id drift, naming-polarity outliers); the automated sweep excels at exhaustive enumeration. **Mechanise the convention-drift classes humans keep catching.** (3) A detector can miss on **two independent counts at once** (the pattern anchor *and* the scan root) — when it misses, check both matcher and input set. (4) **A derived authority LAGS its source**; when the derived one lacks a value the source defines, the system silently FORCES a less-precise substitute — that forcing is the root cause of downstream incoherence and is an upstream gap, not a local bug. (5) A detector's standing counts are **context to compare against a baseline, not per-run alarms**; the actionable signals are a self-test failure and a stale-snapshot warning. (6) **A personal harness must never gate other contributors' CI.** (7) **Trace comment-vs-string before counting occurrences** — prose *describing* a convention is not an instance of it. | detector paths, code counts | `testing-ci` + `pr-review-method`; (7) reinforces CM Verification § |
| `reference_per_diff_review_detectors.md` | (1) **Every detector is a WORKLIST, not a verdict** — heuristic, human adjudicates, self-testing. (2) **The input to a new automated check is the corpus of ESCAPED defects, not imagination** — each detector here was built from a real miss someone else caught after the review passed. (3) A consolidation gate must **diff the citations in the working notes against the final artifact** and flag any that vanished — consolidation loses *sites*, not just findings. (4) **"optional / non-gating" is a laundering phrase for a dropped finding.** (5) **Auditing only the easy-to-grep surface is incidental coverage** — it catches a bad value only if it is a named constant *and* the name matches; enumerate every EMISSION surface for a value. (6) A grounded constant with **no test comparing it to the pinned source** means a future drift reaches the user with nothing red. (7) A papered-over import cycle held together by a lazy in-function import fails the moment it is hoisted. (8) **Wire the detector into the agent's instruction — recall ≠ enforcement.** | detector names/paths | `pr-review-method` + `testing-ci` |
| `reference_review_patterns.md` | 42-pattern catalog; ~30 survive substitution. Standouts: **P9/P14 the change's stated purpose has a pin test — a single-line revert must redden ≥1 test**; **P39 don't relocate code to evade a guard, widen the guard** (a scan-set escape is a silent hatch); **P38 `raises(SpecificError)` with no value assert still passes when production swaps to a PARENT class — assert the externally-visible value, not the type**; **P41 error-path code must never raise — a translator that crashes shadows the original exception and fails open with no output**; **P28 a test field populated by calling the same helper production calls is lossy — capture real bytes or rename the field**; **P27 enforcing a constraint by REBUILDING a typed object drops every field not passed**; **P32 an eagerly-derived table built at import misses lazily-loaded members — assert completeness at test time**; **P30 a test pinning a dead shape gets deleted, not maintained**; **P40 never assert on framework-internal error text**; **P25 a consolidation that ships unused is debt — the reinvention IS the defect**; **P37 one operation family, one verb prefix, so grep finds the primitive**; P5/P26/P36/P42 cross-surface symmetry; P16 every scoped query filters on the scope key; P17 never string-match another layer's error message; P18 named outcome types replace ambiguous tuples; P22 no compat path without a deletion criterion. Meta: **many items were defects the rework itself introduced — pre-apply the catalog before requesting review, and after fixing a nit once, grep to confirm it did not regress**; **substrate authorship does not exempt the work from the substrate criteria.** | 42 IDs, file paths, PR #s | `pr-review-method` — richest file in the corpus for review content |
| `reference_ruff_f821_ignored.md` | **A linter's ignore list can disable the exact check you are relying on.** Verified live today: the undefined-name and unused-variable rules are both in the ignore list (`pyproject.toml:161,167`), so a missing import fails only at runtime. Before trusting "the linter would have caught it", **read the ignore list**. Asymmetry: the autofixer still prunes *unused* imports correctly — the blind spot is *missing* ones. | rule codes, repo | `testing-ci`; textbook case of CM's "verify with the same instrument" |
| `reference_run_all_tests_in_network_artifacts.md` | **A containerised test runner sees a build context stripped by the ignore file**, so any test that scans excluded paths fails inside the container and passes on the host — runner artifacts, not regressions. Keep a known-artifact set; only failures beyond it are real; prove each is branch-independent by **both** provenance and environment-specificity. Second P: **the runner's tail and exit code lie about totals — the per-suite structured results file is authoritative.** | ignore-file lines, test names | `testing-ci` |
| `wire_envelope_policy.md` | (1) **Assert on the bytes the consumer receives, not on a reconstructed in-process object. Reconstruction is LOSSY** — two distinct internal errors collapse to one external code, so the reconstruction always picks one regardless of which was raised; a test on it grades the reconstruction layer. (2) **A synthesised fallback can land in the same field as real captured bytes and look identical**, so a "wire" assertion can pass against something the consumer never received — carry an explicit `synthesized` flag and let security/disclosure tests demand the real one. (3) **A new assertion option needs its own meta-test pinning all three states** (real passes / rebuilt rejected / rebuilt-without-flag permissive). (4) Cite the reference implementation **by name, not line**. (5) Migration policy: new sites use the new form, existing migrate when touched. | helper signature, PR # | `testing-ci` — top-tier |
| `flask_migration_critical_knowledge.md` *(unindexed)* | Migration Ps, all surviving substitution: (1) **Re-derive the RATIONALE, not just the decision** — a right decision resting on a wrong reason gets reversed the moment someone checks the reason. (2) **On a framework swap, enumerate the DEFAULTS that differ, not just the APIs** — a changed default (redirect 307 vs 302) is a silent behavioural change at every site that relied on it; remedy is a wrapper helper carrying the old default plus a guard banning the raw call. (3) **Pin externally-visible constants (callback URLs, route paths) byte-immutable with a test** so a refactor cannot move them. (4) **Before "enabling" a control, check whether it was ever actually active** — a defined-but-never-called helper means the control is off and turning it on breaks callers. (5) Signature-compatible calls with different **multiplicity** semantics (returns last vs all) are the silent-data-loss class. (6) A lazy-evaluation hazard reproduces only on the path production takes — test-path/prod-path accessor divergence hides it. (7) **Sequence a large migration so the risky semantic cutover is a late, single-file alias flip** with all mechanical work done first. (8) The plan document drifts from the code faster than the code does — treat every load-bearing constant in it as a claim. | framework names, 18 items, commits | `system-design` + `refactor-safety` |
| `flask_to_fastapi_migration_v2.md` *(unindexed)* | (1) **Freeze the external contract for the whole migration and lock it with protective tests before layer 0** — breaking changes are fine internally as long as the external surface is unchanged. (2) **Introduce the indirection early so the semantic cutover is a one-line change** (an alias flip at a late layer). (3) A layer table where each layer has an explicit **machine-checkable exit gate** (a version constant asserted by a test) is what makes "which layer are we on" answerable without judgement. (4) A paused programme records its **resume preconditions** — verify branch currency and check whether the approach shifted while paused. | branch, layer names, commits | `system-design` |
| `reference_bdd_inline_steps_escape_guards.md` *(unindexed)* | **A guard anchored on a directory root cannot see code outside it — and a green guard suite reads as "caught everything" while being structurally unable to see the file. This is a SCAN-SET escape, distinct from a matcher gap.** When a file escapes the scan set, apply the guard's matchers by hand for that file and file the widening as its own follow-up — it is not any single change's job. | UC name, guard paths | `testing-ci`; independently re-derives ROUTING-RULE ship-gate #4 |

## 1B. agentic-prebid, architecture & strategy (9 indexed + 9 unindexed)

| file | P | I | destination |
|---|---|---|---|
| `project-configuration-first.md` | **Ship a configurable engine, not opinionated code: everything that is POLICY becomes schema-validated CONFIG; the mechanism enforces the config.** Layer it (defaults → deployment → tenant → per-request) with explicit precedence; keep it modular; hold **strong defaults — configuring nothing must give a working system**; and **do not expose every internal as a knob, only genuine policy decisions** (that clause is what prevents config sprawl). | product names, decision IDs | `system-design` |
| `project-neutral-primitives-charter.md` | **Split every capability into a coordination MECHANISM (a testable pure function or state machine) and an AUTHORITY (trust root, identity, money). Own the mechanism; never own the authority. THE TEST: is it provable by a test that references no domain meaning and no party's trust relationship?** If the test needs "party X is trusted" or "this is the authoritative record", it is authority — keep it external. Corollaries: *notary not bank*; **never collapse two distinct trust roots into one**. | protocol names, tier lists | `system-design` — crispest architecture test in the corpus |
| `project-salesagent-lessons.md` | (1) **TYPED BOUNDARIES: one typed internal representation; serialise/deserialise ONLY at system boundaries — once in, once out; never between internal layers. Errors are typed models too. The boundary absorbs version differences so upstream changes don't break internals.** (2) **The teaching story:** an untyped dict boundary read a nested key against a flat real shape; the lookup silently never matched, the system ran with EMPTY parameters, no error, spending real money invisibly — until the boundary became a typed model. (3) **EXECUTABLE POSTMORTEM: every fixed bug becomes a blocking hook or guard test.** (4) **Two-mechanism versioning** — an extra-fields mode for forward-compat junk, declared-deprecated-fields + pre-validators for version *translation*; conflating them fails one job. (5) Environment-driven strictness: forbid-extras in dev, ignore in prod. (6) A serialiser wired once at the engine level beats per-call serialisation. (7) **Never edit a test to pass.** (8) **Middleware does NOT run on a direct in-process call to the underlying manager** — an in-process test bypasses every middleware. (9) Authenticate **before** resolving tenant/context. (10) A parent model must re-dump and propagate serialisation mode to nested children. (11) **A prior team's abandoned PROPOSAL validates the diagnosis, not the solution — say "validated diagnosis, not shipped-proven".** (12) A repo's own notes cite artifacts that were never built — verify existence before citing. (13) A squashed history makes cited upstream changes unverifiable; state the provenance limit. | repo paths, 55-item catalog | `system-design` + `CM` Claims § (11,12) |
| `project-technical-grading.md` | (1) **A conformance grader can grade YOUR OWN system fully (you own the internal log) but only a third party's OBSERVABLE RESPONSES — for a third party it CORROBORATES, it never CLEARS**; it cannot distinguish a real implementation from a schema-valid shell. (2) Gaming modes that generalise to any self-reported compliance or benchmark: **surface-minimisation** (declare few capabilities → "conforms" on a slice; *undeclared* reads as "not tested", not "failed" → declared breadth must be first-class and cross-checked against observed); **the test-control hook is implemented by the party being tested**; **forward-compat grading unknown checks as PASS while the party pins an old version** (fix: version floor at current-minus-N); advisory-severity laundering; **"no scalar score" is a NEUTRALITY property, not an ASSURANCE one — publishing a verdict rule just relocates the collapse to the consumer**; **the party being graded runs the harness → require an INDEPENDENT re-run over a RANDOMISED surface for consequential use**. (3) **Rewrite the body down to the residuals' altitude — if the caveats contradict the headline, the headline is wrong.** (4) A fresh adversarial pass found a real hole even on a *focused single-lens* review of a *mostly-consolidation* document. | corpus names, IR ids | `system-design` + `pr-review-method` |
| `project-real-money-first.md` | (1) **A safety check's failure mode must be HALT, not continue** — "every violation freezes rather than proceeds" is the difference between a toy and a real system. (2) **Compensate FORWARD**: an effect on someone else's system cannot be un-done, so a reversal is a NEW obligation, never an edit to the old record. (3) **Act only on ≥2 independently-sourced corroborated facts, with NO single-source fallback** — the fallback silently defeats the requirement. (4) **The core never self-advances a positive irreversible fact** — such transitions are ingress-only, gated on cross-boundary signed evidence. (5) **Default to a boundary-RESPECTING mock** (the fake sits across the real trust boundary) rather than an in-process fake, which proves nothing about the boundary. (6) **"Reads correct" ≠ "is correct"** — six consecutive fresh adversarial passes each found a real hole, including on a small symmetric edit. (7) **Mirroring a hardened contract's PROSE is not inheriting its ENFORCEMENT.** (8) **A ceiling without a floor is half an authority model — when you add a bound in one direction, ask for its dual.** | invariant IDs, doc paths | `system-design` + `pr-review-method` (6,7) |
| `project-authority-layer.md` | (1) **A predicate must resolve the SPECIFIC controller × scope × fact-type, not just the KIND** — pinning a kind collapses the gate to a single spoofable bit. (2) **A feature's headline capability IS its attack surface** ("trust many issuers" ⇒ any trusted issuer can forge for anyone). (3) **Monotonicity checks are direction-blind: a naive subset test INVERTS for fields where narrower means more permission** (a floor; an exclusion list emptied now targets the excluded) — encode direction per field. (4) **Without a domain separator inside the signed bytes, a signature is re-aimable across consumers.** (5) **Self-issuance collapse**: when issuer and subject can be the same key the assertion is self-asserted, not witnessed — the issuer must be provably disjoint from the delegated keys. (6) **"Ours" ≠ "bankable ahead of the chain"** — a mechanism you own can still be unenforceable because its inputs are not plumbed. (7) Own the keyring never the keys; own the verifier never the root; **emit a source×assurance SET, never a scalar — collapsing to a score is the act that makes you the authority**. (8) **"Assurance = attribution, not truth."** (9) Reject a format whose only production-grade library is in another runtime — pick what your runtime verifies natively. | RT ids, credential formats | `system-design` — the security-design vein |
| `project-threat-model.md` | (1) **A threat model for autonomous components is not an enumeration of attacks across a perimeter — it is a statement of what holds no matter what the components do.** The classic assumptions die: the dangerous actor is inside, authorised and autonomous; you cannot tell compromised from honest by inspection; the real boundary is **economic** (whose interest does this serve), not cryptographic; the human backstop is gone at machine speed. (2) **State the adversary ONCE as a small capability set and assume every component, including yours, may exhibit all of them.** (3) **Deterministic containment: components PROPOSE, the kernel DISPOSES; gate on WITNESSED facts, never on authored state.** (4) The honest output is a **bounded guarantee with named residuals, never "airtight"** — name what is out of scope rather than assuming it away. (5) **SEAM-BY-SEAM verification is structurally blind to CROSS-SEAM composition** — a chain where every hop passes its local guard can be globally catastrophic. Hunt it with **fresh, diversified** adversaries (diversified by surface, not identical); if the finding is real they CONVERGE. (6) **Sequential quorums compose as `min`, not sum** — a corrupted input to the first flows through the second uncaught, because the second corroborates a different proposition. (7) **A human-in-the-loop gate is model-STEERABLE via persuasion** when the model authors the rationale the human reads; an exfiltration scan does not catch persuasion. (8) **Two adjacent components can EACH assume the OTHER gated a check.** (9) **"X happened" ≠ "X was authorised."** | A1–A6, TC/GAP ids | `system-design` + `agent-dispatch` (5) — (5) applies to this very audit |
| `project-transition-as-trigger.md` | (1) **A durably-committed transition atomically enqueues its reactions in the same transaction**; at-least-once delivery made idempotent by the causing sequence number + ordinal. (2) **Record the author on every transition and declare a closed authorship policy per edge** — no party unilaterally moves a shared record to its own benefit. (3) **A strictly-backward causal reference makes the causality graph acyclic BY CONSTRUCTION.** (4) Cascade safety = acyclic-by-construction + a closed finite catalog with static depth/fan-out bounds + one choke point + idempotent effects; **a cascade only PROPOSES, the guard DISPOSES.** (5) **THE BIG ONE: guards evaluated once at enqueue and re-checked only for legality at drain are a TOCTOU — every precondition-guarded deferred action must re-evaluate the FULL precondition at the commit fence.** (6) **A safety property scoped to one path leaves every other path unprotected — a scoped invariant is an asymmetry another axis will expose.** (7) **An approval bound to an ID rather than a content digest permits a post-approval swap.** (8) A singleton where the domain has N is a cardinality bug. | §/T/CR ids | `system-design` |
| `project-agent-discovery-decapture.md` | (1) **UNBUNDLE what a single gatekeeping service bundles** — identity at the party's own domain, capability self-published, findability via plural indexes, authority as a signed mandate checked against an open root, trust as portable attestations. **Being in a registry is not a trust signal.** (2) **Bind to FROZEN objects, never to a churning reference SDK — the SDK is the capture surface at the impl layer even when the spec reads neutral.** (3) **No single external standard is load-bearing** — keep plural, bind the transport-neutral core not vendor extensions, treat unratified bodies as watch-and-align not depend. (4) **Origin ≠ control** — vendor-originated but now neutrally governed is a different risk. (5) **Make the dependency deletable and prove it with a test.** (6) Grade conformance (own the mechanism), never grant existence (authority). (7) **Prefer catalysing a multi-party arrangement under an existing neutral body over doing it solo** — solo hands critics the symmetry. | org names, motive intel | `system-design`; (2) reinforces `prebid-adcp` |
| `project-agent-trust-layer.md` | (1) **Untrusted autonomous code that ACTS needs a hardware-virtualisation boundary; a shared-kernel container is escapable.** Defence in depth beyond it: egress control, ephemeral one-shot instances, resource/time caps, **no secrets in the guest**. (2) **Secrets and orchestration state live in the HARNESS, never the guest; credentials are runtime config, not prompt content.** (3) **Two independent containment layers: it cannot escape the box AND it cannot do what the mechanism will not authorise** — even a fully compromised agent can only emit a proposal. (4) **A sandbox verifies conformance and probes KNOWN-bad behaviours deterministically but CANNOT certify a non-deterministic agent's FUTURE behaviour — pair pre-deploy testing with a production runtime witness; neither alone.** (5) **Never roll your own isolation** — inherit a battle-tested primitive; ship safe-by-default; state that self-hosting shifts host hardening to the adopter. (6) **Open-sourcing the isolation primitive is a strength; only the DEPLOYMENT stays private. A vendor closing source is a commercial moat, not a security truth.** (7) **Tier options by the actual isolation boundary and LABEL the tier** rather than treating them as interchangeable. (8) **Neutral governance is the decisive tie-breaker between otherwise-equivalent open dependencies.** (9) Ground a governance question in how the org already operates legitimately, not abstract anxiety. (10) **Display FACTS, not endorsements; mirror, don't judge.** | vendor names, tiers | `agent-dispatch` + `system-design` — the most directly applicable file for running agents |
| `project-transport-and-comms.md` | (1) **Design against a protocol's ANNOUNCED direction, not just its current version** — when a release candidate removes sessions, never depend on the session. (2) **Identify what a protocol PUNTS and own that**; completing its mechanism layer is not competing with it. (3) **Confirm wire-string literals byte-level against the schema before hardcoding** — abstract names differ from wire method strings. (4) **A seam over multiple transports MUST abstract push-native vs poll-native**; that difference does not vanish behind a common interface. (5) A directional primitive is not a symmetric peer volley — model the asymmetry. | protocol versions | `system-design` |
| `project-two-seat-model.md` | (1) **Two roles are one symmetric substrate, not two systems — the role is a FILTER derived from the authenticated principal, NEVER a request field, and it NARROWS, never widens, the policy.** (2) **A stored earmark alongside a derivable fold is a second source of truth** — drop the object, keep the fold term. (3) A cross-aggregate invariant needs a real mutex, not an optimistic fence. (4) **Two parties measuring DIFFERENT events need a structural tolerance band** — a systematic gap, not noise; without it you get spurious disputes or hidden fraud. (5) **Design the corroboration so the two sources have OPPOSED incentives** — the gap then freezes. (6) An exposure fold must be TOTAL so it never releases un-reversed spend. (7) **When two designs collide, look for the orthogonal axes that make them non-competing** (authorisation × solvency at cross-scope × per-scope; they compose at the commit edge as a conjunction). | I/T ids, doc paths | `system-design` |
| `project-reference-agents.md` | (1) **INDEPENDENCE MUST BE ECONOMIC, NOT CRYPTOGRAPHIC.** Key/ownership disjointness does not imply the interest-independence corroboration needs. Two parties are safe checks on each other when their interests are **opposed**; a third party is dangerous precisely because whoever **selects and pays** it can align its interest. (2) Record who **funded** and who **selected** any attestation; make a witness's fee **volume-independent** if it also witnesses the volume. (3) **Check disjointness over the UNION of witnesses AND fee beneficiaries** — a shell passes a "not self" test because the shell's key is not the party's. (4) **A resold or re-signed reading is two roots, one observation** — evidentiary independence ≠ signature independence. (5) **A fresh adversary, not the synthesiser, must run the adversarial pass** — the synthesiser's own fixes "read correct" by mirroring the hardened prose; confirmed 7×, including recursively on the red-team's own output. (6) **A self-reported marker cannot bound an adversary who controls it** — attribute the bound to the independent mechanism, not the tag. (7) **Every new side-effecting port must be registered in the replay/shadow set, or replay becomes a live action.** | RA/B ids, archetypes | `system-design` + `agent-dispatch` (5) — top-tier |
| `project-human-surface.md` | (1) **The surface is a typed API, not a UI; the frontend is pluggable and non-load-bearing.** Testability becomes first-class: headless request/response asserts, no browser, and a bring-your-own frontend tests against a **mock of the contract** so it never gate-keeps the surface's testability. (2) **Render NEUTRAL domain objects, never the upstream payload** — the source format is a labelled attribute plus a raw drill-down, never the STRUCTURE of a screen; otherwise you build one console per source. (3) **Scope/tenant comes from the AUTHENTICATED identity, never a header.** (4) **Testable BECAUSE the queue IS the durable workflow** — state in an in-process thread plus a module global is dropped by a restart and cannot be durably tested. (5) **Render errors as ACTIONS by recovery class** (correctable → fix the field; transient → retry ETA; terminal → why + receipt). | framework names, endpoints | `system-design` |
| `project-aamp-play.md` | (1) **When a spec is mature at the OBJECT layer but reference-code-only at the WIRE layer** (its own SDKs disagree, share no state machine, ship several error shapes), **bind every semantic to the frozen objects and author the missing mechanism layer yourself**; treat the reference SDK as evidence plus a divergence ledger, never as the contract. (2) **Make extensions additive-and-ignorable — strip them and the payload is still valid under the base spec, proven by a guard test.** (3) **The SDK soft-power trap: a reference SDK relabelled "production-ready" becomes the de-facto contract.** | org names, versions | `prebid-adcp` + `system-design` |
| `project-order-of-record-pivot.md` | (1) **NEVER let a foreign system's identifier be your primary key** — mint your own durable identity and keep the external id as a foreign key inside the adapter; crossing that line welds you to the external system. (2) **Prove removability with a test that runs the whole flow with the optional package UN-IMPORTABLE** — the presence of a seam is not proof of separability. (3) **Maintain the PORT and your own adapters; third parties write theirs** — taking on a fleet of third-party adapters is the maintenance trap, and someone else's early stubs are not harvestable assets. | vendor names, seat framing | `system-design` |
| `project-substrate-ownership-strategy.md` | (1) **When two formats have no direct lossless translation, both must project through a superset pivot — whoever owns the pivot owns the interop.** (2) **Anchor a canonical model to the SLOW-moving standards, not the churning wire protocols.** (3) A "no single authoritative wire-contract artifact" finding means adopting it requires you to **author** the contract — that is leverage, not just labour. (4) **Run the ground-truth pass before committing the strategy** — the reality check corrected an earlier optimism (the "uncontested" seat was already contested and consolidating). | org names, doc paths | `system-design` |
| `project-positioning-and-core-decisions.md` | (1) **A "swap X for Y behind a thin seam" escape hatch can be SEMANTICALLY ILLUSORY — the seam ports the INTERFACE, not the GUARANTEES.** Same-transaction exactly-once and queryable state are properties of the co-located store; a separate-cluster engine cannot reproduce them. **Check whether the alternative can actually deliver the property before calling it an escape hatch.** (2) **Mitigate vendor risk by DESIGN, not by vendor health**: permissive licence + state as ordinary tables in *your* database means company failure loses future development, not your running system or data. (3) **Two bright lines for a non-deterministic dependency: usage may grow but IMPORT SITES may not** (one seam, stays deletable); **and it lives ONLY in the decision layer, never in mechanism** (translation/booking/idempotency/receipts stay deterministic), with every call wrapped as a durable step. (4) **"Inherited-solved": name the risks your operating context already covers and stop re-raising them**; focus risk thinking on the genuinely novel adversarial surface. | engine names, R0–R7 | `system-design` + `agent-dispatch` (3) — (1) is the sharpest architecture insight in the corpus |

## 1C. remaining silos (18)

| file | P | I | destination |
|---|---|---|---|
| `project_agentic_advertising.md` (AA) | **Lock the data model before building the viewer** — v1 is data + schema only, no build toolchain; the frontend *and* the automation both come after the corpus stabilises. **Do not design the drift-detection mechanism before you have classified anything.** Corpus discipline: every record validates against a schema, every cross-reference uses stable IDs, every citation is a typed object, the filesystem maps 1:1 to the route space. Scope: a downstream repo's broken dependency pin is *recorded*, not fixed from this work. | repo path, schema names | `craft-context-file` / `system-design`; matches ROUTING-RULE "cheapest form first" |
| `project_storyboard_run_methodology.md` (AA) | (1) **Capture a proven reproduction recipe once, in the repo, and point at it — do not re-derive.** (2) **A local-only compat patch is stored VERBATIM in a durable doc, not left in a temp worktree the OS wipes** — re-apply from the doc. (3) **CLASSIFY REACTIVELY — real work touching an area produces the classification; never pre-emptively classify areas no work touched.** (4) The absorb pattern: one question doc per ambiguity; overlay updated with verified file:line; a run dossier with setup + headline numbers + raw artifacts + a gaps catalog carrying severity, impact, citation, location and prior art. | commands, Q ids | `testing-ci` + `system-design` |
| `adcp-kernel-standalone-decoupled.md` (ADT) | (1) **Build the shared kernel in a repo you control, decoupled from every consumer; take only PUBLISHED, signed artifacts as input, never another repo's SOURCE** — the original sin was each consumer hand-curating a drifting table. (2) **The drift guard diffs a published DUMP artifact, not source**, so it works across languages without coupling to internals. (3) **Facts can ALL be right while RELATIONSHIPS hide blockers.** (4) **ALWAYS re-trace after heavy edits — a convergence pass catches the errors your own fixes introduced, and MONOTONICALLY DECREASING findings is the signal you have reached the fixed point; stop there, don't loop forever.** (5) **An enumeration of EXPLICIT occurrences systematically undercounts** — a 4-way scan found 106 explicit sites and verifiers showed that was ~55–60% of the real surface; the rest was implicit. | S1–S16, paths | `pr-review-method` + `agent-dispatch` — (3)(4)(5) are method-grade |
| `adcp-kernel-tool-is-go.md` (ADT) | **Choose the implementation language of a CI-distributed tool for its DISTRIBUTION properties** (single static binary, zero runtime deps in every consumer's CI), not for the ecosystem of its data — "X-first" applies to the reference *binding*, not the tool. **Keep per-language pieces in their language; the tool consumes only their JSON and never imports another language's SDK.** Concrete: check whether your directory name is reserved by the toolchain. | language names | `system-design` |
| `no-real-names-in-db.md` (GAD) | **Store only stable identifiers, never display names** — display names are a separate enrichment with partial coverage (~57%), and accounts get renamed or deleted. **A failed lookup's error body can leak into stdout — scrub it.** Machine fact: the system Python fails TLS verification against some hosts; drive the CLI via subprocess instead. | db/table names | `CM` Memory § (aligns with "capture WHAT, never WHO") |
| `cmux-shortcuts-cheatsheet.md` (HOME) | **NONE.** Substituted: *"a reference file the user maintains lives at a known path; open it when relevant."* Vacuous — it changes no behaviour without knowing which file, and reduces to "keep notes in files". Anchor verified live (`/Users/quantum/cmux-shortcuts.md` exists, 2887 B). Correctly filed; leave it. | — | stays |
| `project-hermes-fleet-plan.md` (HP) | Very large; ~20 Ps survive. Standouts: **(1) CAPTURE AT THE HIGHEST VANTAGE POINT THAT SEES THE WORK** — the orchestrator spawns the leaf tools, so it captures their I/O even when the tool exports nothing; vantage order = orchestrator > native telemetry > session-file harvest > provider API. **(2) GUARANTEES ASSERTED, NEVER EXERCISED** is the headline audit class: a scrub step never tested (so a redaction regression becomes an *immortal* leak under write-once storage), an archive claimed "never lose, verified" that was never read back, loss-prevention machinery unobserved ("no loss until the buffer fills"), a precondition (clock sync) never stated anywhere, and **nothing watching the watchdog**. **(3) Enable irreversibility only AFTER the protective test is green N consecutive times — having the test is not having the gate; the ORDER matters.** **(4) Workers hold CAPABILITY, never third-party CREDENTIALS** — an injected worker can call a tool but cannot exfiltrate a key it never had. **(5) Pending approvals WAIT or EXPIRE-DENY, never default-approve.** **(6) One human approver by design: agent gates do the heavy reading; the human gets one batched queue where every item carries evidence, so ratification is a READ, not an investigation** — compensating controls are unskippable gates + reversibility + reconciliation that catches approval abuse as drift. **(7) SUBSTITUTION MAP: classify every dependency swappable-by-design (substitute named, seam thin, exit REHEARSABLE) or deliberately-coupled (conscious bet + documented exit + trigger); unclassified is an audit finding** — the rule caught its own first violation immediately. **(8) A hermetic renderer: pure function of (tree, target) → artifacts + manifest, no timestamps or env inputs, so the same tree yields the same hash — which is what makes one-variable experiments and incident attribution real; lint INSIDE the render, unskippable.** **(9) Two grains that cannot be joined (per-run vs per-day-aggregate) — reconcile at the grain boundary with a tolerance and make "spend joined to no run" a first-class alerted metric.** **(10) PER-FIELD PROVENANCE `value + source + is_estimate + candidates`** so disagreement is visible as data. **(11) Cost DERIVED from a hardcoded price table discards the authoritative reported total.** **(12) Relocating one component is availability THEATER when the actual gate lives elsewhere — the unit is the whole control path.** **(13) Separate first-pass validity from post-repair validity, or the repair step launders model weakness.** **(14) A bake-off holds the GATES constant and varies only the PROPOSER; one mind per treatment; tune each scaffold per model before comparing.** **(15) Report capability and reliability separately (pass@k vs pass^k); cost as $/task AND $/success.** **(16) RAMP-WITH-CAPABILITY — cheap load-bearing controls now, heavy infrastructure when authority and evidence earn it.** **(17) "Buy portability, not multi-cloud"; pipeline logic in repo scripts with the CI YAML as a thin trigger adapter.** **(18) Harness facts: a subagent does NOT inherit the parent's system prompt, style or history, and a delegation that skips context files gives the child a DEFAULT identity — a persona hole at depth ≥1; the append-to-subagent-system-prompt flag pierces all nesting depths and is the only reliable non-negotiables channel headless; hooks receive the agent type, so per-depth capture is free; subagent transcripts live one level down under `subagents/`.** **(19) A change can be factually flawless and still wrong at the level of what it prioritises.** **(20) When a change makes a standing decision's wording false, amend the decision row or it becomes a trap ("spirit survives, letter misleads"); flag immutable-once-set parameters at the point of setting.** | D1–D95, O-rows, host names, vendor names | `agent-dispatch` (4,5,6,14,18) + `system-design` (rest) — the single densest PROJECT file |
| `temporal-backbone-design.md` (NOR) | (1) **"A transition skill without a trigger is a wish"** and its generalisation **"a prediction without a tripwire is a worry, not a control"** — a rule without a firing condition is not a control. (2) **Metrics are diagnostic, never targets**; and **"no agent writes the instruments it is measured by"** — the file also records the **authorship collision** where its own build plan assigned the same agent to author the lints it would be judged by, i.e. check whether your plan violates your own invariant. (3) **Every threshold is a number, with unvalidated defaults explicitly marked unvalidated.** (4) **A self-declared convergence is self-attestation** — claimant-authored criterion, self-generated generators, one context, no power validation; what stands is **searcher exhaustion only**. (5) **The falsification protocol: power-validate the instrument FIRST by seeding known flaws and requiring recall ≥2/3 or the nulls are discarded; use independent, diverse-prior searchers, half under a refute-condition; freeze the novelty criterion before the run; the owner may hold a private known-answer seed; the strongest permissible claim is "power-validated independent search found nothing", never proof of absence; the claimant NEVER authors the acceptance criterion.** (6) **A principle abstracted FROM the cases you found and then claimed as a bound on all cases is circular — testable only by searchers not told the principle.** (7) **POWER VALIDATION IS PER-TIER AND NON-TRANSFERABLE** — the same seeded-recall test scored 2.4/5 at one model tier and 5/5 at another; the "blind spot" was a tier artifact. (8) **NO INTEGRITY BOUNDARY: every trust anchor — receipts, stamps, tiers, hook scripts, the "append-only" ledger, settings — is plain text writable by the thing being constrained. A guard is not state-independent while the agent can edit the guard.** (9) **Fresh context ≠ fresh gradients** — an independent observer at the top of the ladder gives context-freshness, but tendencies are weight-level; cross-**model-family** observers are required, and same-family observers share a common-cause failure. (10) **Vacuous-oracle check: commands authored by the checked party; exit-0 ≠ discriminating.** (11) **Regulate SITUATIONS, not intentions** — self-detection fails exactly when needed; the model is the PLANT, guards are disturbance rejection outside it. (12) **The guard ladder: a tendency recurring twice despite in-context measures moves to a procedural checkpoint; three times moves to mechanical interdiction — you cannot out-prompt gradients.** (13) **A self-reported count is gameable by reclassification** — the process held its own gauge flat by relabelling decisions as amendments; check the classification rule, not the count. (14) A taxonomy that scores 6/6 on the cases it was built from is train==test; the out-of-vocabulary case had no cell. (15) **"A mechanism states which tension it resolves, or it is ornament."** (16) **Compilation must never sever provenance, or the compile step becomes a staleness-laundering machine.** (17) **Leverage(form) ≤ tier(content) — prose cannot become a gate without receipts.** (18) **Template adherence decays within hours** — audit same-day. (19) **Never explore beyond what the owner points at.** | group names, ledger ids | `CM` (12,19) + `agent-dispatch` (4,5,7,8,9,11,13) + `testing-ci` (10,14) — richest method file in the corpus |
| `user-norbert-wiener.md` (NOR) | **NONE.** Substituted: *"the project is named for a person the user admires, and that person's discipline is a design value rather than a naming joke; ground explanations in it."* Vacuous without knowing the eponym — it changes no behaviour in an unseen repo. Correctly filed. | — | stays |
| `project_teal_pr_remediation.md` (PAS) | (1) **A review can have full DETECTION and still fail, by resolving findings against the WRONG NORM** — it caught ~6/8 items and resolved them toward source-language fidelity and "more artifacts = quality" against a target ecosystem whose bar was lean conformance. **When porting between ecosystems, the TARGET's norms outrank source fidelity — state the precedence rule explicitly.** (2) The true gap: coverage and fallback-silence were both checked, but **nothing linked a DECLARED capability to the per-item obligation that declaration creates** — link declarations to their obligations. (3) **Two workstreams: fix the instance AND harden the generator/template, enumerated, not a footnote.** (4) When two sources can answer a question, rank them by authority and **fail loudly on miss** rather than silently defaulting. | adapter name, PR #, reviewer | `pr-review-method` — (1) is a genuinely non-obvious review insight |
| `corpus-contradictions-are-cross-file-and-untooled.md` (PH) | (1) **The characteristic defect of a multi-file corpus is one canonical file contradicting another — and per-artifact linting is structurally blind to it**, because each rule judges one artifact and each changed file is self-consistent. **A green suite plus a clean lint proves nothing about a cross-file invariant.** (2) The fix: a corpus-level check that loads the whole set in ONE process and fails on the invariant even at warn level; **verify it by injection; make it FAIL LOUD when its own anchors go missing** ("cannot safely enforce X") rather than silently disabling. (3) **Sweep TWICE — once on the identifier, then again on the SEMANTIC claim** — files disagree without using the identifier. (4) **Join soft-wrapped prose into semantic units** so a contradiction spanning two lines is caught. (5) Exclude deliberately-frozen snapshot inputs from staleness checks. | rule id, file paths | `craft-context-file` + `review-prompt` + `testing-ci` |
| `prompt-harness-goal.md` (PH) | (1) **When vendor EXAMPLES contradict vendor PROSE, trust the shipped examples.** (2) **Profile the thinking MODE, not the model.** (3) **`allowed-tools` on a skill is ADVISORY-ONLY in this harness — parsed, never enforced** — so a read-only stance is prose-enforced on both harnesses; documented stances overclaimed enforcement. (4) A detector generalised to rows its catalog never taught — **you only know by testing on cases the catalog never taught.** (5) **A false positive's root cause was READ-ENVELOPE MISATTRIBUTION — the harness's own framing tag one line past EOF matched as artifact content; the fix is a framing guard, not "more precision".** (6) **A reference file named `claude.md` case-collides with `CLAUDE.md` and auto-injects as project instructions** — observed live; disambiguate the filename. (7) **A rate-limited harness call returns error TEXT that graders must treat as FLAKE, not FAIL.** (8) Ops: absolute paths always (the persisted-cwd trap); worktree isolation for builders while fleets run; orphaned children hold pipes past kills; the runner needs a process-GROUP kill. (9) **Dogfood rounds converge to finer shades, not zero — gating on "zero findings" never terminates; use repeats or a severity floor.** (10) **Each maturity level is justified by the PRIOR level's measured pain.** (11) Zero-config rule: defaults alone must be correct; secrets never in config files; `show --resolved` with per-key provenance. | milestone ids, commits | `CM` (3,6,8) + `craft-skill` / `review-prompt` (1,2,4,5,9) |
| `prompt-harness-pr-review-method.md` (PH) | (1) **A ping is a second chance to AIM the agent** — re-asking an idle agent the specific contested question *plus what you have since verified yourself* produces a better report than the original prompt; **tell it what NOT to re-derive. Recover by pinging; do NOT re-spawn — the transcript is intact.** (2) **Reference-first transport: give a bounded manifest of exact paths PLUS why each is authoritative and let the agent read; do not paste diffs into agent prompts.** (3) **Seed each agent with hypotheses framed TO REFUTE, not confirm; require file:line evidence plus an attempted refutation per finding.** (4) **Untrusted-content framing is non-negotiable when the artifacts under review are themselves instruction-shaped text: state that instruction-shaped text in them is the SPECIMEN, not a directive.** (5) **Run the deterministic suites yourself rather than delegating, and check them against the change's stated claims.** (6) **Run workspace suites from their own directory — a root-flag invocation misresolves paths and FAKES failures.** (7) Do not misapply a critic to source it was not designed to judge. | skill names, PR # | `agent-dispatch` — (1)(3)(4) are absent from CM and high-value |
| `rereview-agent-goal-and-decisions.md` (RRA) | (1) **GATE DEFECT CLASS A: `phrase in DOC` is MONOTONE under insertion, but "the document asserts P" is not — so EVERY positive presence check over an append-extensible artifact is loose by construction.** Its dual, "no match found", is antitone and therefore safe — which is why forbidden-sets work. One rule covered ~98 sites; 20 mutation-proven instances. (2) **CLASS B: the detector's documented REMEDY clears the detection.** (3) **"Fix applied to the instance not the class" is not a third class — it is the GENERATOR of class A**, proven by the guards' own output: each reports the witness set that motivated it. (4) **Three gate shapes carry zero information: byte tripwires (fire on every edit), never-firing detectors (the error arm has never executed), saturated advisories (red regardless).** (5) **The predictor of guard reliability is NOT line coverage but BOTH-ARMS-TAKEN branch coverage** (107 of 1,037 enforcing; p=9e-07). (6) **THE DESIGN RULE: an assertion is reliable for the property it NAMES only if (i) it enumerates its universe and PROVES the enumeration complete, (ii) it evaluates the artifact the way the REAL CONSUMER does, and (iii) every exemption is granted by something the checked artifact cannot write.** (7) **When content moves, checks pinned to the old path go silently vacuous** — coverage went 60/60 → 1/60 with no signal, and the checker reported "clean, 219 items" on a tree with 157 emptied. (8) **A number needs a landed script, exact tree, seed, output and hash before a gate may depend on it** — the author's own re-derived figure failed to reproduce. (9) **THE AGENT HARNESS THAT WORKS (explicitly generalised): per-agent isolated clone pinned to the commit; a MANDATORY BASELINE ASSERTION (clean status + green exit) before EVERY mutation with stop-and-report; assignment by DERIVATIVE CHAIN and SEAM rather than by file (both strongest structural findings came from holding adjacent regions together); a mandatory PRIOR-ART step labelling each finding documented-where; LANDED NUMBERS ONLY; word limits; explicit delivery instruction; and a CROSS-VERIFY AGENT FORBIDDEN FROM READING OTHER AGENTS' WORK.** Result: 5 claims refuted including 2 relayed as settled; 3 agents self-corrected; 2 caught their own contamination. (10) **Maintain a standing "my own corrections — do not re-report these" list.** (11) **Read the project's own self-audit before reporting a breakage as a defect**, or you report its self-assessment back to it. (12) **Score raw and semantic mutation kills separately and run the documented remedy before declaring a survivor.** (13) **Do not generalise a per-endpoint reliability finding** — one connection's count is broken while four siblings are exact across 536 threads; over-generalising once fabricated a figure. (14) **A keyword detector on self-reported language measures claims, not outcomes** — it fired on 83.5% of bodies and was wrong about the majority because 59.3% of the "closure" language was written by the claimant. (15) **Test a proposed taxonomy against the real corpus before adopting it** — a flat enum was empirically disproven on 389 real threads. (16) **"Structurally collision-free" indices are observer-relative** — use a content digest. (17) **Read-only beats a sentinel: an older reader must never retract a newer peer's state.** (18) **Autonomy ladder with a USER-OWNED ceiling — config can never RAISE autonomy; a capability absent from the consent registry is unconsented and fails closed.** (19) **Contamination honesty: the first slice of a held-out set is not contamination-free, because the rubric revision saw every initial item — it is earned only post-rotation after the rubric freezes.** (20) **State a zero-observed result as a release BAR plus its power bound ("zero of 15 ⇒ ~18% upper bound"), never as "never".** (21) **PIPELINE LESSON: drafts must re-verify their findings at delivery against the then-current tree.** (22) **A term introduced during integration propagates silently** — a phantom label reached 19 sites and would have been falsely certified. | WP/B/D ids, schema fields | `pr-review-method` + `agent-dispatch` + `testing-ci` — the single most extractable file in the corpus |
| `rereview-preimage-underdetermination.md` (RRA) | (1) **When re-reviewing a revision, diff the specific DEFECTIVE CONSTRUCTS byte-for-byte against the prior baseline BEFORE reading any new material — measure whether the defective text changed, not whether the document grew** (703 lines added, defect blocks byte-identical). (2) **A revision can add REQUIREMENTS describing content without adding the CONTENT** — a schema with no instance, a law with no executable function; check for the instance. (3) **Include a CONTROL that should match: three implementations disagreed on two fields while the control matched byte-for-byte — the control is what proves the instrument rather than the subject is at fault.** (4) **Point at the live artifact rather than duplicating its state into memory.** (5) Agreement between two implementations is not agreement with the spec; distinct-valued inputs catch positional errors. | commit SHAs, field names | `pr-review-method` |

---

# JOB 2 — the 48 DELETE files

`~/.claude` is not a git repo (verified). Deletion is irreversible.

## 2A. `superseded-by-CLAUDE.md` (27) — supersession verified by reading `~/.claude/CLAUDE.md`

| file | superseder present in CLAUDE.md? | P destroyed by deletion | verdict |
|---|---|---|---|
| `feedback_agent_team_execution_model.md` | **PARTIAL.** L23–24 says "Strip named roles, on-call rosters, handoffs and sign-offs — map each to an agent equivalent." The **mapping itself is absent.** | The concern→agent-equivalent table: pattern drift → a fresh cold-context review pass on every layer-exit change; security gates → a security-review pass on the branch; irreversible-cut safety → rollback runbooks executable from cold context (**the docs ARE the bus-factor protection**); handoff between sessions → **the docs are the handoff, because agent sessions are stateless**. Plus two carve-outs CM lacks: **external-audience comms are still real — keep them, renamed to a named-audience doc**; and **effort estimates are fine as units, they proxy scope not calendar ownership**. | **UNSAFE-HIGH** |
| `feedback_docs_voice_factual_dense.md` | **YES.** L118–120 covers structure-not-less-content, the banned register, and location+fact+fix. | Minor residue: significance is stated as a fact (e.g. a version), not a claim; reserve trimming for genuine redundancy. | SAFE |
| `feedback_escalate_recurring_lessons_to_guards.md` | **PARTIAL.** L176–178 has the leverage hierarchy and "violated twice needs the gate". | **The guard's failure list IS the work-list and it stays red until done (a shrink-only allowlist ratchet).** The split: a code-pattern lesson → a structural guard; a process lesson → a CM gate **plus making verification an OBSERVABLE step (paste the evidence before claiming done)**. | **UNSAFE-HIGH** |
| `feedback_generalize_memories_and_skills.md` | **YES.** L169–170. | Minor: strip one-off provenance (numbers, dates, rounds) unless load-bearing and ours; the rule extends to skills, agent definitions and anything publishable, not just memory. | SAFE |
| `feedback_github_issue_drafting.md` | **PARTIAL.** L79–81 has delivery + "scope the gap and the contract". | **The entire include/exclude specification**: include impact bullets, what the contract requires, the current gap with pointers, **implementer-beware notes as non-obvious FACTS not solutions**, **acceptance criteria as OUTCOMES not steps**, open questions, out-of-scope. Exclude pseudocode, design alternatives with pros/cons, file-by-file plans, test inventories, the verification dump. **Length signal ~150–250 lines; 700 is too long.** | **UNSAFE-HIGH** |
| `feedback_lean_toward_quality_not_smaller_option.md` | **PARTIAL.** L124–125 has the preference. | **The recognisable triggers** — "document as known-deviation" / "defer to a follow-up" / "note it in the description" is the smaller option; focused sub-suite vs the full thing; the literal ask vs ask + the architectural completion. Plus **the self-check** ("am I offering A because it is correct, or because it is quicker?") and the boundary against unrelated scope expansion. | **UNSAFE-HIGH** |
| `feedback_no_agent_fanout_by_default.md` | **MOSTLY.** L31–39 covers default, named-tool, latitude, hidden downgrade, cost. | **The enforcement form: deny the agent-spawning tool in settings so it is physically blocked — OFFER it, never apply it unilaterally.** Plus **self-assess equivalence honestly and name where the substitute is structurally weaker** (CM says "flagged up front" but not "name the weakness"). | **UNSAFE-HIGH** |
| `feedback_no_code_in_planning_stage.md` | **MOSTLY.** L82–86 covers the rule, config-bump, ambiguity default, recovery. | **The SUBAGENT STAGE CONTRACT: when spawning, explicitly state "planning stage — spec only, no file edits" or "implementation approved — edits in scope"; if the stage is not marked implementation it is planning.** Subagents do not inherit the stage. Plus **"do not re-attempt the edits under a different framing."** | **UNSAFE-HIGH** |
| `feedback_no_internal_dialogue_in_public_artifacts.md` | **PARTIAL.** L113–116 names the categories. | **The sentence-by-sentence filter**; the **review-WORKINGS** category as *distinct* from strategy/motive (no fan-out counts, no detector names, no "our tooling caught it" meta-scorekeeping, **strip any "what we learned" section entirely**); the **before→after strip examples** (which are the teaching); **echoing the user's own framing does not exempt it**; and **run a vocabulary gate on the outbound artifact and on any draft handed over to paste**. | **UNSAFE-HIGH** |
| `feedback_no_pr_specifics_in_memory.md` | **MOSTLY.** L168–170. | **For ongoing tracking across cycles, edit ONE comment in place — do not add a new one each round**; and **include the head SHA so reviewers can tell when it is stale.** Plus the worth/not-worth lists. | **UNSAFE-LOW** |
| `feedback_no_ready_claims.md` | **YES.** L90–94 carries the banned list, raw state, both commands. | Only the bad/good example pair and the "narrower = stickier" rationale. | SAFE |
| `feedback_no_unsafe_autofix.md` | **PARTIAL.** L105–109 has the ban, the incident, and 3+ files. | **The enumerate-call-sites-first procedure** (grep the name, categorise each site as annotation-only / subclassed / called / identity-checked, hand-apply only to safe ones); **the linter's own safe/unsafe classification is untrustworthy for re-exported names — it is semantics-preserving only under the usages it can see**; **run the affected tests BEFORE committing any autofix**; **`git checkout -- <paths>`, never a hard reset**; and the alternative to the cascade (a targeted suppression with a tracking note, or defer the triggering config change to its own change). | **UNSAFE-HIGH** |
| `feedback_plan_approval_gate.md` | **MOSTLY.** L82–86. | The **precise trigger** ("about to make the first edit in a session whose scope was not explicitly approved") and the **exception**: mechanical/verbatim items a reviewer demanded are the only low-discretion carve-out, **and even those are stated as "I'll do X first" before doing X.** | **UNSAFE-LOW** |
| `feedback_session_workflow_rules.md` | **PARTIAL — about half.** CM has W2/W5, M3, M8, M1, M6. | **Absent from CM: W1** atomic actions with a verification gate between each; **W3 verification authority lives OUTSIDE the model — "I tested locally" is one observation, not a verification**; **W4** one change at a time, finish or hand off cleanly; **W6 re-read prior decisions before each substantial action — do not generate the next action from the most-recent context alone**; **W7** surface uncertainty *before* predicting; **M2** test-scope defaults by change class; **M4** observation-first sentence structure with inference explicitly labelled; **M5** tag unavoidable predictions explicitly; **M7** surface drift in real time and name it. | **UNSAFE-HIGH** — largest single loss in the DELETE set |
| `feedback_subagent_model_inherit.md` | **YES.** L55. | Only the sweep grep for stray model pins under the config dir. | SAFE |
| `feedback_subagents_need_explicit_read.md` | **YES.** L55–57. | Nothing material. | SAFE |
| `feedback_thorough_review.md` | **MISLABELLED.** Its own banner says superseded by `feedback_thoroughness_over_momentum`, **not** by CLAUDE.md — and that superseder is **also in this DELETE set.** | Nothing beyond the superseder. | SAFE **but the label is wrong** |
| `feedback_thoroughness_over_momentum.md` | **PARTIAL.** CM L99 has only checkpoint 1 (the assumption stack). | **Checkpoint 2 — the sweep precedes the fix: enumerate the class, then fix all instances; never push the first instance "to keep moving" while promising the sweep later.** **Checkpoint 4 — completeness defines done: every real instance addressed, end state verified, enforcement in place.** **Checkpoint 5 — after the first surprise, the verification bar RISES; the urge to skip because "we're so close" is the tell.** Plus the named failure mode itself. | **UNSAFE-HIGH** |
| `feedback_trace_flows_not_claims.md` | **PARTIAL.** L96–98 lists the claim-sources and the evidence types. | **The ORDERED hierarchy with "never let a lower tier overrule a higher one"**; **debugging: trace the DATA — what value, through which functions, serialised how — rather than pattern-matching the error text to a remembered cause**; and **when a big infrastructure change merges, drift-audit the affected memory cluster against current code** (an event-driven retirement trigger CM does not carry). | **UNSAFE-HIGH** |
| `feedback_truth_over_optimism.md` | **PARTIAL.** L90–94 has the posture, the banned words, "name the bias", "I don't know". | **The SIX NAMED bias sources — CM says "name the optimism bias out loud when felt" but never supplies the taxonomy to name it with:** helpful=positive-prediction; vendor framing leak; completion-feel (work-done ≠ verification-done); speed-pressure rationalisation; politeness wrap-up defaults; **story-generation when asked "what happened?" — plausible narrative ≠ observation.** Plus the six-item honest-alternatives menu. | **UNSAFE-HIGH** |
| `feedback_user_owns_git_push.md` | **MOSTLY.** L73–77. | **On the one confirmation, surface the target AND the identity it goes out under (which account).** Plus restore the tree afterwards. | **UNSAFE-LOW** |
| `feedback_verify_before_asserting.md` | **PARTIAL.** L96–101 covers claim-scope, premise re-verification, the full-gate floor, and "redundant needs the same proof". | **When offering verification options, the RECOMMENDED one defaults to the BROADER scope — the narrower ones are for follow-up.** **When editing at a boundary, read the WHOLE file, not the diff hunk.** Plus the four-step audit protocol (read the exact function the ask references; read the actual failure log and check whether the fix exists on the base; confirm the bug is still present; verify scope from the diff, not the title). | **UNSAFE-HIGH** |
| `feedback_verify_remedy_before_public_post.md` | **YES.** L108–109. | Minor: the mechanism naming ("a remedy chosen to satisfy the checker rather than the root cause"); do not launder subagent output into a public recommendation. | SAFE |
| `user_profile.md` | **YES.** L19–27 carries every item. | Nothing. | SAFE |
| `feedback_iterative_planning.md` (AA) | **MISLABELLED — the claimed superseder is largely ABSENT.** CM L82 covers the approval gate only. | **Expect 2–3 rejection rounds; "one final pass" means a thorough verification round, NOT ship-now.** **Embed concrete viewable samples INSIDE the plan file — in plan mode the plan file is the only writable surface.** **Do not use a question-tool to ask "is this plan ready / should I proceed" — that is the plan-exit tool's job; the question tool is for clarifications and trade-off choices.** None of this is in CLAUDE.md. | **UNSAFE-HIGH** |
| `feedback_opus_verification.md` (AA) | **MISLABELLED.** The file's own banner says **STALE by contradiction** with the no-fan-out default — not superseded by CLAUDE.md. Its `model: "opus"` instruction actively contradicts CM L55. | The **three-lens verification design**, which is nowhere in CM: **a critic** (challenge end-to-end, findings severity-ranked), **a source-freshness verifier** (re-pull the current state of every cited external anchor — it found stale version pins and *every* cited path wrong), and **a completeness checker** (read every source the artifact claims to distil from and name what was lost or thinned). Also: **each prompt must be self-contained and rich because subagents start cold**, and **synthesise the findings yourself rather than passing them through.** | **UNSAFE-HIGH** (delete the model-pin, keep the three lenses) |
| `feedback-explicit-go-before-building.md` (AP) | **PARTIAL.** CM L82 covers research→code only. | **Approval is scoped to the STEP approved — completing step N is not authorisation for step N+1** (a "good to push" is not a go to start the next phase). This is broader than CM's planning→code rule. Plus: **even after an exhaustive design pass, do not tee up "build next" as the assumed step — let the owner declare when design is done.** | **UNSAFE-HIGH** |

**Systemic finding — chain deletion.** Seven `stale-or-dup` files in other silos defer to a
"canonical version kept in the salesagent silo". **Six of those seven canonical twins are
themselves in this DELETE set** (`feedback_no_internal_dialogue_in_public_artifacts`,
`feedback_no_pr_specifics_in_memory`, `feedback_user_owns_git_push`,
`feedback_verify_before_asserting`, `feedback_thoroughness_over_momentum`,
`feedback_subagent_model_inherit`, `feedback_no_agent_fanout_by_default`). Only
`feedback_no_blockquote_for_pasteable_text` has a surviving twin (routed
`SKILL:authoring-artifacts`). Executing both halves of the plan deletes the duplicate
*because* the canonical is kept, and the canonical *because* CLAUDE.md covers it — and
where CLAUDE.md's coverage is partial, both copies vanish.

*(Aside: `SKILL:authoring-artifacts` is a destination name in `route.py` that does not appear
in PLAN.md §4's 12-skill list — worth reconciling before execution.)*

## 2B. `stale-or-dup` (21) — anchors checked

Anchors I verified **directly, today** are marked ✅ VERIFIED-BY-ME. The rest are relayed
from the file's own audit annotation and are **NOT VERIFIED** by me.

| file | staleness claim | anchor check | P destroyed | verdict |
|---|---|---|---|---|
| `ci_refactor_rollout_state.md` (SA) | "all merged, nothing in flight" | NOT VERIFIED (needs `gh`) | **A planning corpus can live only on a branch — read it with `git show <branch>:<path>` without checking it out.** Plus a live security anchor: the hardening action's flag that actually mitigates the CVE is the combined one, not the narrower one. | UNSAFE-LOW |
| `feedback_a2a_harness_real_auth_chain.md` (SA) | "merged; harness now does it" | NOT VERIFIED | **Drive the REAL credential→lookup→identity chain in a boundary test rather than patching the two identity seams — patched seams verify the patch.** Keep the patched path only as the unit-mode fallback. Not covered by the kept sibling, which handles the entry-point half only. | **UNSAFE-HIGH** |
| `feedback_black_ruff_format_disagreement.md` (SA) | "resolved by the single-formatter change" | ✅ **VERIFIED-BY-ME**: `.pre-commit-config.yaml:151` has `id: ruff-format`; line 148 records that it replaced the other formatter's hook | The file's own labelled durable lesson: **two formatters with overlapping scope is a CONFIG DEFECT — converge on one, in lockstep with the checker; neither is idempotent on the other's output, and the second one reformats pre-existing lines in any file it touches, aborting commits mid-hook.** Plus the construct-level fix and an explicitly-flagged **unverified residue** (whether CI swallows format drift). | **UNSAFE-HIGH** |
| `precommit_mypy_adcp_pin.md` (SA) | "resolved; no pin to sync" | ✅ **VERIFIED-BY-ME**: no isolated-env hook; `entry: uv run mypy` at line 172; line 15 states deps resolve from the lock | The file's own labelled **"durable, transferable lesson": a type-checking hook running in an ISOLATED env with its own dependency declaration produces PHANTOM errors when its pin drifts from the runtime pin** — 249 of them at one point; a floating pin then drifted to a restructured version breaking different sites; **exact pinning matched to the runtime is the only stable state, bumped in the same change, with the hook cache cleared.** | **UNSAFE-HIGH** |
| `prek_pre_commit_replacement_bug.md` (SA) | "tool not installed; hook not generated by it" | ✅ **VERIFIED-BY-ME**: `which prek` → not found; no binary in the cache; `.git/hooks/pre-commit` header says it was generated by the standard tool | **A globally-installed drop-in replacement can silently take over a repo's hook and ignore the config's version pins, installing a build from upstream HEAD instead.** Diagnostic: **read the hook file's generator header before trusting the pinned version**; the fix is per-repo (`unset` the hooks path and reinstall). Still true even though this machine no longer has the tool. | **UNSAFE-HIGH** |
| `project_backlog_triage_2026_07.md` (SA) | snapshot of 151 issues | NOT VERIFIED; the file itself says "re-verify issue states before acting" | Thin: cluster a large backlog into root-cause groups; record deliberate splits that must NOT be re-merged. Belongs in the tracker, not memory. | SAFE |
| `project_sovereign_multiprotocol_rail.md` (SA) | superseded by the newer strategy silo | ✅ partial: both claimed clone paths exist | **The rewrite-strategy rule: greenfield the spine; COPY (do not rewrite) edge-case-dense modules verbatim WITH their tests; PORT the behavioural specs as the acceptance layer; drop the rest — and do NOT rewrite an edge-case core from the contract tests alone, because they under-specify the internal business rules.** Also: **strategy/motivation stays in private notes; the committed docs are the neutralised technical version.** | **UNSAFE-HIGH** |
| `reference_verify_spec_skill_not_portable.md` (SA) | third party's skill unusable here | NOT VERIFIED | **A third party's skill can hardcode their local clone paths and a stale version pin — check both before relying on it.** And: **a tool that MUTATES the files it inspects is wrong for reviewing someone else's change.** Plus the fallback (introspect the installed library, but ground the contract in the pinned spec prose). | **UNSAFE-HIGH** |
| `pr1389-property-list-context.md` (CCS) | wrong org in the reference | NOT VERIFIED (needs `gh`) | **A per-adapter declared-capability flag plus an explicit unsupported-error is how you avoid silently dropping a request feature.** Also: **a cache with no eviction is unbounded growth**; **a deliberate divergence from the upstream library must be labelled as deliberate**; **read a change's own "design decisions (locked)" section before flagging**; and **do not conflate two distinct error paths** (an outbound write failure is transient; a fetch of caller-supplied input splits correctable/transient by status). | UNSAFE-MEDIUM |
| `salesagent-worktree-layout.md` (CCS) | wrong org | NOT VERIFIED | **When the working directory is a stub, "this PR" means a sibling worktree — confirm which by recent mtime plus branch name.** And: **measure a review against the REPO's stated bar, not generic standards** (a repo may treat duplication as a correctness invariant with a ratcheting baseline and a mandatory pre-push full-suite gate). | UNSAFE-LOW |
| `adcp-kernel-empirical-anchors.md` (ADT) | consumer-side claims dead | NOT VERIFIED | **A four-way count drift between the published manifest, the generated enum, a hand-maintained table, and the consumer's own mapping — with a comment that LIES about its own count. The independent oracle is the published manifest; a self-guard asserting against the drifted table is CIRCULAR.** Plus: **one identifier space is release-precision on the wire while another is full-semver — a naive equality check false-positives, so normalise before comparing.** Plus: **an unknown-value policy that keys on the small table crushes the values the large one defines, and stamping "terminal" where the policy says "unknown ⇒ retryable" is self-contradictory.** | **UNSAFE-HIGH** |
| `project-tech-stack-audit.md` (AP) | "not yet in docs" is false | NOT VERIFIED | **Supply-chain flags that generalise: a thin single-maintainer library on the critical path should be vendored behind your own thin implementation; a package with no release in a year and issues disabled should be pinned and wrapped behind a swappable client; keep the type-gate on a checker NOT owned by the same vendor as the rest of your toolchain.** Plus: **record provider-specific sampling parameters as a provider-tagged union, because one provider rejects another's fields**; **a hosted model has no reproducibility guarantee, which UPGRADES record-and-replay from prudent to necessary**; and **an under-specified layer surfaced only when someone asked — "the whole web/API + data + tooling layer was never decided" was the real finding.** | **UNSAFE-HIGH** |
| `feedback-budget-is-a-resource.md` (NOR) | dup of the fan-out rule | ✅ superseder file exists — **but it is itself DELETE-routed** | **Not in CLAUDE.md: budget is a metered, WINDOW-EXPIRING resource, so its value is TIME-VARYING — scarce mid-window, near zero at reset. Waste runs in BOTH directions: heavy apparatus on light questions mid-window, AND surplus dying unused at reset while pre-approved heavy work sits parked. SURFACING the estimated cost and window position is the rule, not frugality per se. Near reset with surplus, drain the parked pre-approved queue — never invent work. Answer from knowledge first when confidence is high, marking uncertainty, instead of reflexively researching.** CM has "count the cost before spawning" and nothing else here. | **UNSAFE-HIGH** |
| `norbert-skills-temporal-design.md` (NOR) | strict subset of the kept file | ✅ superseder exists and is **PROJECT-kept** | One residue the superseder does **not** contain: **this harness only scans a flat skills directory — grouped subdirectories require a plugin manifest entry or symlink installs.** A portability fact. | UNSAFE-LOW |
| `feedback_multi_phase_workflow.md` (PAS) | **no annotation at all — the staleness claim is bare** | NOT VERIFIED | **After plan approval and before execution: compact the context, then cut the branch BEFORE any file edit.** Plus: **when the user is manually approving edits, batch tool calls to reduce approval prompts.** CM has neither. | UNSAFE-LOW |
| `feedback_no_blockquote_for_pasteable_text.md` (PAS) | dup of the salesagent twin | ✅ **VERIFIED-BY-ME**: twin exists and is routed **SKILL** (survives) | Nothing lost — the twin carries it. | SAFE |
| `feedback_no_internal_dialogue_in_public_artifacts.md` (PAS) | dup; "already in CLAUDE.md verbatim" | ✅ twin exists — **but is DELETE-routed**; CM's coverage is a compression, not verbatim | Two strip examples the twin lacks ("shouldn't have shipped" → "non-standard file, removed"; "so the two implementations re-align deliberately" → "for parity") plus **no self-deprecation about how the code came to be**. | UNSAFE-LOW (chain risk) |
| `feedback_no_pr_specifics_in_memory.md` (PAS) | dup | ✅ twin exists — **DELETE-routed** | Chain risk only. | UNSAFE-LOW (chain risk) |
| `feedback_push_policy.md` (PAS) | dup with a weaker exception | ✅ twin exists — **DELETE-routed** | Its distinct sharpening: **each push is its own action needing its own confirmation — an earlier phase-level authorisation does not carry**, and **state the suggested command and stop.** | UNSAFE-LOW (chain risk) |
| `feedback_review_rigor.md` (PAS) | dup of verify-before-asserting | ✅ twin exists — **DELETE-routed**; and the content is **not** a duplicate | **STEELMAN FIRST: ask "what could justify this?" — read the surrounding paragraphs, decision records, sibling files and commit messages, and assume the implementer knows things you do not. An excerpt is not the file: "X is missing" must be checked across the whole repo. Scan for an explicit prior decision (decision records, "notable design decisions", "risks", "out of scope", audit-correction tables) before raising. Frame an ambiguous finding as a QUESTION, not a defect. "I read your X but you may want to clarify Y" is acceptable; "X is wrong" is not, unless verified end-to-end.** This is a distinct discipline, absent from both the twin and CLAUDE.md. | **UNSAFE-HIGH** |
| `feedback_subagent_model.md` (PAS) | stale; contradicted by the inherit rule | ✅ **VERIFIED-BY-ME**: CM L55 states the opposite | Nothing — its instruction is actively wrong now. | SAFE (delete) |

---

# NOT VERIFIED — explicit list

These are claims I relayed rather than checked. I did **not** run `gh`, did not query GitHub,
did not start any container, and did not run any test suite.

1. Every `stale-or-dup` staleness claim that depends on GitHub state — merge status of the
   referenced changes, whether an issue is closed, whether a repo/PR resolves under a given
   org. That covers `ci_refactor_rollout_state`, `feedback_a2a_harness_real_auth_chain`,
   `project_backlog_triage_2026_07`, `pr1389-property-list-context`,
   `salesagent-worktree-layout`, and `feedback_opus_verification`'s contradiction claim.
2. `adcp-kernel-empirical-anchors` — the claim that the cited mapping "no longer exists" was
   not re-run; I did not grep the current source.
3. `project-tech-stack-audit` — the claim that both library decisions "landed in the docs"
   was not checked against the repo.
4. `project_sovereign_multiprotocol_rail` — the file:line anchors inside it (class counts,
   `exceptions.py` line numbers) were not re-derived; only the two clone paths were confirmed
   to exist.
5. `feedback_multi_phase_workflow` (PAS) — carries **no audit annotation**, so its
   `stale-or-dup` classification has no stated evidence at all and I could not test it.
6. All 66 PROJECT files' internal file:line citations. Several of those files **say
   themselves** that line citations drift; two of them contradict each other on a live
   detail (`reference_review_patterns` P24 documents a helper signature and a line-numbered
   test citation that `wire_envelope_policy` explicitly states are wrong). I flag the
   contradiction; I did not adjudicate it against the code.
7. Whether the routing destination `SKILL:authoring-artifacts` corresponds to a skill that
   will actually exist — it is not in PLAN.md §4's list.
8. I did not verify that the 121 SKILL-routed files (outside my scope) do not already
   capture some of the Ps I recorded above; a de-duplication pass against that set is still
   owed before anything is written into a skill body.
