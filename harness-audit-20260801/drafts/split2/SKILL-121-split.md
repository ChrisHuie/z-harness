# Gate 0 applied to all 121 SKILL-routed files

Every file read in full. P = what survives replacing every proper noun with its category and is
still actionable. I = what was deleted.

## The three numbers

| | count |
|---|---|
| **Yield a genuine, non-vacuous travelling principle** | **121 / 121** |
| **Pure instance (no P survives)** | **0** |
| **Already universal (I is illustrative only; deleting it loses nothing operational)** | **17** |

104 files need an actual split. 0 NONE verdicts — see "Where NONE was considered" at the end for
the four closest calls and why each was rejected.

## Destination codes

**P:** `AH` agent-harness · `AA` authoring-artifacts · `GW` git-workflow · `PRM` pr-review-method ·
`PA` prebid-adcp · `PR` prebid-repos · `PTT` python-tooling-traps · `TCI` testing-ci ·
`CMD` ~/.claude/CLAUDE.md · `CS` craft-skill · `CCF` craft-context-file · `RP` review-prompt · `TOOL` tool/hook

**I:** repo name · `MACHINE` (machine/harness-level; no repo owns it) · `DROP` · `stays-with-P`

`⇒` marks a **misroute**: P belongs somewhere other than where `route.py` put the whole file.
`(R2)` marks P already present verbatim-ish in `~/.claude/CLAUDE.md` — a duplicate to delete, not re-file.

---

## Bucket 1 — routed to `agent-harness` (14)

| # | file | P | P dest | I | I dest |
|---|---|---|---|---|---|
| 1 | crash_forensics_check_user_scope_first | Answer a loss question in the scope the asker NAMED before widening; the largest surviving artifact is evidence about itself, not theirs. Work whose only output is transcript prose must be streamed to a durable file as produced | CMD (scope half, R2) + AH (stream-findings half) | crash signature `*.meta.json` w/o `agent-*.jsonl`, ~100-byte parent transcript, tmutil/iTerm2/APFS checklist | MACHINE |
| 2 | docker_desktop_stale_singleton_after_crash | A launch that exits 0 while spawning NO process is a stale single-instance lock naming a dead PID or a reboot-cleared socket — check the lock before reinstalling | AH | `Singleton{Cookie,Lock,Socket}` paths, `unmarshaling start request: unexpected EOF` | MACHINE (R2 — already in CMD) |
| 3 | feedback-agent-token-budget | State agent count and token ceiling before a fan-out; batch by SHARED CONTEXT, not per-item — N-per-item designs re-read the same files N times | CMD (ceiling, R2) + AH (batching mechanism, new) | 10M-token audit, 93 findings, two user quotes | DROP |
| 4 | feedback-timeout-is-not-an-answer | A harness-injected "proceed on best judgment" after a question times out is not an answer — restate the question and stop; where a vendor default can't be disabled, a hook is the only reliable layer | CMD (R2) + TOOL | `AskUserQuestion` 60s, `askUserQuestionTimeout` enum, binary 2.1.201, `question_timeout_guard.sh` | MACHINE |
| 5 | feedback_detecting_subagent_overconfidence | Delegate output carries the same confident-wrong bias as yours: hedging words mark inference, a confidence figure without a sample size is not a measurement, "clean" needs the same verification as "violation". Producing output, pre-label observation vs inference and enumerate what you did NOT verify | CMD (R2) + AH (9+7 check lists) | "Agent C said MCP timeout is flakiness", PR #1273 | DROP |
| 6 | feedback_fanout_preflight_disk_and_docker | Preflight the shared resources a fan-out depends on BEFORE dispatch — at exhaustion every command fails on an unrelated path, and a mutating agent under exhaustion emits findings indistinguishable from real ones. Infra failure is neither pass nor fail | CMD (R2) + AH (reclaim order, never-cycle-mid-fanout) | `df -h /System/Volumes/Data`, ENOSPC, `TMPDIR` workaround, 4-of-6 died | MACHINE |
| 7 | feedback_isolate_mutation_testing_reviewers_in_worktrees | Any agent that mutates files to test them needs isolation; concurrent readers see half-mutated state and a status-clean check between another agent's edit and restore still RACES | CMD (R2) + AH | PR #1543, the two specialist agents, `delivery`/`generation` failures | DROP |
| 8 | feedback_no_mutation_agents_on_shared_worktree | Commit before dispatching mutating agents so their revert lands on your commit. When the review TARGET is itself a guard, every specialist mutates concurrently — "revert and verify clean" IS the collision path, not the fix; mandate isolation up front | CMD (commit-first, R2) + AH (collision-path insight, new) | 5 agents on one tree, `# MUTATION` markers, `_github_yaml_candidate → return False` | DROP |
| 9 | feedback_subagent_mechanism_is_a_claim | *(already universal)* A delegate's symptom is evidence; its because-Y is a hypothesis. The tell: ONE named cause for a symptom with several candidates and no experiment isolating it | CMD (R2) + AH (bisect technique) | `patch.dict(clear=True)` vs `importlib.reload` capturing an autouse mock | salesagent |
| 10 | feedback_two_phase_subagent_audit | After an agent does modifying work, run a second agent whose only job is to audit the first against an explicit Step-0 read list — the load-bearing variable is the explicit READ plus fresh context, not the model class | AH | PR #1306, the 5 named findings, `PrincipalFactory.make_identity()` | salesagent |
| 11 | installed-skills-are-frozen-snapshots | An installed copy of a skill/plugin is a point-in-time snapshot with a FLATTENED layout; when reviewing a branch that changes it, the repo tree is the doctrine and anything the loader serves is possibly stale. Say which one you used; if a dogfood run behaves like the old doctrine, suspect the snapshot before the change | **⇒ CS + RP** (not AH — this is skill authoring/review) | `~/.claude/skills/<n>/`, Jul-14 timestamp, PR-11 Step-4 row, nested-vs-flat reference layout | prompt-harness |
| 12 | reference_agent_worktree_isolation_may_provision_main | An isolation feature that provisions a fresh checkout may branch from the DEFAULT ref, not your HEAD — pass the target commit explicitly and make the agent assert it before trusting any result | CMD (R2) + AH | 2 of 4 on origin/main, `git checkout --detach <SHA>` sibling pattern | MACHINE |
| 13 | reference_background_agent_report_retrieval | A finished background agent may signal idle instead of delivering — instruct it to push its report on completion, and never read its raw output handle (it is the full transcript and overflows context) | CMD (R2) + AH | `idle_notification`, `SendMessage`, `TaskOutput`, `.output` symlink | MACHINE |
| 14 | worktree-window-helpers | A repeated multi-step environment action gets a NAMED verb, not a recalled sequence — and "open it in a window" needs a per-target checkout because editors reuse the window keyed to the FOLDER, so same-repo targets hijack each other | **⇒ GW** (not AH — worktree mechanics; GW's description already claims "worktrees") | `prw`/`iss`/`wtnew`/`wt-ls`, `~/.oh-my-zsh/custom/worktree-windows.zsh`, `WT_EDITOR` | MACHINE |

---

## Bucket 2 — routed to `authoring-artifacts` (8)

| # | file | P | P dest | I | I dest |
|---|---|---|---|---|---|
| 15 | feedback-patterns-not-people | *(already universal)* Authored docs describe ROLES and patterns, never named individuals; a heuristic keyed to one repo's quirk is one manifestation — key it to the pattern with a pluggable lexicon | AA (+ CMD Memory is the sibling rule) | the 2026-07-24 quote, `@reviewer-a` mock convention | rereview-agent |
| 16 | feedback_author_artifacts_with_investigation_rigor | An artifact authored for someone ELSE to act on gets the same tracing rigor as investigating a bug — prior-session and summary facts are claims, and a published wrong fact transfers the error to an implementer who builds on it in good faith. An onboarding-labelled task with an unmade design decision is a hard task wearing an easy label | CMD (rigor half, R2) + AA (easy-label half, new) | #1259, adcp 5.7.0 vs 4.3.0, `_handle_sync_creatives_skill` | salesagent |
| 17 | feedback_final_reports_must_be_contributor_actionable | A "final report" for an external actor is self-contained — per item: location, what's wrong, why it matters, the literal tested change, how to verify. Never a status summary; never a pointer to a local path the reader cannot open | AA | PR #1372, `/tmp/pr1372-improvements.patch` | DROP |
| 18 | feedback_issue_draft_neutrality | An artifact that surfaces a decision to a GROUP presents options and tradeoffs with no recommendation; a 1:1 proposal is the opposite — recommend. The discriminator is the audience, not the artifact type | AA | Q-006/Q-008, `findings/YYYY-MM-DD-q*-issue-draft.md`, atom files | agentic-advertising |
| 19 | feedback_no_blockquote_for_pasteable_text | Paste-bound text goes in a fenced block, never a quote-prefixed one — the per-line prefix must be stripped by hand. Discriminate on the request: "draft a comment for" is paste-bound, "what's the status" is read-only | AA (CMD issue-body line is the always-on anchor) | `> @reviewer —`, PR #1312 | DROP |
| 20 | feedback_no_issue_refs_in_comments | *(already universal)* A tracker identifier inside a comment duplicates traceability the VCS already carries and goes stale — a comment that restates metadata creates a second thing to keep true | AA | `# Fix for #1057` | DROP |
| 21 | feedback_no_pointless_comments | *(already universal)* Do not comment what the code already says; a comment explaining a standard framework idiom is a second copy of the same fact. Reserve comments for genuinely non-obvious logic, and prefer the docstring | AA (merge with #20 — one rule, two files) | mock patching, inline imports | DROP |
| 22 | feedback_pr_review_writeup_style | **(a)** Pick the artifact SHAPE before writing — a multi-finding consolidation and a single comment have different structures, and a rule stated only as a parenthetical exception reads as the rule's absence. **(b)** Every outbound artifact carries the verified head id and a footer split into Ran / Not-run. **(c)** The artifact is the deliverable, surfaced verbatim in the response — writing it to a file is additive, not the hand-off | **⇒ PRM** for (a)+(b) · AA for (c) · CMD (R2) for banned openers / floor-not-target | `/full-review` Step 7a, #1511/#1465/#1464, 4k–18k char lengths | salesagent |

---

## Bucket 3 — routed to `git-workflow` (13)

| # | file | P | P dest | I | I dest |
|---|---|---|---|---|---|
| 23 | backgrounded_commit_exit_code_masks_failure | A compound command reports the LAST stage's exit code, so a failed operation inside it reads as success. Run the fallible operation bare, and verify the STATE it was meant to change (did the ref advance?), never the reported exit code | GW | `git commit; echo; git log`, the black hook, `MM` status | salesagent |
| 24 | feedback_atomic_breaking_changes | A change that invalidates existing usage lands with every caller/fixture update in the SAME commit; if it must split, name the dependency loudly. A readiness claim cites the REMOTE head plus the unpushed set — the other party cannot see your local | GW (atomicity) + CMD (remote-head, R2) | PR #1273, `test_mode: str → Literal[...]`, `"stress"` | salesagent |
| 25 | feedback_cleanup_chain_use_semicolon_not_ampersand | Chain a mandatory cleanup with an UNCONDITIONAL separator or a trap — success-gated chaining skips it on non-zero exit, and for a stash that silently reverts your tree to the base state so a later "verify my changes" reads the wrong content. Better: don't mutate the working tree for a comparison at all | GW | `stash push && pytest && stash pop`, 10 files in `stash@{0}` | DROP |
| 26 | feedback_feature_branch_first | Cut the branch as the FIRST action of implementation, before any edit — branching retroactively hides in-flight work from the branch listing. Basing on a remote ref, suppress upstream tracking, or the first push resolves its DESTINATION from the tracked branch's name and gets rejected by protection | CMD (branch-first, R2) + GW (`--no-track` mechanism) | `feature/<slug>`, `GH013`, issue #1246 | salesagent |
| 27 | feedback_issue_vs_pr_number | *(already universal)* Issue and change-request identifiers draw from ONE shared counter, so they are never interchangeable — confirm the real identifier from the API after the object exists, and don't let a worktree named for one lull you into reusing that number | GW | #1394/#1395, `salesagent-1394` | DROP |
| 28 | feedback_merge_creates_duplication_sweep_ssot | **(a)** Merging the mainline into a long-lived branch CREATES duplication neither side had — no conflict fires because the files differ. After every merge, list helpers the merge brought in and grep branch-authored code for the same IDIOM, not the helper's name; sweep both directions. **(b)** A rebase can resurrect the line a relocation change moved, keeping BOTH — the fix becomes the dead copy while its comment still asserts it. Count the statement per revision; 1→1→2 is the tell | GW | `identifier_type_str()`, `enum_value`, #1389/#1399, R0801 | salesagent (zsh `$r:` modifier gotcha → MACHINE) |
| 29 | feedback_no_alarm_mid_operation | File state during an in-flight VCS/hook operation is TRANSIENT — a hook that stashes unstaged work makes files read as their committed content. Never describe state mid-operation; defer the observation, read the final state, then describe what happened | GW | `~/.cache/pre-commit/patch*`, `products.py:891` | MACHINE |
| 30 | feedback_verify_branch_currency_before_rebuild | Verify a branch's currency against the mainline BEFORE implementing a plan written against it — including whether the mainline already restructured the files the plan targets. A plan grounded on a stale base is itself stale. Detect "synced" by observation; never wait to be TOLD a state you can check | GW | ahead/behind + `merge-base --is-ancestor` + targeted `diff --stat` | stays-with-P (VCS vocabulary, not repo nouns) |
| 31 | gh_actions_dirty_merge_skips_pull_request_workflows | **(a)** When a request has an unresolvable merge state, the CI events needing a synthetic merge commit are SILENTLY SKIPPED — an empty run list for the pushed commit IS that failure, not "frozen tests". Assert both merge state AND run existence after every push. **(b)** Taking one commit from the mainline by cherry-pick instead of merging its full state is what turns a clean branch dirty | GW | `mergeStateStatus=DIRTY`, `pull_request_target`, `.ast-grep/rules/`, `.gitleaks.toml`, #1519/#1541 | salesagent |
| 32 | git_mv_then_add_old_path_aborts_commit | A VCS-staged rename stages the rename ONLY — working-tree edits to the new path stay unstaged. A later stage-command naming the now-nonexistent OLD path fatals and aborts the ENTIRE staging, so the commit ships an empty 100%-similarity rename. Diff-stat the staged content before committing any rename+edit | GW | PR #1307 T4, commits `2aa1c0356`/`129a1dd6f` | DROP |
| 33 | reference_git_grep_ere_no_word_boundary | A POSIX-ERE search silently ignores word-boundary escapes and returns EMPTY rather than erroring — so an "empty grep" is a hypothesis about your MATCHER, not a fact about the tree. Re-run with a matcher modelling every form before acting on absence | GW | `\bProperty\(`, the noqa incident | DROP |
| 34 | reference_release_please_pr_ci_mechanics | A request authored by an AUTOMATION identity can leave required checks in a pending-approval state that never reports, blocking it permanently — re-trigger by an event attributable to a HUMAN, not by a manual dispatch, because a manually-dispatched run's checks do not attach to the request's required contexts | GW | release-please, `GITHUB_TOKEN`, the approve endpoint, the main ruleset | salesagent |
| 35 | reference_stacked_pr_ci_does_not_run | Heavy CI is filtered by BASE branch, so a stacked change targeting another feature branch gets none of it — a short check list is EXPECTED, not a misconfiguration, and the local full suite is then the only gate. Scanner alerts on such a request are frozen at the last mainline-targeting scan | **⇒ TCI** (not GW — pure "can this green be trusted"; contains no git operation) | `ci.yml`/`codeql.yml` branch filters, #1274, `pip-audit`, `security-ignored-vulns.sh` | salesagent |

---

## Bucket 4 — routed to `pr-review-method` (42)

| # | file | P | P dest | I | I dest |
|---|---|---|---|---|---|
| 36 | feedback-adversarial-review-user-adjudicates | *(already universal)* Default to adversarial structure on substantive work — independent draft, then an attack pass that refutes with MEASUREMENT — and put acceptance of contestable findings to the user rather than auto-applying | PRM ⚠ **conflicts with CMD "default is a SINGLE pass, ZERO agents"** | the 2026-07-24 quote | rereview-agent |
| 37 | feedback-mirror-prose-not-enforcement | Reusing a hardened contract's VOCABULARY is not inheriting its MECHANISMS — a design reads correct precisely because it uses the right words. Per borrowed claim: which predicate/invariant/guard makes this true, and is it the SAME one the parent shipped? The adversary must be a FRESH reader — a synthesizer cannot red-team their own synthesis | **⇒ design/spec-review** (PRM's description scopes it to pull requests; category error) + CMD ≤3-line gate candidate | `sink-verify.md`/`payment-adapter.md`, PC5, SV1–8, RT1–9, I16, T15, UCAN/macaroon | agentic-prebid |
| 38 | feedback_audit_checklist_systematic | Before any ready/clean/addressed claim, run a WRITTEN checklist across every open item — an ad-hoc sweep with no written list narrows silently over time. Re-sweep after every commit. A delegate's "clean" needs the same verification as its "violation" | PRM (~15% of file) + CMD (symmetric verify, R2) | Dimensions C–H are ~85% repo-specific: `_impl`, `AdCPError`, cached schemas, `make quality`, `.duplication-baseline`, `tox -e unit`, FIXME format | salesagent |
| 39 | feedback_complete_claim_requires_full_pattern_enumeration | A completeness claim requires enumerating every known pattern across every OPEN item, from a written list — recency bias narrows the working set silently. **A decision to defer a FIX is not a decision to stop CHECKING** | PRM | #1313/14/15, R8 on #1276, `gh pr list --author "@me"` | salesagent |
| 40 | feedback_confession_marker_grep_before_ready | A codebase confesses its own debt in prose — keep-in-sync notes, docstrings admitting a test only checks setup, unverified defensive rationales. Grep the confessions before any ready claim: a keep-in-sync comment IS a deferred duplication, so extract it rather than document it | PRM + **TOOL candidate** (English-phrase regexes over a diff; T1/T2 pass) | PR #1389, the `-- src/`/`-- tests/` scoping, the spec-tracking exemption | stays-with-P (phrasings are English, not repo nouns) |
| 41 | feedback_defect_recurrence_is_the_root_cause | *(already universal)* Count the copies of a repeated shape BEFORE writing the fix — N>1 means the finding is a MISSING ABSTRACTION, not a site defect, and the metric framing produces a fix that leaves the cause in place. **Structural enabler: reviewers dispatched by DIMENSION own no defect living on the seam between two** | PRM + **⇒ AH** for the dimension-seam half (fan-out design) | the `except Exception` case, "11 mocks vs a ceiling of 10", charter §1.5b | salesagent |
| 42 | feedback_detector_surface_blindness | A detector's SCAN SET is part of its claim — a verdict complete and correct over the files it read says nothing about where the obligation actually lives. State the scan set with every verdict; when prose is the artifact of record, prose is in scope. Verify a detector in BOTH directions before repeating it | **⇒ TCI** (detector-verdict trust; it restates SHIP GATE item 4) | the version-citation detector, the PR-description surface, the DORMANT/LIVE false pair | salesagent |
| 43 | feedback_diff_scope_hides_duplicate_twin | "Is this new?" and "does an equivalent already exist?" are REPO-WIDE questions; answering them inside the diff answers a narrower question and reads as clean. Search the whole tree for every new top-level definition before treating it as new | PRM | `ssot_docstring_duplication.py --base origin/main`, the wire-helper exemplar | salesagent |
| 44 | feedback_duplicate_deletion_needs_reader_enumeration | Deleting one of N duplicate writes is a BEHAVIOUR claim and a green suite is not evidence for it — enumerate every reader on the success path AND every error path, and ask what each does when the state is ABSENT. **A duplicate is exactly where a coverage hole hides: the reason it went unnoticed is the reason its consumer is ungraded** | PRM (+ TCI: mutation measures coverage, not behaviour) | `_send_protocol_webhook`, 38 green tests, the rebase-produced double write | salesagent |
| 45 | feedback_evidence_is_not_a_disposition | *(already universal)* If a sentence inside a finding's EVIDENCE would be actionable alone, it is a finding — own row, own disposition. "Folds into X" is legitimate only when the same edit closes both; if the fix can land while the observation stays true, it does not fold | PRM | charter §3/§4b.0/§4b.5, the scheduler `RuntimeError` | salesagent |
| 46 | feedback_exception_class_partition_needs_condition_mapping | Branching on a TYPE to answer a control-flow question makes the type a PROXY — enumerate which CONDITIONS land in it and whether they all deserve the same answer to THIS question. Reusing a set built for a different consumer's question is a semantic-boundary violation even where it came from. One constant answering N questions is the tell; exporting it to a single home fixes the COPY and leaves the MEMBERSHIP untouched | PRM | psycopg2 hierarchy, `connection_invalidated`, SAVEPOINT | salesagent |
| 47 | feedback_findings_need_anchors_post_as_threads | *(already universal)* A finding delivered as prose inside one summary comment has no LIFECYCLE; as an anchored thread it lands in a queue, carries resolution state and survives a push. Remediation tracks the THREAD list, so unanchored items fall out unaddressed AND undeclined. Never reuse a finding ID across rounds. Being right is not being actionable | PRM | the 16-vs-9 round, the A2/A3 ID collision | DROP |
| 48 | feedback_github_ci_review_blind_spots | **(a)** Duplication ratchets scan only source scopes, so copy-paste inside CI config is invisible — worst on a security gate with two hand-synced verifiers. **(b)** Gate logic inside a config heredoc is untestable BY CONSTRUCTION — extraction is the prerequisite for coverage, so "add tests" is the wrong ask. **(c)** A security-workflow change can move WHICH events it runs on and relocate the merge-block outside the repo — diff triggers vs BASE and ask what still blocks a fresh, never-commented request | PRM for (b)+(c) · **⇒ TCI** for (a) | `check_code_duplication.py` SCOPES, `github_workflow_duplication.py`, `review-security` | salesagent |
| 49 | feedback_grade_remedy_composition_not_checkboxes | Confirming each prior item landed at a citable line verifies CHECKBOXES, not outcome. Two escapes: remedies COMPOSE (two individually-correct fixes to one path can cancel — order matters when a handler and a context manager interact through exit), and a remedy can fix ONE SIDE of its own mechanism (construct vs read, declare vs enforce, extract vs pin). Mutate the half nobody asked about | PRM | the SAVEPOINT+re-raise pair, the `NamedTuple` positional construction | salesagent |
| 50 | feedback_infra_limitation_needs_cost_estimate | *(already universal)* "The environment can't host this" is a load-bearing claim needing a throwaway prototype or a line-level cost read off the actual call sites — a TRUE premise does not license the deferral. An infra excuse is the easiest way to quietly take the smaller option, because it sounds like engineering judgement | CMD (R2) + PRM (body) | the BDD case, the ~55-line prototype, charter §1.7c | salesagent |
| 51 | feedback_new_abstraction_needs_adoption_completeness | Grade a new shared helper on N adopted / M ELIGIBLE, naming each holdout — the diff shows what changed and never what SHOULD have, so partial migration is the most likely defect in any change creating an abstraction. Then check WHICH unified behaviour it kept: a seam built from N divergent sites can ship the WEAKEST one everywhere. Enumerate the contract's PARTS as a table — a seam can own one part of five and be named for the whole | PRM | `run_isolated_batch`, 1-of-5 auth sites, charter §1.5c | salesagent |
| 52 | feedback_no_author_filter_on_first_audit_pass | Never filter by author on the FIRST pass of a feedback sweep — inventory everyone who commented, then narrow. Filtering first makes anything from a non-cited participant invisible, and the strategically important comment is often theirs | PRM | PR #1312, the "free in sdk 4.4+" comment | salesagent (jq shape stays-with-P) |
| 53 | feedback_no_cheap_wins_pings | *(already universal)* A nudge does not change the state it is nudging — never frame it as low-cost/high-return. "Wait for the reviewer" is itself a legitimate answer, named directly rather than dressed as activity | CMD Output (banned framing) + PRM | the user quote | DROP |
| 54 | feedback_no_optional_disposition | *(already universal)* "Optional / non-blocking / nice-to-have" is a banned disposition: it collapses SEVERITY (how wrong) and DISPOSITION (what happens next) and answers neither. Disposition is a closed enum always resolving to an action; low severity is not a reason to defer — only scope or correctness is. **No finding left undone AND untracked** | PRM | charter §1.8/§3/§4b.5; ⚠ cites `bd create` — **CMD says no beads/`bd`: R1 anchor failure** | salesagent |
| 55 | feedback_no_pr_count_reduction_framing | *(already universal)* Lead a prioritization with what each item UNBLOCKS, never with what it clears — a queue length is a UI number, not an outcome | CMD Output (merge with #53) + PRM | the user quote, the 3 docs PRs | DROP |
| 56 | feedback_pattern_extraction | The cited site is one of N — run the whole-tree scan BEFORE editing it, classify hits so you neither over- nor under-count, and don't say "addressed" until the scan is empty. If the pattern is machine-detectable, **the GUARD is the deliverable**, not the single migration — because this rule existing has not been enough: recall is not enforcement | PRM + CMD (build-the-gate, R2) | #1276/#1307, `ResolvedIdentity(`, the 24+ guards | salesagent |
| 57 | feedback_principled_scope_expansion | The scope test is not "is it in the title" but two questions: does the expansion let the change land at a more correct end state, and does its PRINCIPAL remain intact? Plumbing that COMPLETES the principal is in scope. If the expansion forces a title change you are drifting. **The strongest temptation is when the cleanup is obviously small — small ≠ in-scope** | PRM (+ CMD 3-file rule is adjacent) | the A2A `skill_handlers` gap, UP042/StrEnum | salesagent |
| 58 | feedback_prior_round_finding_needs_remeasure | *(already universal)* A finding YOU produced in an earlier round is a lead, not a fact — re-derive every number and every "because X" on the current tree before re-quoting. Applies hardest to text you proposed that the author already ADOPTED: nobody else will check it, and your wrong finding is then defended by your own prior authority | PRM + CMD (premise re-verify, R2) | the storyboard ratio, `basicConfig(force=True)`, charter §1.1b | salesagent |
| 59 | feedback_project_wide_pattern_understanding | A pattern understood in ONE cell of a repeated matrix is a HYPOTHESIS about the others — enumerate where it appears, read 2–3 sites across DIFFERENT areas, and state where it holds, where it intentionally diverges and why, and what enforces it. **When recording it, record its SCOPE explicitly so the next reader inherits the map, not a data point** | PRM + **⇒ CMD Memory** for the scope-recording clause | 3 transports, `_impl`, ~50 guards, P42, #1306/07/89 | salesagent |
| 60 | feedback_ready_claim_requires_fresh_pr_audit | A readiness claim about ANY item — including one you are not actively working — requires a fresh query of the feedback surfaces immediately before the claim. Inherited summary state is a starting hypothesis, never the final claim. Compare latest-feedback timestamp to latest-commit timestamp | PRM | PR #1276 R8 17:14Z vs commit 18:18Z | salesagent (endpoint set stays-with-P) |
| 61 | feedback_rereview_diff_against_current_main_not_mergebase | On a re-review of a stale-based change, the merge-base diff shows every commit as a legitimate addition — including commits whose content ALREADY landed via a sibling. Compare merge-base-diff against direct-diff-vs-current-mainline; present in the first, absent from the second = already upstream. **The highest-value class the fan-out misses, because it is invisible in the diff every specialist reads** | PRM (+ GW cross-note for two-dot/three-dot) | #1665, the mcp CVE bump, pinact `-no-api` | salesagent |
| 62 | feedback_reverify_pr_head_before_and_after_fanout | An active target MOVES while your review runs — pin every delegate to an explicit commit, have them assert it, and re-check the head AFTER they return, re-basing every finding onto the current one before consolidating | PRM + CMD (pin-the-SHA, R2) | PR #1543, `7ccc2efc7`/`387eb516a` | salesagent |
| 63 | feedback_review_consolidation_drops_findings | A review can FIND a real defect and still DROP it in consolidation — the failure is disposition, not severity. **De-dup by (path, line) is NOT synthesis**: it collapses the same finding raised twice and leaves same-CONCEPT findings at different paths as separate low-severity items, which is precisely the drop channel. Run de-dup → concept-unification at max member severity → symmetric verify | PRM | `check_code_duplication.py`, baseline 89→88, `disposition_ledger.py`, charter §4b | salesagent |
| 64 | feedback_review_deliverable_against_issue_contract | Grade a fix against the issue's BEHAVIOURAL done-contract, not the mechanism — tests one layer BELOW the boundary the contract names do not count. And trace the CHANGED VALUE into its downstream consumers, not just its construction: a "safe" default can be wrong three calls later. Mechanism-correct, contract-satisfied and downstream-safe are three separate axes | PRM | `to_brand_reference`, `_get_products_impl`, `brand_manifest_policy` | salesagent |
| 65 | feedback_review_tooling_prs_derive_invariant_and_probe_inputs | Catalog-driven review is strong at "does this violate a known pattern" and weak at "what invariant should this guarantee that nobody wrote down, and what inputs break it". For a tool/CLI change: state its CARDINAL invariant, adversarially enumerate the inputs and environment that defeat it, and RUN it end-to-end on real input — **its unit tests mock exactly the environment-bound layer where these bugs live** | PRM (heavy TCI overlap) | the dormancy checker, `FORCE_COLOR`, `-OO`, `full-review.md` Step 4 | salesagent |
| 66 | feedback_reviewer_first_not_steward | On someone else's change you are a REVIEWER: propose, never author, patch or push for them. A green CI rollup is NOT verification — the verdict comes from running the gate yourself on the ASSERTED head. And "clean and easy" almost always means NARROW, not correct: the narrow change is fine for its scope; calling the AREA clean is the over-optimism | PRM + CMD (never-push, R2) | #1439, 4785 passed, the `account`-five-ways subsystem | salesagent |
| 67 | feedback_root_cause_first | Before proposing or claiming, walk four levels: the change and its full execution path; everything it touches across boundaries; what MASKS or PROVES those interactions (does the test prove the code or the mock; is the green real); and what KEEPS it true after this session. **A proposal grounded only at level 1 is a surface patch** | PRM (>3 lines, so not CMD) | #1306/07/12/89 as the bar's provenance, the 3 transports | salesagent |
| 68 | feedback_semantic_ssot_defect_class | Semantic SSOT — one CONCEPT materialized twice — is a defect class distinct from textual-clone duplication, and every shape-modelling guard is blind to it (one constant as two literals, one invariant computed two ways, one concept named three ways). **And THE FIX COMMIT IS THE LEAST-REVIEWED, HIGHEST-RISK CODE: a clean verdict must be re-earned on the actual final head, never inherited across a new commit** | PRM (+ TCI for the re-earn clause) | #1312 round-4 cluster, `86400`, the 3 wire-payload names, R0801 | salesagent |
| 69 | feedback_single_source_of_truth_requires_deleting_duplicates | A consolidation claim requires DELETING the duplicates in the same change — otherwise the consolidation is fictional and the canonical thing's own "replaces the per-X helpers" docstring is not yet true. Reviewing one: count references to the old name (1 or 0). Prefer DERIVING a table from its source over maintaining it by hand | PRM | `build_two_layer_error_envelope()`, `_ERROR_CODE_TO_STATUS`, 401-vs-403 | salesagent |
| 70 | feedback_substrate_prs_need_production_callers | A new helper needs at least one PRODUCTION caller in the same change — helper + tests + aspirational docstring + zero callers means the contract has nothing to enforce and the layer above stacks on a fiction. **A guard pinning a helper as "the production path" when nothing calls it is checking dead code** | PRM + **⇒ TCI** for the guard half (verbatim duplicate of #114, which is already in TCI) | `_adcp_to_a2a_error`, `ContextManager.fail_step`, #1306 items #4/#11 | salesagent |
| 71 | feedback_verify_provenance_before_preexisting_claim | Before telling a reviewer a pattern is pre-existing, prove it: **a stable COUNT is not sufficient — the set can churn (remove N, add N)**. Prove the SET is byte-identical base-vs-head and that no commit in the range touched a matching line. And "pre-existing" ≠ "harmless" — frame the reply as a SCOPE decision, not a dispute of the finding | PRM | the pickaxe/blame/merge-base spellings | stays-with-P (VCS vocabulary) |
| 72 | feedback_verify_reviewer_fixes_against_code | A reviewer's proposed FIX can rest on a false premise about the code, the spec, or the test-environment taxonomy — applying it literally can REGRESS behaviour. Verify against the authority first; when the literal fix is wrong, achieve the reviewer's UNDERLYING GOAL and surface the deviation with evidence. **Applies hardest to items INHERITED from a colleague: deference is not verification, and an unsatisfiable ask sends the author to do impossible work and spends the credibility you need for the real items** | PRM | the 4 numbered #1307 cases, `_default_error_code`, `TaskNotFoundError`, charter §1.7b | salesagent |
| 73 | feedback_verify_worklist_items_real | *(already universal)* An extrapolated sibling work-list is a list of HYPOTHESES — verify each against the code before executing. A verified non-defect gets SKIPPED with one line of evidence; executing it to look list-complete churns files and bloats the change. **Padding is not thoroughness** | PRM | the 5 patterns, `recovery=`, f-string loggers | salesagent |
| 74 | pr_review_audit_workflow | Feedback lives in SEPARATE surfaces that must all be pulled up front; one endpoint misses items. Build a FLAT item-level checklist with a per-item verification COMMAND — "review 3 has 7 items" is not a checklist. Treat a composite comment as multiple items. Verify each with a code diff, never a commit message: **messages describe intent, not completeness** | PRM (founding content) | PR #1276, the endpoint + GraphQL spellings | stays-with-P (platform-generic, not repo) |
| 75 | reference_docker_image_cve_review_gotchas | Reviewing an image security fix is EMPIRICAL: build BOTH before and after with the gate's exact flags, scan both, confirm the cited findings are present-then-gone and the gate's exit flips. **And a scanner reporting "not scanned" is NOT "zero findings" — a tool's no-op mode reads as a clean result** | PRM + **⇒ TCI** for the not-scanned clause | Trivy `fs` vs `rootfs`, cross-compile output path, buildx `/private/tmp` xattr, Debian backport suffix | MACHINE + salesagent |
| 76 | reference_github_code_quality_no_path_exclusion | A vendor CAPABILITY gap misfiles as an internal configuration task — when a doc or issue says "add the exclusion in settings" and the setting does not exist, the blocker is upstream and the doc is wrong. A roadmap-dependent fact carries an expiry: re-verify before acting | PRM (misattribution rule) | the bot name, the community discussion, issue #1422, alembic globals | salesagent + **MEMORY with the expiry event on line 1** (the corpus's clearest Gate-5 candidate) |
| 77 | reference_github_inline_comment_anchoring | **An anchor the platform refuses is a SCOPE SIGNAL, not a formatting problem** — do not fall back to the nearest acceptable line. A refused anchor usually means the finding is about code the change did not touch, and asking the author to edit untouched code is scope creep. Compute the fix TARGET's intersection with ADDED lines (not added ∪ context); empty = body item. Verify the children landed — the container can post while they are dropped | PRM | the hunk-header regex, `on_message_send_stream` vs `on_message_send` | stays-with-P (platform vocabulary) |

---

## Bucket 5 — routed to `prebid-adcp` (7)

| # | file | P | P dest | I | I dest |
|---|---|---|---|---|---|
| 78 | feedback_ground_protocol_work_in_spec_not_assumptions | Ground behaviour in the NORMATIVE SOURCE for the pinned version before implementing — a downstream artifact shipping an error code tells you nothing about WHEN to emit it, and an internal contract/gap item is not the spec and can itself be wrong. **Two reviewers and three rounds can polish the wrong feature without questioning the premise** | PA + CMD (validate-against-source, R2) | PR #1312, AdCP 3.0.0/3.0.1 dates, #1303 item 7 | salesagent |
| 79 | feedback_primary_sources_over_press_coverage | *(already universal)* For any EXTERNAL system, a search summary or vendor writeup is a lead to a primary source, never the evidence. Order: the LOCALLY INSTALLED ARTIFACT (introspect it — ground truth, checked FIRST), then the normative repository (distinguish MUST from SHOULD for every security-relevant claim), then the implementation source — never its README. **Secondary sources compress away exactly the normative/advisory distinction the analysis depends on** | **⇒ CMD** (nothing AdCP about it; A1 — must hold before the first search; ≤3 lines) | the three plan rounds, site-packages, `.proto` | DROP |
| 80 | feedback_recovery_audit_coherence_blind_to_condition | A checker validating a PAIRING against a table is COHERENCE-only — it cannot see whether the pair fits the actual CONDITION. **A clean run means coherent, not correct.** When a sibling is cited as precedent, match the CONDITION to its SPECIFIC branch; a surface match is a false premise a strong reviewer will confirm and be wrong about | **⇒ TCI** for the coherence-vs-correctness half · PA for the condition→recovery grounding | PR #1698, `SERVICE_UNAVAILABLE`/`transient`, the sibling's line numbers | salesagent |
| 81 | reference-aamp-maturity-mid2026 | When mapping an umbrella standard, separate the RATIFIED object substrate from the reference-code-only wire layer — SDK churn is transient, the layer split is the constant. Bind to the frozen objects; cite the churning implementations as evidence only, never bind to their code. An umbrella that delegates its transport owns only object semantics, so the neutral cross-org primitives are the unowned seat | PA | the whole maturity map, version tags, enum divergences, `docs/design/aamp-substrate.md` | agentic-prebid (expiry: "when ARTF reaches FINAL") |
| 82 | reference_adcp_sdk_spec_mapping | When an SDK is code-generated from a versioned spec, the SDK↔spec mapping is usually NOT in package metadata — it is discoverable only per-artifact (a bundled version file, an accessor). Derive it from the artifact, never from release notes. **And a citation naming a spec version must name the version where the feature ENTERED, not the version you happen to pin** | PA | the entire 4.3.x→6.6.0 table, `ADCP_VERSION`, `STANDARD_ERROR_CODES`, `exceptions.py:117` | salesagent; ⚠ **file declares its own anchor unstable ("the pin MOVES") — R1 self-failing** |
| 83 | reference_adcp_spec_grounding | Establish WHICH version is authoritative first: the current pin by default, UNLESS declared work targets a different one — then that. Never ground against a version that is neither. **Fetch normative text VERBATIM and read it yourself — a summarizing fetch is unreliable for MUST / version-timing questions.** Prose plus the executable conformance artifact are ground truth; the implementation SDK, even when it ships a full reference impl, is a read-only cross-check that CAN diverge | PA + CMD candidate for the verbatim-fetch clause | `dist/docs/`, `dist/compliance/`, the 3.0.1 idempotency contract, `adcp==5.7.0` | salesagent |
| 84 | reference_sdk_bump_delta_verification | A dependency BUMP is typically scoped "fix what broke", which is blind to changes that break nothing: a new optional field, a field going optional, a removal, a renamed enum, a swapped nested type. Green CI proves only touched paths — the authoritative check is a full model-surface DELTA between the two versions, every unhandled change traced as a hypothesis to falsify. **And a code generator can DIVERGE from its spec and FORCE a value, so the wire can carry a spec-questionable value no local choice can fix** | **⇒ PTT** (generic bump methodology; exact sibling of #93) | the enum rename, the nested-type swap, the container `git ls-files` fallback | salesagent + MACHINE |

---

## Bucket 6 — routed to `prebid-repos` (8)

| # | file | P | P dest | I | I dest |
|---|---|---|---|---|---|
| 85 | feedback-firsthand-intel-outranks-docs | When the user is a FIRSTHAND source on a domain, their account outranks sanitized public documentation — a public record built to read neutral is not evidence against an insider account. **Keep MECHANISM research ("how does it work") separate from PREMISE ("is it real"): refining the how must never read as doubting the whether** | **⇒ CMD** (evidence hierarchy for a source class; A2 — nothing names it) | Interchange/AAO/Scope3/BOK, the 2026-07-11 session | agentic-prebid |
| 86 | project-non-commercial-identity | A neutrality guarantee is only as strong as the STRUCTURE enforcing it — "we promise fairness" is policy; "the approvers are the parties it would harm" is a mechanism. Prefer the CATEGORICAL form ("we structurally cannot") over case-by-case, because it dissolves a whole class of objections at once instead of answering instances | PR (also serves a design-review lens) | Prebid board composition, the AAO/BOK contrast, author-not-operate, the handling rule | agentic-prebid |
| 87 | project-salesagent-wire-vs-engine-control | Conform at the WIRE, own the ENGINE — a conformance change may EXTEND the engine, never weaken, inert or delete it, and a declaration-honesty problem is fixed in the DECLARATION, not by disabling the mechanism. **Install authority IN-REPO so control is citable law rather than per-PR argument: contributors follow the most legible authority, so if only the external spec is legible in-repo, every review converges on spec-shape** | PR + **⇒ CCF** for the legible-authority clause | PR #1546, the collaborator names, `ADVERTISED_ADCP_VERSIONS`, the review id | salesagent / agentic-prebid (expiry: "when the ADR PR lands") |
| 88 | project-scope3-interchange-posture | Separate a competitor's COMMERCIAL power (unwinnable, off-premise) from its STRUCTURAL power (the dissolvable part) and attack only the second — make the seat contestable and routable-around rather than removing it. Excluding the adversary makes YOU the concentrator. **And if a document cannot stand on its stated merits alone, it does not go in** | PR + CMD Output (last clause, R2) | interchange.io, AAO, `adagents.json`, `AuthorityAssertion`, the A1 requirements | agentic-prebid |
| 89 | project_port_skills_hardening | **(a)** A hardening program can be DOCTRINE-complete and CHECK-incomplete: a principle with no concrete mechanical check to fire on catches nothing. **(b)** A fix correct for ONE case becomes a bug when generalized to the template — verify a proposed default against the corpus's ACTUAL DISTRIBUTION before making it the default | **⇒ CS** (authoring mechanical checks into skills) + CMD (build-the-gate, R2) | phases 0–2, C10–C18, the branch name, the 261-adapter counts, per-phase progress | prebid-agent-skills (volatile; expiry: "when Phase 2 completes") |
| 90 | reference-salesagent-python-in-legacy | Verify a local clone's ORIGIN before treating its layout as the upstream project's — a diverged fork can carry the same name and a completely different structure, and every document grounded against the wrong one inherits the error. **When a correction lands, enumerate every artifact grounded on the wrong source and re-ground it, not just the sentence that was caught** | **⇒ GW** (check the remote before reasoning about a checkout) | the two repo identities, `_legacy/` paths, the mining SHA, the five affected docs | agentic-prebid |
| 91 | reference_port_fidelity_vs_target_norms | Porting between two homes: the TARGET's review norms beat literal source fidelity — fidelity is the means, conformance the end. Where they conflict the target wins; record the divergence and file it upstream so both sides re-align deliberately rather than silently. **A latent anti-pattern in the source is a bug to fix in the port, not a behaviour to reproduce** | PR (cue: "porting a module between two repos") | `getBidType`, 135/261, `pruneImps`, the canonical corpus file list | prebid-agent-skills |
| 92 | user-chrishuie-role | Attribute governance and maintainership to the ORGANIZATION, not the individual who operates the repos; when a design needs an "authority", resolve it to the real organizational structure, never a person or a code-invented body. **Corollary: do not invent operational scope for an organization just because a design could use it** | **⇒ CMD** ⚠ and reconcile with CMD's existing "`@chrishuie` for CODEOWNERS/reviewers" — different scopes, will read as conflicting | "not President, not Board", the GOVERNANCE.md instance, PMC chairs | agentic-prebid |

---

## Bucket 7 — routed to `python-tooling-traps` (10)

| # | file | P | P dest | I | I dest |
|---|---|---|---|---|---|
| 93 | feedback_dependency_widen_pr_frozen_lock_masks | A change that only WIDENS a version constraint is a NO-OP against a frozen lock — the installed version does not move, so green CI proves the OLD version still works, which was never in question; the detonation is latent, at the next lock refresh. **Verify by INSTALLING the target and running the actual import lines: a source-tree 404 at a tag is INFERENCE (built artifacts ship shim modules); the installed package is the OBSERVATION.** A ceiling pin with a rationale is a guardrail — widening it without the migration removes the guardrail | PTT | `fastmcp <3.3.0`→`<3.5.0`, `uv sync --frozen`, the CVE floor detail | salesagent (`timeout` absent → MACHINE) |
| 94 | feedback_precommit_black_shifts_line_allowlists | Any allowlist keyed by (file, line) breaks on ANY line-count change — yours, or a formatter running mid-commit. The trap: derive locally → commit → a hook reformats → the committed file has post-format lines and the same commit's allowlist has pre-format ones. Converge formatting FIRST, then derive. A guard reporting MORE violations than you knew existed in a file you didn't change structurally = suspect line shift first. **Prefer SYMBOL-keyed entries** | PTT + **⇒ TCI** for the symbol-keyed design lesson | the named guard + `KNOWN_VIOLATIONS`, PR #1356/#1276, #1370 | salesagent |
| 95 | feedback_refactor_makes_dead_patches_live | `from X import Y` binds a reference at import time, so patching `X.Y` does not affect a module that imported the name — moving a call into a helper in ANOTHER module changes where the callee resolves and makes previously-DEAD patches LIVE, with their stale return values now in effect. Grep for patches at BOTH sites, and run the FULL suite: **a test that "passes" may be passing via a dead patch falling back to the real call** | PTT | PR #1307, `resolve_principal_or_raise`, `DetachedInstanceError`, 4722 | salesagent |
| 96 | reference_httpx_ssrf_ip_pinning | Pinning a connection to a validated address must not disable hostname verification — an option that sets the TLS server name separately from the connect target silently removes the cert-hostname check, so a WRONG name still connects. Keep the request on the hostname and pin only the TCP target. Validate EVERY resolved address, not the first. **A pin reaching into a library's PRIVATE internals needs a failing oracle so an internals change reddens rather than silently reopening the hole** | PTT + **⇒ TCI** for the private-internals-oracle clause | `sni_hostname`, `httpcore._backends`, `_pool._network_backend`, the repo module path | salesagent |
| 97 | reference_lazy_imports_load_bearing | A function-local import is usually LOAD-BEARING, not style debt — three reasons that each break if hoisted: the suite patches at the SOURCE module (only a lazily re-resolved consumer sees the patch), cycle avoidance, and heavy/optional-dependency deferral. Check WHY before hoisting. **A lint config ignoring undefined-name means a broken hoist will not fail lint — only tests and an import check catch it** | PTT | `ProductUoW`, `get_db_session`, the admin↔core cycle, PR #1307, F821 | salesagent |
| 98 | reference_mcp_structured_content_bypasses_model_dump | Two transports can serialize the SAME model through DIFFERENT paths — one honours field-level exclusion and core serializers but BYPASSES method-level dump overrides, the other calls the method. So a field "excluded" only in the method override still ships on the first wire. To remove a field from ALL wires use the field-level mechanism. **"Wire verified via the dump method" is a claim about one transport only** | PTT (+ PRM project-wide-pattern lens) | fastmcp 3.2.0, `to_jsonable_python`, `_serialize_for_a2a`, PR #1399 | salesagent |
| 99 | reference_reload_captures_active_mock | Reloading a module re-executes its `from X import Y`, so if `X.Y` is patched the MOCK binds in PERMANENTLY — teardown restores `X.Y` and knows nothing about the copy the reload left. Never co-select suites whose fixtures differ in one process. Generalizable check: a mock object on a source-module attribute AFTER teardown means a reload ran under a patch. **The resulting failures manufacture false findings and VACUOUS passes simultaneously** | PTT | the conftest fixture, the polluter test, 11→7/4, the marker analysis | salesagent |
| 100 | reference_tox_commands_no_shell_substitution | A runner that execs its command list as argv WITHOUT a shell passes shell syntax through as literal tokens and fails at runtime — so a config line depending on substitution can be "verified" by READING it and still be broken. Verify config by RUNNING the environment, or at minimum by printing the resolved argv | PTT + **⇒ TCI** for empirical-over-static-on-config | tox 4, the `--fail-under=$(tr …)` line, `|| echo non-fatal` | salesagent |
| 101 | uv_frozen_for_commit_drift | A pre-commit hook whose entry RE-RESOLVES the dependency lock modifies a tracked file mid-commit, and the framework fails any hook that modifies tracked files — so the commit aborts citing a file unrelated to your change. Force the resolver to use the lock as-is. **A post-hoc restore cannot unblock it: the abort happens mid-hook** | PTT | `UV_FROZEN=1`, `uv.lock`, the obsolete SKIP note, #1370 | salesagent / MACHINE |
| 102 | uv_venv_corruption_reinstall | A package manager trusting its own METADATA rather than on-disk files reports an environment "in sync" while a package is unimportable — an import failure with the package listed installed means a corrupted environment, not a lock or code problem. **A type-only dependency being unimportable surfaces as SPURIOUS "unused ignore" errors, which reads as a code issue.** Verify the environment FIRST after any surprising failure following branch switches | PTT | `uv sync --reinstall`, the three named packages, PR #1307, 30 minutes | MACHINE |

---

## Bucket 8 — routed to `testing-ci` (19)

| # | file | P | P dest | I | I dest |
|---|---|---|---|---|---|
| 103 | feedback_always_improve_testing | Faced with delete-vs-resurrect for a dead or broken test, default to RESURRECT with strengthened assertions — reducing test surface is a regression even when the tests are currently dead code. Offer new edge cases in the same change rather than restoring the prior state; genuinely obsolete needs explicit confirmation | TCI + CMD (non-destructive-first, R2) | issue #1233 D11, the 22 marked tests | salesagent |
| 104 | feedback_ci_optimization_silent_noop | A conditionally-gated optimization can silently NO-OP through a working fallback while everything stays green — the gate is false at runtime, control falls to the else-path, every check passes, and the headline claim is unfulfilled. Verify it ENGAGED from the runtime log's per-branch marker, never from pass/fail. **Then: ENGAGEMENT ≠ BENEFIT — measure wall-clock against the no-optimization baseline, because a confirmed hit can deliver zero net speedup if a per-run tax dominates. A pattern borrowed from a build-only job does not transfer to a build-then-run job** | TCI | the cache flags, the two runtime var names, the action name, the 10 GB budget | salesagent |
| 105 | feedback_claimed_invariant_needs_failing_oracle | An invariant a change ASSERTS in prose is only real if a mechanism goes RED when it breaks. Five ways it LOOKS enforced and is not: convention/docstring only; an unreachable defensive clause; an adjacent test green for the WRONG reason; partial enumeration (3 of 4); and **a centralized helper whose revert reddens everything through every path — which looks like full coverage but proves nothing about whether each CALL SITE is independently guarded; mutate each site, not the helper.** This is the exact gap a green quality gate and a strong structural harness cannot see: they check the assertions you ENCODED, never the ones you only WROTE | TCI (CMD ≤3-line gate candidate) | the four round items, the named helper, #1647 | salesagent |
| 106 | feedback_empirical_over_static_guard_assessment | Static counting of which branches violate a tightened guard fails three distinguishable ways: scanning a head with the NEW cap counts the entire INHERITED population; intersecting hits with added lines over-reports moved/re-indented code; a net count delta folds in intervening rebases. **The SET (which items to look at) is reliable across methods; the per-site fix-list is not.** Gold standard: rebase onto the target and RUN the gate. Never hand anyone a "do exactly these N edits" list derived purely from static analysis | TCI | the two named guards and their caps, #1313, `merge-tree --write-tree` | salesagent |
| 107 | feedback_flaky_test_single_green_run_is_luck | A single green run on a timing-sensitive test proves nothing — a flaky test passes MOST of the time, so "all checks green" can be the lucky branch of a race. Run it N times (≥5) on the EXACT shipped artifact, never a reconstruction. **Treat a strict expected-failure as a load-bearing assertion: it must fail RELIABLY, not usually.** Duplicate/late async deliveries leaking past a single drain is a recurring trap — drain to quiescence | TCI | #1465, the named test, 2/6 then 8× | salesagent |
| 108 | feedback_guard_matcher_completeness | A structural guard catches only the syntactic forms its matcher MODELS — an empty allowlist plus a green run reads as "every site caught", but an unmodelled equivalent survives, often in the exact files you just migrated. Enumerate every syntactically-distinct spelling before coding the matcher, or document what is out of scope (never let the docstring overclaim). Add POSITIVE and NEGATIVE self-tests: **a guard that trivially returns zero because its matcher matches nothing is worse than no guard.** Treat "empty allowlist" as a hypothesis to falsify | TCI (implements SHIP GATE 1/2/5) | the named guard, `X is None` vs `not X`, the three-lines-away find | salesagent |
| 109 | feedback_mutation_oracle_selector_must_be_verified | "I broke the production line and nothing failed" is a claim about your SELECTOR, not the code, until the selector is proven to select the grading test. Establish the baseline count first — baseline-N-passed → mutated-N-passed is the only trustworthy shape; a changed DESELECTED count means the selector moved. Print the selected identifiers or drop the selector entirely. **An empty red set is a hypothesis, and the first thing to falsify is your own matcher** | TCI | the `-k` string, the scenario-naming derivation, charter §1.4 | salesagent |
| 110 | feedback_oracle_decay_from_redundant_mechanism | A failing oracle is NOT permanent. A test that once discriminated the fix from the bug can be silently NEUTERED by a later commit adding a redundant mechanism covering the same case — the oracle exists, passes, looks strong, and guards nothing. **Defence-in-depth accretes redundant protections on a hot path, so no single test isolates any one — and the path where a mechanism is the SOLE protection often has no test at all.** Re-mutation-test after any change to the guarded path; run an on/off matrix when two mechanisms coexist | TCI | `populate_existing=True`, the named transition seam, BDD gotcha #8 | salesagent |
| 111 | feedback_personal_harness_gitignored_only | Personal tooling that REVIEWS others' work must never mutate the shared repository or gate other contributors' builds — "enforce" in a personal-harness context means an IGNORED detector the agent runs, never a repo-level guard, which is a shared-project decision. Before creating any file under a tracked path as harness work, check whether it is ignored; if tracked, that is a separate opt-in. **The reviewer tier is the CEILING** | **⇒ CMD Permission boundaries** (A1: must hold before the first Write; A2: "strengthen the harness" names none of this) | the staged guard filename, the detector path, the 2026-07-09 halt | salesagent |
| 112 | feedback_short_circuit_coverage_illusion | A compound boolean guard can be fully "covered" while one operand is NEVER EVALUATED — the language short-circuits, so line and branch coverage both read as covered and the second operand is dead to the entire suite. Require one test per OPERAND and mutate each non-first operand away independently. Inverted in test code: `assert A and B` short-circuits on the first FALSE operand, hiding whether B was checked. **A detector can list operands; it cannot tell you which one a test evaluates — and "none does" is a legitimate answer** | TCI | the A2A ownership gate, the detector path, charter §1.5d | salesagent |
| 113 | feedback_snowball_to_avalanche | *(already universal)* Skipping the full gate once is fine; skipping it AFTER a skip already burned you in this session is the failure — the contract ESCALATES on the first surprise. Track skips across the session, not per commit. **The tell: if skipping is tempting right after a surprise, that is the avalanche signal** | TCI | commits B and C, the named suite, 11 failures | DROP |
| 114 | feedback_structural_guards_pin_production_paths | Every allowlist entry in a structural guard must point at something reachable from PRODUCTION — a guard pinning a dead helper is false confidence while the real path goes unguarded. Trace backwards for CALL sites (search the name with an open paren, excluding import lines); only the definition = pinning it is meaningless. **And make the walker index NESTED definitions — a top-level-only walker makes "we can't pin this" look true when it is a five-line fix** | TCI | `BOUNDARY_FUNCTIONS`, the three live paths, the deleted dead one | salesagent |
| 115 | feedback_verify_branch_runs_assert_head | A checkout can SILENTLY FAIL and leave you on the previous tree, so a multi-branch verification reports a false result — and the common cause is that the gate itself DIRTIES a tracked file (an auto-ratcheting baseline), which blocks the switch. Every per-branch run: discard the gate's dirt, switch, ASSERT the resolved commit equals the expected one AND the tree is clean, abort on mismatch. **Identical test COUNTS across "different" branches is the symptom that exposes a wrong-tree run** | TCI | `.duplication-baseline`, `make quality`, the Pattern-A guard, 4713 | salesagent |
| 116 | feedback_wire_shape_change_systematic_sweep | For a cross-cutting contract change the WHOLE sweep happens before the FIRST push — never one symptom per CI cycle. Read the schema first; never construct a payload from memory. Mirror a working sibling rather than re-inventing the shape. Enumerate every FORM of the old pattern across every suite and classify each hit. When CI surfaces a failure, ask what CLASS of assertion it represents. **And a wire VALUE change is a shape change with no structural old-form to grep — review it by CONSUMER; an oracle pinning the emitted value to the constant it is DERIVED from moves in lockstep and can never fail** | TCI | the field rename, the fixture name, the schema paths, the detector name | salesagent |
| 117 | reference_bdd_negative_scenario_vacuous_on_empty | A negative/counter assertion whose only check is set-EMPTINESS passes VACUOUSLY on an empty response — reachable whenever the query legitimately returns nothing. Pair it with a non-empty anchor. **And a guard that scans only one directory never sees assertions defined INLINE in the scenario module** | TCI | the step name, PR #1551, the use-case id, the wire guard name | salesagent |
| 118 | reference_guard_selftest_full_chain_over_leaf | A known-bad self-test must drive the guard's REAL entry point end-to-end against a synthetic bad input — a test calling only the extracted LEAF matcher reddens when the matcher is neutered but stays GREEN when the mid-chain walker or the file-filter is mutated to yield nothing, and the paired assertion is green-when-empty, so a real violation escapes with the whole suite green. Add a NEGATIVE case. **Prove completeness by mutating EACH LINK to yield nothing.** If a leaf was extracted SOLELY to enable a leaf test, inline it back once the full-chain test exists | TCI (implements SHIP GATE 1/4) | the detector name, the filter name, PR #1498 | salesagent (tmp-repo fixture recipe stays-with-P) |
| 119 | reference_release_jobs_invisible_to_pr_ci | Jobs gated on a RELEASE condition never run on a change request, so a fully green check list is no evidence the release path works — **the check list having zero release entries IS the tell.** Verify by SOURCE-TRACING: registry-push credentials are separate from signing identity; per-job permission grants must be cross-checked at EVERY use site (the lone exception is the bug); a "gate" in a job depending on the publish job runs AFTER publication and does not gate; a poll-for-completion loop must budget more than real wall-clock. **A presence-only structural check passes green through every one of these** | TCI | the gate expression, cosign/attestation internals, the SARIF permission, D47, the workflow-scope limit | salesagent |
| 120 | run_all_tests_congratulations_masks_failures | Never judge pass/fail from a runner's terminal TAIL — a runner prints its OWN success banner whenever ITS environments exit zero, and an aggregating environment can succeed while the suites it aggregates failed. The authoritative record is per-suite machine-readable output: parse each summary AND enumerate every outcome outside the expected set. **Corollaries: a scanner's tooling crash is not a finding — re-run before owning it; and log-capture assertions pass in isolation and fail in a full suite because other tests leave global logging state behind, so assert on BEHAVIOUR, not captured logs** | TCI | the banner string, the results-dir layout, the scanner's crash text | salesagent |
| 121 | synthetic-tests-self-confirm | Fixtures that mirror the parser's own assumptions are a SELF-REFERENTIAL oracle: a green suite proves the parser parses ITS OWN fixtures, not that it reads the real input. Commit ≥1 fixture from the REAL corpus, add a completeness oracle asserting a structural invariant over real data, and **when a tolerant parser silently skips unknown input, make the skip OBSERVABLE — a real-corpus fixture is the only thing that catches future schema drift** | TCI | the two YAML key forms, the missed code names, the overlay name | adcp-tooling |

---

## Misroutes created by whole-file assignment (23 + 1 added TOOL destination)

Where the principle belongs somewhere other than where `route.py` sent the file. The last row (#40)
is not a skill misroute — it is a destination whole-file routing cannot express at all.

| # | file | route.py said | P actually belongs | why |
|---|---|---|---|---|
| 11 | installed-skills-are-frozen-snapshots | agent-harness | **craft-skill / review-prompt** | it is about authoring and reviewing a skill, not running agents |
| 14 | worktree-window-helpers | agent-harness | **git-workflow** | worktree mechanics; GW's own description already claims "worktrees" |
| 22 | feedback_pr_review_writeup_style | authoring-artifacts | **pr-review-method** (shape + Ran/Not-run) | only the "surfaced verbatim, not as a file" clause is authoring |
| 35 | reference_stacked_pr_ci_does_not_run | git-workflow | **testing-ci** | contains no git operation; it is entirely "can this green be trusted" |
| 41 | feedback_defect_recurrence_is_the_root_cause | pr-review-method | **agent-harness** (partial) | "a defect on the seam between two dispatched dimensions is owned by neither" is fan-out design |
| 42 | feedback_detector_surface_blindness | pr-review-method | **testing-ci** | detector-verdict trust; it restates SHIP GATE item 4 |
| 37 | feedback-mirror-prose-not-enforcement | pr-review-method | **design/spec review — no home exists** | PRM's description scopes it to pull requests; this is contract design |
| 48 | feedback_github_ci_review_blind_spots | pr-review-method | **testing-ci** (partial) | the ratchet-scope-blindness clause is guard-scope, not PR review |
| 59 | feedback_project_wide_pattern_understanding | pr-review-method | **CLAUDE.md Memory** (partial) | "record the pattern's SCOPE explicitly" is a memory-authoring rule |
| 70 | feedback_substrate_prs_need_production_callers | pr-review-method | **testing-ci** (partial) | its guard clause is a verbatim duplicate of #114, already in TCI |
| 75 | reference_docker_image_cve_review_gotchas | pr-review-method | **testing-ci** (partial) | "a scanner's not-scanned mode is not zero findings" is verdict trust |
| 79 | feedback_primary_sources_over_press_coverage | prebid-adcp | **~/.claude/CLAUDE.md** | nothing AdCP about it; general research-evidence hierarchy, A1+A2 both fire |
| 80 | feedback_recovery_audit_coherence_blind_to_condition | prebid-adcp | **testing-ci** (partial) | "a coherence checker cannot see condition-correctness" is detector doctrine |
| 84 | reference_sdk_bump_delta_verification | prebid-adcp | **python-tooling-traps** | generic dependency-bump methodology; exact sibling of #93 |
| 85 | feedback-firsthand-intel-outranks-docs | prebid-repos | **~/.claude/CLAUDE.md** | evidence hierarchy for a source class; user-level, cue-free |
| 87 | project-salesagent-wire-vs-engine-control | prebid-repos | **craft-context-file** (partial) | "contributors follow the most legible in-repo authority" is context-file doctrine |
| 89 | project_port_skills_hardening | prebid-repos | **craft-skill** | the extractable P is about authoring mechanical checks into skills |
| 90 | reference-salesagent-python-in-legacy | prebid-repos | **git-workflow** | "check the remote before treating a clone as the project" |
| 92 | user-chrishuie-role | prebid-repos | **~/.claude/CLAUDE.md** | persistent authoring substitution about the user; cue-free |
| 94 | feedback_precommit_black_shifts_line_allowlists | python-tooling-traps | **testing-ci** (partial) | "prefer symbol-keyed to line-keyed allowlists" is guard authoring |
| 96 | reference_httpx_ssrf_ip_pinning | python-tooling-traps | **testing-ci** (partial) | "a pin into private internals needs a failing oracle" is oracle doctrine |
| 100 | reference_tox_commands_no_shell_substitution | python-tooling-traps | **testing-ci** (partial) | "verify config by running it" is empirical-over-static |
| 111 | feedback_personal_harness_gitignored_only | testing-ci | **CLAUDE.md Permission boundaries** | must hold before the first Write; nothing in the request names it |
| 40 | feedback_confession_marker_grep_before_ready | pr-review-method | **+ TOOL** | three English-phrase regexes over a diff; Gate 1 T1/T2 pass |

**Direction of the misroutes:** 9 of 24 point at `testing-ci`, and 5 point at `~/.claude/CLAUDE.md`.
`route.py` systematically under-routes to both — because a file's *instance* is a PR, a spec or a
dependency, while its *principle* is about whether a verdict can be trusted, or is a boundary that
must hold before the first tool call.

---

## Duplicate principles the split exposes (route.py cannot see these — it assigns whole files)

| principle | files stating it | buckets |
|---|---|---|
| An empty result set is a hypothesis about your MATCHER, not a fact about the tree | 33, 43, 108, 109 | GW, PRM, TCI×2 |
| A detector's SCAN SET is part of its claim | 42, 48a, 75, 108, 117 | PRM×3, TCI×2 |
| A guard pinning something with no production caller checks dead code | 70, 114 | PRM, TCI |
| Mutating agents need isolation; a shared tree races | 7, 8 | AH, AH (near-verbatim) |
| Never frame a no-op action as a win | 53, 55 | PRM, PRM |
| Personal-harness enforcement stays in ignored tooling | 68 (one clause), 111 | PRM, TCI |
| Validate against the SOURCE, never a derived authority | 46, 78, 80, 83 | PRM, PA×3 |

---

## Anchor failures and contradictions found while reading (Retirement triggers R1/R2)

- **#4** carries a `SUPERSEDED … Kept for history` banner inside a channel that still loads —
  ROUTING-RULE R1 calls that a defect, not a retirement.
- **#54** instructs `bd create`. `~/.claude/CLAUDE.md` says: no beads / `bd` / formula tooling. R1.
- **#82** declares its own anchor unstable ("the pin MOVES — never quote this literal"). R1 by construction.
- **#36** says "default to adversarial structures (drafter + attacker, multi-lens panels)";
  `~/.claude/CLAUDE.md` says "Default is a SINGLE pass, done myself, with ZERO spawned agents."
  Direct tension, both currently live.
- **#92** says attribute governance to the org, not the person; `~/.claude/CLAUDE.md` says use the
  personal handle for CODEOWNERS/reviewers/@-mentions. Different scopes, but they will read as
  conflicting to an agent authoring a governance file.
- **#22 / #67** use "gold standard" as an internal quality label; CLAUDE.md bans that phrase from
  outbound text. Internal use is legal, but it seeds the banned phrasing.
- **R2 (already in CLAUDE.md, delete the copy rather than re-file):** 1, 2, 3, 4, 5, 6, 7, 8, 9, 12,
  13, 16, 24, 26, 38, 50, 53, 55, 56, 58, 62, 66, 78, 88, 89, 103 — 26 files whose P is already
  always-on.

---

## A gap in the destination taxonomy

The brief offers three destinations for I: a named repo, DROP, or stays-with-P. **21 of the 121
instances belong to none of them** — they are machine/harness-level: crash-forensics signatures, the
desktop-app lock paths, disk/daemon preflight commands, the shell helper file, the agent-isolation
and background-report mechanics, venv corruption, the pre-commit stash cache, the build-host xattr
failure, the missing `timeout` binary. No repo owns them and DROP loses real value. They need a
fourth home — a machine-facts reference that is neither a repo nor always-on.

---

## Where NONE was considered and rejected (the four closest calls)

| file | substituted sentence I first wrote | why I rejected it and what I found one level up |
|---|---|---|
| 14 worktree-window-helpers | "keep useful shell aliases around" — vacuous | the mechanism is that editors reuse a window keyed to the FOLDER, so two targets in one repo collide; a per-target checkout is what buys isolation. That is specific and non-obvious |
| 2 docker_desktop_stale_singleton | "restart tools when they break" — vacuous | the mechanism is a single-instance lock naming a dead PID plus a reboot-cleared socket, producing exit-0-with-no-process. That signature is diagnosable anywhere |
| 21 feedback_no_pointless_comments | "be careful adding comments" — vacuous | the mechanism is that a comment restating the code creates a SECOND copy of the same fact to keep true — the same defect class as semantic-SSOT (#68) applied to prose |
| 76 reference_github_code_quality_no_path_exclusion | "check vendor docs" — vacuous | the mechanism is a vendor CAPABILITY gap misfiling as an internal configuration task, so an issue gets blocked on a person who cannot unblock it. The doc instructing a nonexistent step is the tell |

---

## NOT DONE

- **No files were edited.** Read-only on `~/.claude/` and all repos was honoured; the only write is
  this file.
- **P wording is not compressed to the ≤3-line CLAUDE.md cap.** Rows marked `CMD` name the
  destination; they are not drafted as always-on lines.
- **Gates 1–5 were not run per file.** The brief asked for Gate 0 only. Gate-1 TOOL candidacy is
  asserted for exactly one file (#40) on T1/T2 alone — T3/T4/T5 were not evaluated for it, and the
  six-item SHIP GATE was not applied to anything.
- **Cue words for the misrouted destinations were not grep'd against existing skill descriptions**
  (Gate 3's mechanical step). The misroute calls rest on reading each skill's description text, not
  on a grep.
- **The R2 duplicate list is by reading, not by diff.** I did not mechanically diff each P against
  `~/.claude/CLAUDE.md`; a byte-level pass would likely move a few files in or out of that list.
- **The 17 already-universal count uses one criterion** — "deleting every proper noun removes only
  illustrative examples". Six more files sit within one judgement call of that line
  (18, 57, 103, 112, 113, 121); a stricter or looser reading moves the number between 11 and 23.
- **The 121→0 NONE result is a claim about extractability, not about VALUE.** Several principles are
  thin (14, 20, 21, 76) and would earn their bytes only inside a skill body that costs nothing until
  it fires. None of them should go always-on.
- **No file was unreadable.** All 121 paths in the eight routing lists resolved and were read in
  full; total 342,921 bytes, 121 unique paths, no duplicates across lists.
